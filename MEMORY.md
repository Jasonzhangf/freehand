# MEMORY

- 2026-07-12: Worker provider/network failure classification now covers OpenAI-compatible provider errors as retryable system failures. `worker_execution_error_is_retryable_system_failure` maps `openai_http_request_failed`, `openai_stream_read_failed`, and retryable `openai_http_status_*` values to Worker `TaskInterrupted`, matching Anthropic behavior, while leaving `openai_adapter_failed`, `openai_callback_failed`, and content/deliverable failures non-retryable `TaskBlocked`. Local proof passed: OpenAI-sample `production_worker_runner_provider_error_records_interrupted_and_requeues_same_task`, non-provider blocked regression, classifier positive/negative regression, required live-dispatch failure projection tests, fmt, runtime check, mainlines generate/check, gates check, and diff check. S-profile 4042 proof passed after service-scoped `restartS` and `restartWorkerS`: health `ok`, ADP smoke, config `cc/openai/responses` on `api.anyint.ai` with env auth, permission preflight `status=ok`, fixed session `online-fixed-observability-standard` kept `runtime-turn-289` observable during pending and final states, and fixture env grep returned 0 matches. Historical Worker task `task-1783787230` remains blocked from earlier provider failure truth but is now observable through AgentBoard.

- 2026-07-12: Provider live bridge is now provider-neutral, with protocol-specific wire rendering/execution owned by provider crates instead of runtime hardcoding. `freehand-runtime` selects a `LiveProviderDriver` from config for Anthropic/messages, OpenAI/responses, or OpenAI/chat_completions. `freehand-provider-openai` owns `OpenAiExecutor`, endpoint selection, HTTP/SSE execution, raw capture, Responses tool declarations and `function_call` / `function_call_output` re-entry, plus Chat Completions tool declarations and assistant/tool-message re-entry. Local proof passed: OpenAI adapter tests, runtime focused descriptor/unsupported/error-metadata tests, `cargo check -p freehand-runtime`, fmt, mainlines generate/check, gates check, and diff check. S-profile 4042 online proof with RCC `cc/openai/responses` passed health and ADP smoke, then fixed Worker task `provider-openai-tool-proof-fixed` reached `TaskReviewSubmitted`; Worker session `worker-task-provider-openai-tool-proof-fixed` turn `worker-turn-exec-worker-worker-1783864675743871000-122-r4` ended success after OpenAI Responses raw ledgers showed available tool schemas and model calls `ls -> read_file -> complete_step`. Worker final summary proved `AGENTS.md` first heading was `# Freehand Project AGENTS`, and provider error query returned count 0. Remaining gap: Master did not approve/close that fixed proof task within the short observation window, and xiaozhi full E2E/HTML report remains incomplete.

- 2026-07-12: Master Worker dispatch is now explicitly lifecycle waiting, not user-task completion. `freehand-blocks` added completion claim `waiting`, `freehand-reason` and `freehand-runtime` persist it as `TerminalStatus::ToolPending`, and `ui.protocol` projects ToolPending terminal rows as `Lifecycle` / `running` instead of `Final` / `completed`. Runtime/tool guidance now says Worker dispatch, heartbeat, timer scheduling, and pending review are lifecycle progress; `claim="complete"` is reserved for verified final user-facing completion. WebUI lifecycle observer replaces debug framing, active Workers render first with distinct styling, and active Worker clicks select the TaskBoard-parented temporary worker session while refreshing TaskHistory/WorkerControl. Verified with `node --check`, focused blocks/ui-protocol/server tests, `cargo check -p freehand-reason -p freehand-runtime -p freehand-ui-protocol`, fmt, diff, mainlines/gates, S-profile `restartS`, health, ADP smoke, served asset checks, and Playwright DOM proof on `127.0.0.1:4042`. Remaining gap: no new real-provider long Worker task was run to completion in this slice.

- 2026-07-12: Freehand architecture governance now starts from a resource center before function-map refactor. Global `/Users/fanzhang/.codex/AGENTS.md`, global `coding-principals`, and compressed `dev-flow-code` now require MemoryPalace -> resource map / registry -> function map -> mainline call map/source -> verification map -> source search -> edit. Freehand added `docs/resource-maps/core.json` plus `docs/resource-maps/README.md`; the resource map defines `resource_type`, owner feature/crate, identity, truth store, operations, projections, operation bindings, direct/indirect relation rules, and forbidden direct relations. `xtask gates check` now validates resource-map parseability, unique resource types, owner feature presence, operation source/target resources, existing mainline call docs, relation resources, and forbidden direct relation `required_via`. First skeleton gate passed with `cargo fmt --check`, `cargo check -p xtask`, `cargo run -p xtask -- gates check`, `cargo run -p xtask -- mainlines check`, and `git diff --check`. Remaining gap: existing feature maps and mainline call maps are not yet fully migrated to resource operation bindings, so the new gate is a resource-center precondition, not full code-edge enforcement.

- 2026-07-12: Resource center migration advanced from skeleton to operation-backlink enforcement. Project `AGENTS.md` now says Freehand is resource-map-first ownership before function-map-bound development/debugging. `docs/resource-maps/core.json` covers the required key resources including `checkpoint`, `node_pairing`, and `instruction_capability`; operation bindings now include checkpoint, node pairing, and instruction capability edges, with future instruction-context admission explicitly `pending`. Relevant mainline JSON files now include `resource_operations` backlinks, paired function maps and test designs name `docs/resource-maps/core.json` plus operation ids, and generated wiki was regenerated from mainline sources. `xtask gates check` now enforces unique resource operation ids, mainline owner-feature match, operation backlink presence in mainline JSON/function map/test design, valid relation resources, and no direct operation binding for a pair declared indirect or forbidden. Verified with `jq empty docs/resource-maps/core.json`, `cargo fmt --check`, `cargo check -p xtask`, `cargo run -p xtask -- mainlines generate`, `cargo run -p xtask -- mainlines check`, `cargo run -p xtask -- gates check`, and `git diff --check`. Remaining deeper gap: gate does not yet statically inspect every Rust call edge for resource shortcuts.

- 2026-07-12: Resource operation backlinks are now call-table-row level for bound resource operations. `xtask` mainline docs parse optional `call_table[].resource_operation`, generated wiki renders a `resource operation` column, and `xtask gates check` requires every `bound` operation in `docs/resource-maps/core.json` to be referenced by at least one row in the operation's referenced mainline JSON. The gate also rejects row-level `resource_operation` values not listed in the same file's `resource_operations`. Current row backlinks cover 13 bound operations across reason persistence/context/turn, provider bridge, tool registry, UI protocol, runtime master-worker timer, error center, checkpoint rewind, node master-slave, and instruction capability loader. `instruction_capability.admit_request_context` remains `pending` and intentionally has no fake call-table binding. Verified with JSON parse, `cargo fmt --check`, `cargo check -p xtask`, `cargo run -p xtask -- mainlines generate`, `cargo run -p xtask -- mainlines check`, `cargo run -p xtask -- gates check`, and `git diff --check`. Remaining deeper gap: source-code shortcut scanning is not implemented yet.

- 2026-07-12: Mainline call-table rows with resource operations now carry explicit resource endpoints. `xtask` parses optional `call_table[].source_resource` and `call_table[].target_resource`, generated wiki renders source/target/resource operation columns, and `xtask gates check` requires row endpoints to match the exact `source_resource` and `target_resource` declared by the referenced operation in `docs/resource-maps/core.json`. Rows declaring source/target without `resource_operation` are rejected. Current endpoint triples cover 13 bound operation rows; `instruction_capability.admit_request_context` remains `pending` without a fake row binding. Verified with `jq empty`, `cargo fmt --check`, `cargo check -p xtask`, `cargo run -p xtask -- mainlines generate`, `cargo run -p xtask -- mainlines check`, `cargo run -p xtask -- gates check`, and `git diff --check`. Remaining deeper gap: Rust source call-graph scanning for undeclared resource edges is still not implemented.

- 2026-07-12: Resource source shortcut gate is now partially enforced from `docs/resource-maps/core.json`. The new `source_shortcut_gates` section lets selected forbidden direct relations declare `forbidden_packages` and `forbidden_import_tokens`; `xtask gates check` verifies the source resource owner crate `Cargo.toml` does not depend on those packages and its Rust files do not reference those tokens. First checked pairs: `ui_projection -> task`, `ui_projection -> session`, `metadata -> request_context`, `instruction_capability -> provider_request`, and `ui_projection -> node_pairing`. Runtime orchestrator pairs such as `timer -> task` and `runtime_command -> workspace_path` are intentionally not broad dependency-scanned because `freehand-runtime` legitimately depends on several owner crates; they require a later precise call-edge gate. Verified with `jq empty`, `cargo fmt --check`, `cargo check -p xtask`, `cargo run -p xtask -- mainlines generate`, `cargo run -p xtask -- mainlines check`, `cargo run -p xtask -- gates check`, and `git diff --check`.

- 2026-07-12: Function maps with resource operations now have required Resource Map Binding sections. `xtask gates check` requires every resource-backed function map to include `## Resource Map Binding`, `owned resources:`, `touched resources:`, and `forbidden shortcuts:`. The 11 currently resource-backed function maps were backfilled: reason persistence/context/turn, provider reason live bridge, tool registry, UI protocol, runtime master-worker loop, error center, runtime checkpoint rewind, node master-slave, and instruction capability loader. Each section names `docs/resource-maps/core.json`, owned resources, touched resources, resource operations, and forbidden shortcuts. Verified with rg coverage, `jq empty docs/resource-maps/core.json`, `cargo fmt --check`, `cargo check -p xtask`, `cargo run -p xtask -- mainlines generate`, `cargo run -p xtask -- mainlines check`, `cargo run -p xtask -- gates check`, and `git diff --check`.

- 2026-07-12: Resource operation test coverage is now gate-structured. The 11 current resource-backed test designs contain `## Resource Operation Test Coverage` tables mapping each operation id to `status`, `white-box`, `module black-box`, and `project black-box` coverage. `instruction_capability.admit_request_context` remains explicit `pending` and does not fake code/test binding. `xtask gates check` now requires every resource operation binding to have a matching table row in the paired test design, verifies row status equals the resource-map `binding_status`, and rejects empty coverage cells. Docs and local skill now record this as part of the resource-center workflow. Verified with `jq empty docs/resource-maps/core.json`, `cargo fmt --check`, `cargo check -p xtask`, `cargo run -p xtask -- mainlines generate`, `cargo run -p xtask -- mainlines check`, `cargo run -p xtask -- gates check`, `cargo test -p xtask -- --nocapture`, and `git diff --check`. Remaining deeper gap: full Rust source call-graph scanning for all undeclared resource edges is still future work.

- 2026-07-12: Forbidden direct resource relations now have explicit source-gate status. Each `forbidden_direct_relations` entry in `docs/resource-maps/core.json` declares `source_gate_status` and `source_gate_reason`; `checked` requires a matching `source_shortcut_gates` static dependency/token scan, and `deferred` documents why broad source scanning would be unsafe or imprecise. Current deferred pairs are `timer -> task` and `runtime_command -> workspace_path` because `freehand-runtime` legitimately orchestrates multiple owner crates and needs a later precise call-edge gate. `xtask gates check` rejects duplicate forbidden pairs, missing status/reason, checked pairs without a source shortcut gate, unsupported status values, and source shortcut gates that are not backed by a forbidden relation. Verified with `jq empty docs/resource-maps/core.json`, `cargo fmt --check`, `cargo check -p xtask`, `cargo test -p xtask -- --nocapture`, `cargo run -p xtask -- mainlines generate`, sequential `cargo run -p xtask -- mainlines check`, `cargo run -p xtask -- gates check`, and `git diff --check`.

- 2026-07-12: The two prior deferred runtime orchestrator resource shortcuts now have precise source-edge gates. `docs/resource-maps/core.json` adds `precise_source_edge_gates` and marks `timer -> task` plus `runtime_command -> workspace_path` as `source_gate_status=precise_checked`. `xtask gates check` validates each precise gate references a declared forbidden relation, file exists, symbol resolves, required owner-hop tokens are present in the function body, and forbidden direct shortcut tokens are absent. Current checks: `ProductionMasterRunner::handle_due_timer` must go through timer claim/live request/execute/complete tokens and must not call Task Center mutation tokens; `rewind_checkpoint` must construct `RuntimeCheckpointStore` and call `store.rewind` without direct filesystem/workspace/tool-execution tokens. Verified with `jq empty docs/resource-maps/core.json`, `cargo fmt --check`, `cargo check -p xtask`, `cargo test -p xtask -- --nocapture`, `cargo run -p xtask -- mainlines generate`, `cargo run -p xtask -- mainlines check`, `cargo run -p xtask -- gates check`, and `git diff --check`. Remaining gap: this is selected precise source-edge coverage, not a complete whole-repo Rust call graph for every undeclared resource edge.

- 2026-07-12: `source_gate_status=deferred` is no longer accepted for forbidden direct resource relations. `xtask` now parses source-gate status through `parse_source_gate_status`, accepts only `checked` and `precise_checked`, and has red test coverage `resource_source_gate_status_rejects_deferred`. Current resource map status counts are 5 `checked`, 2 `precise_checked`, and 0 `deferred`. Future forbidden shortcuts must add a broad source shortcut gate or a precise source-edge gate before landing in `forbidden_direct_relations`. Verified with `jq empty docs/resource-maps/core.json`, `cargo fmt --check`, `cargo check -p xtask`, `cargo test -p xtask -- --nocapture` (24 tests), `cargo run -p xtask -- gates check`, `cargo run -p xtask -- mainlines generate`, `cargo run -p xtask -- mainlines check`, and `git diff --check`.

- 2026-07-12: Resource map gate now has explicit positive/negative xtask coverage for registered direct mainline resource edges. `resource_map_accepts_registered_direct_edge` proves a call-table row with `source_resource`, `target_resource`, and matching `resource_operation` passes when backed by `operation_bindings`. `resource_map_rejects_unregistered_direct_edge_row` proves a mainline row that declares `source_resource`/`target_resource` without `resource_operation` fails with `declares source/target resource without resource_operation`. This locks the rule that machine-readable mainline direct resource edges cannot bypass the resource operation registry. Verified with `cargo test -p xtask -- --nocapture` (26 tests) plus the resource-map/mainline gate stack.

- 2026-07-12: Resource projection declaration is now a resource-center gate. Every resource in `docs/resource-maps/core.json` must declare at least one non-empty, unique projection, and `cargo test -p xtask resource_map_ -- --nocapture` includes `resource_map_rejects_missing_resource_projection` to prove projectionless resources fail. Projection means the named surface where a resource becomes observable or testable outside its owner, including UI protocol frames, tool results, debug/query rows, runtime receipts, ledger views, or internal wakeups.

- 2026-07-12: Operation binding contract fields are now explicitly gated. `verify_resource_map` rejects empty `operation_id`, `owner_feature_id`, `source_resource`, `target_resource`, `effect`, `mainline_call_doc`, and `binding_status` before backlink validation, and red test `resource_map_rejects_empty_operation_binding_effect` proves a binding cannot omit its human-readable resource effect. This closes the objective's requirement that each operation binding carries all declared contract fields rather than relying on later indirect failures.

- 2026-07-12: Relation rules now require explicit reasons. `verify_resource_map` rejects empty `relation_rules.reason`, and red test `resource_map_rejects_empty_relation_rule_reason` proves a direct/indirect resource relation cannot be only structurally declared without explaining why the direct edge is allowed or why the path must route through `via_resources`.

- 2026-07-12: Forbidden direct relations can no longer conflict with allowed direct relation rules. `verify_resource_map` rejects a forbidden direct relation whose source/target pair is also declared as `allowed_direct=true`, and red test `resource_map_rejects_forbidden_allowed_direct_conflict` proves one resource edge cannot be both directly allowed and forbidden in `docs/resource-maps/core.json`.

- 2026-07-12: Source shortcut gates can no longer be no-op structural backlinks. `verify_resource_map` rejects source shortcut gates with empty `reason`, rejects gates with both `forbidden_packages` and `forbidden_import_tokens` empty, and rejects empty entries inside those check lists. Red test `resource_map_rejects_noop_source_shortcut_gate` proves a checked forbidden relation must have at least one actual source boundary check.

- 2026-07-12: Source gate pairs are now unique in the resource map. `verify_resource_map` rejects duplicate `source_shortcut_gates` and duplicate `precise_source_edge_gates` source/target pairs, and red test `resource_map_rejects_duplicate_source_shortcut_gate_pair` proves duplicate source shortcut gates cannot silently collapse into a set before validation. Precise source-edge token lists also reject empty token entries.

- 2026-07-12: Forbidden direct relation reasons are now explicitly gated. `verify_resource_map` rejects empty `forbidden_direct_relations.reason`, and red test `resource_map_rejects_empty_forbidden_direct_relation_reason` proves a forbidden shortcut must explain why direct access is invalid in addition to declaring `required_via`.

- 2026-07-12: Duplicate precise source-edge gate coverage is now explicit. Red test `resource_map_rejects_duplicate_precise_source_edge_gate_pair` declares a `precise_checked` forbidden relation with two `precise_source_edge_gates` for the same resource pair and proves `verify_resource_map` rejects the duplicate pair before body scanning.

- 2026-07-12: Forbidden direct relations must be backed by matching indirect relation rules. `verify_resource_map` now requires every `forbidden_direct_relations` source/target pair to have an `allowed_direct=false` `relation_rules` row with identical `via_resources`/`required_via`; red test `resource_map_rejects_forbidden_without_indirect_relation_rule` proves a forbidden shortcut cannot exist only as a one-sided `required_via` note. Production resource map now declares indirect rules for `metadata -> request_context` and `ui_projection -> node_pairing`. Local proof passed: `jq empty docs/resource-maps/core.json`, `cargo fmt --check`, `cargo check -p xtask`, `cargo test -p xtask resource_map_ -- --nocapture` (19 tests), `cargo test -p xtask -- --nocapture` (43 tests), `cargo run -p xtask -- mainlines generate`, `cargo run -p xtask -- mainlines check`, `cargo run -p xtask -- gates check`, and `git diff --check`.

- 2026-07-12: Function-map resource backlinks are section-scoped and cannot be empty labels. `verify_resource_map` now extracts each paired function map's `## Resource Map Binding` section and requires non-empty `owned resources`, `touched resources`, `resource operations`, and `forbidden shortcuts`; the same section must name the resource operation id plus its source and target resources. Red test `resource_map_rejects_empty_function_map_resource_binding` proves a function map cannot satisfy resource-map binding through an empty `touched resources:` label or scattered prose outside the binding section. Local proof passed: `jq empty docs/resource-maps/core.json`, `cargo fmt --check`, `cargo check -p xtask`, `cargo test -p xtask resource_map_ -- --nocapture` (20 tests), `cargo test -p xtask -- --nocapture` (44 tests), `cargo run -p xtask -- mainlines generate`, `cargo run -p xtask -- mainlines check`, `cargo run -p xtask -- gates check`, and `git diff --check`.

- 2026-07-12: Bound resource-operation test coverage cannot use future/pending placeholders. `verify_resource_map` now finds `## Resource Operation Test Coverage` rows by exact first-cell operation id, preventing prose mentions in other cells from satisfying a row lookup. For `binding_status=bound`, coverage cells reject placeholder language including `pending`, `future`, `not claimed`, `not yet`, `todo`, and `tbd`; red tests `resource_map_rejects_pending_coverage_for_bound_operation` and `resource_map_rejects_operation_only_mentioned_in_wrong_coverage_cell` lock both cases. Local proof passed: no bound coverage rows contain those placeholders, `jq empty docs/resource-maps/core.json`, `cargo fmt --check`, `cargo check -p xtask`, `cargo test -p xtask resource_map_ -- --nocapture` (22 tests), `cargo test -p xtask -- --nocapture` (46 tests), `cargo run -p xtask -- mainlines generate`, `cargo run -p xtask -- mainlines check`, `cargo run -p xtask -- gates check`, and `git diff --check`.

- 2026-07-12: Master live tool guidance and framework orchestration are now closed for the reused xiaozhi prompt. Master provider schema is framework-only (`task`, `timer`) and excludes file/search/write tools, shell, `todo_write`, and `complete_step`; injected Master non-framework calls return paired failed capability-boundary tool results with Worker dispatch guidance and no file-content leak. Completion guidance locks `continue` to immediate same-turn rounds, optional `freehand_status` is omitted in ordinary responses, task calls require top-level `op` with concrete create/assign examples, `target_cwd` must be an expanded absolute existing repo/workspace path, and Master cannot claim a timer exists unless the current turn got a successful `Timer scheduled` result. Online S-profile proof on `127.0.0.1:4042`: fixed session `webui-session-fixed-xiaozhi-tool-retest-v6` submitted the exact prompt `分析 ~/Documents/github/xiaozhi-esp32-2.2.4 ... html ...`; receipt was `reason_live_turn_completed rounds=3 schema_rejections=0 tool_executions=2`; tool calls were exactly `task(op="query", task_id="task-1783783921")` and `timer(op="schedule", delay_seconds=300, ...)`; forbidden Master calls count was 0 for `read_file/ls/grep/glob/write_file/edit_file/multi_edit/complete_step/todo_write/bash`; timer result was `Timer scheduled: timer_id=timer-master-1783789815 next_due_at=1783790115 max_runs=1 fired_count=0 status=active`; terminal success cited Worker task `task-1783783921`, target cwd `/Users/fanzhang/Documents/github/xiaozhi-esp32-2.2.4`, active execution `exec-worker-worker-1783789054065083000-1651`, and timer `timer-master-1783789815`. Local proof: focused freehand-tools/freehand-blocks/freehand-runtime tests for master surface, glob/task/timer guidance, completion guidance, injected Master read rejection, tool-result continuation, and long Master prompt contract passed; `cargo fmt --check`, `cargo run -p xtask -- mainlines generate/check`, `cargo run -p xtask -- gates check`, and `git diff --check` passed. Final config remained minimax/MiniMax-M3 inline auth with no provider/autonomy fixture env. Marker: `master-framework-tool-guidance-closeout-1783789815`.

- 2026-07-11: Tool-call guidance must prevent model trial/error by putting current framework behavior and valid first calls into context/schema. Latest failure analysis used session `webui-session-20260711144341-d20c3ef1`: the model first called `glob("/Users/*/Documents/github/xiaozhi-esp32-2.2.4")` and hit `absolute patterns are not supported`, then tried `glob("~/**/xiaozhi-esp32-2.2.4")`, later called `task(op=list_tasks,status="all")` and hit `unsupported task status all`, and created/assigned `task-1783781111` before cancelling it as a duplicate. Root cause: `glob`/`task` schemas and Master context did not make framework/path/status/dispatch behavior explicit enough, so the model spent rounds probing the framework. Fix: `glob` schema now says relative-only and no absolute/tilde; `task` schema points to `TaskSpaceSnapshot`, says omit `status` for all visible tasks, lists valid statuses including `interrupted`, and documents `dispatch.mode="none"` plus configured Worker assign; Master live context injects volatile `TaskSpaceSnapshot` from TaskBoard/AgentBoard/EventInbox before the original task. Local proof: `cargo test -p freehand-tools glob_tool_schema_prevents_absolute_or_tilde_trial_calls -- --nocapture`, `cargo test -p freehand-tools task_tool_exposes_operation_parameter -- --nocapture`, `cargo test -p freehand-runtime live_bridge_admits_long_operator_task_without_semantic_truncation -- --nocapture`, `cargo fmt --check`, `cargo run -p xtask -- mainlines generate/check`, `cargo run -p xtask -- gates check`, and `git diff --check` passed. Online S-profile proof after service-scoped `scripts/install-launchd.sh restartS`: fixed session `webui-session-fixed-tool-guidance-proof` completed `runtime-turn-272` with `rounds=1 tool_executions=0`; model output explicitly cited configured worker, task space, agents, review queue, and recent events from injected snapshot; state payload contains `task-space-snapshot`, `<freehand_task_space>`, `valid_task_status_filters`, and `Do not call status="all"`. Config remained `provider=minimax`, `base_url_host=api.minimaxi.com`, `default_model=MiniMax-M3`, `auth_source=inline`, with no provider/autonomy fixture env. Marker: `tool-guidance-taskspace-snapshot-closeout-1783782834`.

- 2026-07-11: WebUI/global session list is now persisted-session-only and worker/subagent sessions are nested under their owning master session. `ui.protocol` owns the top-level filter: `QuerySessionList` / archived list returns only owner-supplied session metadata from `CreateSession` / session metadata truth; turn-only sessions no longer become global sessions. Internal framework sessions `master-lifecycle-*`, `master-timer-*`, and `worker-task-*` are hidden from global lists but remain directly queryable via `QuerySessionTurns` for debug/replay. `UiTaskSnapshotProjection.parent_session_id` now carries TaskCenter parent truth from `TaskSnapshot.parent.session_id`; WebUI uses that owner truth to render `worker-task-<task>` rows only as indented temporary child rows under the persisted parent session and excludes child rows from rename/remove/select-all. Online proof on S-profile `127.0.0.1:4042` used fixed session `webui-session-fixed-subagent-proof` and fixed task `task-fixed-subagent-proof` (no random test sessions): ADP owner query showed fixed session top-level, no top-level `worker-task-*`/`master-timer-*`/`master-lifecycle-*`, and task parent `webui-session-fixed-subagent-proof`; browser proof `artifacts/webui-online/fixed-session-subagent-1783780857107/summary.json` passed with `fixedChildExists=true`, `fixedChildKind=worker`, `workerTopLevelRows=[]`, `masterLifecycleVisible=false`, and `masterTimerVisible=false`. Local proof: `node --check apps/freehand-server/assets/webui.js`, `cargo check -p freehand-runtime -p freehand-cli -p freehand-server`, `cargo test -p freehand-ui-protocol session_list -- --nocapture`, `cargo test -p freehand-server webui -- --nocapture`, `cargo test -p freehand-runtime runtime_dispatches_phase2a_master_worker_loop_into_task_truth -- --nocapture`, `cargo fmt --check`, `cargo run -p xtask -- mainlines generate/check`, `cargo run -p xtask -- gates check`, and `git diff --check` passed. Marker: `metadata-only-session-list-worker-child-closeout-1783780857107`.

- 2026-07-11: Timer/wakeup is now an independent standard internal framework tool, not task truth. `tool.registry` owns the `timer` schema and exposes `timer(op="schedule"|"cancel"|"list")` with relative delay, absolute unix timestamp, local-time recurring interval/daily/weekly rules, strict 5-field local-time cron expressions, weekdays, skip weekends, max runs, reason, persisted wakeup prompt, and model-facing examples. Worker tool surfaces exclude `timer`. `runtime.master-worker-loop` owns durable timer execution: schedules live under `~/.freehand/state/timers/<agent>.json`, timer ledger rows live under `~/.freehand/ledgers/timers/<agent>.jsonl`, and the Master runner claims due timers before Task Center EventInbox processing. Due timer wakeups start an internal Master turn with the persisted prompt; one-shot timers complete, recurring timers reschedule until max runs, and failed wakeups record timer failure truth and release the schedule back to active retryable state instead of getting stuck in `running`. Do not encode wait/schedule semantics as `task(op="wait")`, task notes, or task lifecycle state. Local proof: `cargo test -p freehand-tools -- --nocapture` passed 34 tests; `cargo test -p freehand-runtime timer -- --nocapture` passed 7 timer tests; `cargo test -p freehand-runtime production_master_runner_ -- --nocapture` passed 9 Master runner tests; `cargo test -p freehand-runtime -- --nocapture` passed 133 tests; targeted clippy, fmt, mainlines generate/check, gates check, and `git diff --check` passed. S-profile online proof: `scripts/verify-timer-tool-online.sh` passed on `127.0.0.1:4042` with timer `timer-online-proof-1783774257-95369`, `mock_attempts=3`, ADP receipt `reason_live_turn_completed rounds=2 tool_executions=1`, fixture second request `sawToolResult=true`, third provider request `sawTimerWakeup=true`, persisted schedule completed with `fired_count=1`, and timer ledger contained `TimerScheduled`, `TimerFired`, and `TimerCompleted`. Restart recovery online proof: `FREEHAND_TIMER_VERIFY_MODE=restart-due scripts/verify-timer-tool-online.sh` passed with timer `timer-online-proof-1783774667-24465`, first persisted as `status=active next_due_at=1783774692`, then service-scoped `restartS` ran before due; after restart the Master runner claimed the overdue timer from durable state, third provider request had `sawTimerWakeup=true`, state became `completed fired_count=1`, and ledger had `TimerFired`/`TimerCompleted`. Post-proof config remained `provider=minimax`, `base_url_host=api.minimaxi.com`, `default_model=MiniMax-M3`, `auth_source=inline`, and fixture env grep had 0 matches. Marker: `independent-timer-internal-tool-20260711`.

- 2026-07-11: Provider retry and Worker task recovery semantics were corrected and online-verified. Recoverable non-stream provider failures retry internally for exactly 10 attempts; production backoff varies between 1s and 20s, and fixture proof may set `FREEHAND_PROVIDER_RETRY_BACKOFF_MS=0` only to shorten waiting while preserving retry cap/classification. Intermediate provider retries remain internal provider/error-center evidence and do not become task/user-visible state. Final provider/network exhaustion in a Worker execution is recorded as `TaskInterrupted`, not `TaskBlocked`, so the same task is requeued to the configured Worker with a new execution id; content/path/model-terminal failures still record `TaskBlocked` and are not silently retried. Owner changes: `task.orchestration` added `ExecutionFactKind::Interrupted`; `runtime.master-worker-loop` maps explicit provider/network executor failure codes to interrupted same-task retry and keeps ordinary execution errors blocked; `ui.protocol` can carry interrupted execution facts; provider retry cap changed from 5 to 10 with 1-20s bounded varied production backoff. Online proof on S-profile `127.0.0.1:4042`: `scripts/verify-provider-retry-online.sh` passed for session `cli-adp-sample-provider-retry-1783760766062266000` with `mock_attempts=10`, error-center rows `retry_same_step` x9 then `fail_turn` x1 for `anthropic_http_status_500`, and config/env restored to minimax/MiniMax-M3 with no fixture env. `scripts/verify-normal-master-worker-e2e.sh` passed after fixing the autonomy fixture to include a real temporary `target_cwd`; success path no longer had `TaskBlocked`, and crash recovery same-task history for `task-normal-crash-1781783761126` included `TaskInterrupted,TaskAssigned,...,TaskClosed`. Local proof included task/runtime/ui-protocol/cli/control tests, daemon error-center focused test, targeted clippy, fmt, mainlines check, gates check, and `git diff --check`. Marker: `provider-retry-worker-interrupted-closeout-1783761041`.

- 2026-07-11: WebUI scroll anchoring, composer clearance, and internal lifecycle session projection are now closed. `app.webui-smoke` tracks a `userScrollLocked` state so live render updates do not pull the operator back to the bottom while reading older transcript content; ordinary render updates no longer call `scrollIntoView`. The composer bottom gap is measured from the real `.composer-card` height and exported as `--composer-clearance`, preventing the latest row from being hidden under fixed/sticky composer chrome. `ui.protocol` filters `master-lifecycle-*` from user-facing active/archived session lists while direct `QuerySessionTurns` remains queryable for debug/replay truth. `runtime.master-worker-loop` now uses task-scoped internal lifecycle session ids (`master-lifecycle-<task>`) with event/attempt isolation in turn and trace ids. Online proof on S-profile `127.0.0.1:4042`: `make verify-webui-online` passed at `artifacts/webui-online/20260711-verify-4042-1783757223354/summary.json`; new checks true: `scrollLockHasScrollableTranscript`, `scrollLockPreservesManualReadPosition`, `bottomPinnedRefreshKeepsLatestVisible`, `composerClearanceApplied`, `sessionListHidesInternalLifecycle`; `all_false=[]`. Marker: `webui-scroll-lifecycle-session-closeout-1783757223354`.

- 2026-07-11: Worker target-cwd preflight errors must not collapse boundary/task-contract mistakes into "path does not exist" world-model evidence. `runtime.master-worker-loop` now classifies Worker workspace preflight failures as missing-parent, likely output-directory misuse under an existing parent, permission denied, generic canonicalization failure, or not-directory. Missing target cwd under an existing parent says it is not a repository-permission denial and likely means `target_cwd` was used for a not-yet-created output directory or the wrong workspace root. Master workspace boundary tool results now say "scope/permission boundary" and "not evidence that the external path is missing." Online proof required restarting the explicit service-scoped `com.freehand.workerS` because `restartS` only refreshed the master daemon; before restart, task `task-preflight-message-1783745178` showed old wording, after `launchctl kickstart -k gui/$(id -u)/com.freehand.workerS`, task `task-preflight-message-1783745289` showed the new classified wording and history `TaskCreated,TaskWaitingAgent,TaskAssigned,TaskResumed,TaskHeartbeat,TaskBlocked,TaskProgressed`. Marker: `target-cwd-preflight-classified-error-closeout-1783745289`.

- 2026-07-11: Master/Worker path delegation now preserves user-supplied repo paths and handles `~` plus symlinked parents on both sides of the contract. Master guidance now tells the model not to repeatedly probe outside `~/.freehand`, not to invent `/workspace`, `/tmp`, or sibling output dirs, and to dispatch external repo work to the paired Worker with original-path and canonical-path evidence requirements. Worker runner now expands leading `~`, canonicalizes through symlinks for the locked execution cwd, preserves the requested `target_cwd` in task truth, injects requested/canonical path preflight into the Worker prompt, and blocks missing `~/...` paths before model execution with both original and expanded path evidence. Local proof: `cargo test -p freehand-runtime production_worker_runner_ -- --nocapture` passed 11 tests; `cargo fmt --check`, `cargo clippy -p freehand-runtime --all-targets -- -D warnings`, `cargo run -p xtask -- mainlines generate`, `cargo run -p xtask -- mainlines check`, `cargo run -p xtask -- gates check`, and `git diff --check` passed. Online S-profile proof on `127.0.0.1:4042`: config query returned `agent=master`, paired `worker`, provider `minimax/MiniMax-M3`; task `task-verify-path-1783740443` used target cwd `/Users/fanzhang/github/xiaozhi-esp32-2.2.4`, Worker reported canonical `/Users/fanzhang/Documents/github/xiaozhi-esp32-2.2.4` with `symlink_detected=true`, ran 9 rounds and 22 tool executions, saw one provider retry and one failed tool result that re-entered the model, then submitted review; Master approved and closed. ADP task history showed 49 events ending `TaskReviewSubmitted,TaskReviewApproved,TaskClosed`. The long `running + heartbeat` observation was a real long Worker execution with periodic heartbeats until review submission, not a stalled lifecycle. Marker: `master-worker-path-symlink-contract-closeout-1783740443`.

- 2026-07-10: Production Master/Worker daemon loop is now real-provider online-verified through the normal daemon command ingress. The first live run exposed a real host bug: Slave mode called the synchronous Worker/provider loop directly from the Tokio async runtime, and the Worker panicked while dropping a blocking runtime (`Cannot drop a runtime in a context where blocking is not allowed`). `app.runtime-daemon` now owns an explicit `tokio::task::spawn_blocking` boundary through `run_blocking_worker_service`; positive coverage drops a nested runtime successfully and negative coverage proves blocking-task panic becomes an explicit join error. The Worker runner was split into `worker_runner.rs` (460 lines), `worker_runner/heartbeat.rs`, and `worker_runner/tests.rs`. Real S-profile proof on `127.0.0.1:4042`: Master session `production-worker-e2e-1783657707` created and assigned `task-1783657707` for external cwd `/tmp/freehand-worker-e2e-1783657707`; Worker `worker` claimed execution `exec-worker-worker-1783657761743691000-81`, emitted initial plus periodic heartbeats, completed turn `worker-turn-exec-worker-worker-1783657761743691000-81-r4`, wrote and re-read `/tmp/freehand-worker-e2e-1783657707/result.md`, then Master approved and closed. Ordered history is `TaskCreated,TaskAssigned,TaskResumed,TaskHeartbeat,TaskHeartbeat,TaskReviewSubmitted,TaskReviewApproved,TaskClosed`; same task/execution/agent truth remained queryable after explicit Worker restart. `make ci` passed after regenerating mainline wiki. Browser-visible WebUI screenshot proof remains unclosed because the available browser registry was empty; do not describe this headless HTTP command-ingress proof as WebUI page verification. Marker: `production-worker-online-closeout-1783657707`.

- 2026-07-09: Production master/worker loop gap was reconciled after the master-worker autonomy closeout. Gap 4 in `docs/architecture/architecture-gaps.md` now reflects current truth instead of stale Phase 2B/2C/2D missing scope: Phase 1/2A/2B/2C/2D foundation truth and WebUI projection are implemented and verified, and `master-worker-autonomy-sample` proves `SubmitUserInput`-only `task(op=...)` autonomy through deterministic provider fixture plus same-id restart verification. The remaining gap is production promotion: daemon-owned non-smoke master scheduler/worker runner activation, configured worker resource acquisition/release beyond CLI samples, real worker queue claiming without scripted CLI mutation, and non-fixture real-provider behavioral eval. Updated docs include `docs/goals/multi-task-foundation-phase2-gap-plan.md`, `docs/function-maps/task.orchestration.md`, `docs/testing/task.orchestration.md`, `docs/function-maps/app.cli-runtime-smoke.md`, `docs/testing/app.cli-runtime-smoke.md`, `docs/mainline-calls/*.json`, and generated wiki. Marker: `production-master-worker-loop-gap-reconcile-20260709`.

- 2026-07-09: Production autonomy acceptance now requires a formal real-provider online E2E task, not just a bounded toy prompt. The canonical acceptance prompt asks the master agent to research a current AI/semiconductor/international-technology-policy news item from the last 72 hours, use current sources, dispatch worker tasks via `task(op=...)` when appropriate, write a markdown briefing document, and report output path, task ids, worker ids, sources, and unverified claims. Pass criteria require real S-profile daemon/provider, normal user-input path, owner truth from TaskBoard/AgentBoard/EventInbox/TaskHistory, output artifact existence, same-id restart proof if worker tasks are created, and explicit queryable failure if search/write capability is missing. Marker: `formal-real-provider-e2e-standard-20260709`.

- 2026-07-09: Master-worker autonomy is now proven through headless ADP with a provider fixture, not just command-driven task mutation samples. `app.cli-runtime-smoke` owns `master-worker-autonomy-sample`: create mode submits only `UiCommand::SubmitUserInput`, then verifies transcript task-tool activity plus TaskBoard, AgentBoard, AgentLifecycle, and TaskHistory; verify mode re-queries the same task/execution/agent ids after restart. The CLI mock test rejects direct task mutation commands with `direct_task_mutation_forbidden`, so the sample cannot pass by scripting `CreateTask`/`AssignTask`/execution commands. S-profile online proof on `127.0.0.1:4042` used `scripts/verify-master-worker-autonomy-online.sh`, which temporarily redirected provider config to a local Anthropic-compatible fixture and restored config/env afterward. The fixture received exactly 27 `/v1/messages` attempts across all scenarios. Success task `task-cli-master-autonomy-success-FHAUTO1783599325364293000` closed with `tool_executions=8`; execution-error task `task-cli-master-autonomy-execution-error-FHAUTO1783599326069343000` stayed blocked with `tool_executions=6` and no review/close; reject-retry task `task-cli-master-autonomy-reject-retry-FHAUTO1783599326552699000` closed with `tool_executions=10`, two review submissions, and ordered `TaskReviewRejected` -> `TaskExecutionRecovering` -> second review -> approval -> close. After `restartS`, same-id verify passed for all three. Post-proof config restored to `base_url_host=api.minimaxi.com`, `default_model=MiniMax-M3`, `auth_source=inline`, and `daemonS.env` had no `FREEHAND_MASTER_AUTONOMY_FIXTURE_KEY`. Marker: `master-worker-autonomy-online-closeout-1783599325364293000`.

- 2026-07-09: Context distribution for live multi-round turns is now first-class and verified. `ContextSegmentKind` includes `TaskContract` and `TaskSpaceSnapshot`; `TaskContract` is `SessionStable + Cacheable + Developer`, while `TaskSpaceSnapshot` is `TurnVolatile + NoCache + Developer`. `provider.reason-live-bridge` now injects every live round with completion contract, control status contract, runtime tool guidance, and original operator task as `TaskContract` before volatile carryover. S-profile proof on `127.0.0.1:4042` used session `cli-adp-sample-failure-1783595301535093000`, terminal `runtime-turn-201-r3`, `rounds=3`, `tool_executions=2`, `failed_tools=1`; planned-context evidence showed stable/session-stable prefix unchanged across `runtime-turn-201`, `runtime-turn-201-r2`, and `runtime-turn-201-r3` (`stable_prefix_hash=b25c8265c341fff3`, stable count `4`, tool schema hash `fe8c952141685333`), while `previous-visible-output` remained volatile/no-cache tail only. Schema-mismatch online proof must not rely on prompt-only steering: the current model may either call unrelated tools or return valid schema immediately. Use a provider fixture or injected first invalid response for deterministic schema-polishing proof.

- 2026-07-09: Control/status and context admission contract update. `control.center` now uses `status.simple_question` as the only structured field for "previous user input was a simple question/answer request"; `simple_request` is intentionally not accepted as an alias and must not allow natural stop. Runtime model-visible dynamic inputs must not use arbitrary small fixed caps: `original-task`, `previous-visible-output`, schema feedback, and future task-space snapshots use content-derived admission budgets so only the planner/model context policy can reject true over-context payloads. Anthropic output budget remains provider-owned at `DEFAULT_ANTHROPIC_MAX_TOKENS=8192`, with no smaller live-bridge cap. Task-space state-machine validation must check only explicit standard fields such as `phase`, `current_step`, `completed`, `blocked`, `needs_user_input`, `target_alignment`, and `simple_question`; natural-language task target text is injected as canonical original text plus model understanding for model self-review, not machine semantic equivalence judgement. Verified locally with `cargo test -p freehand-control`, `cargo test -p freehand-runtime`, `cargo test -p freehand-ui-protocol`, `cargo test -p freehand-tools`, `cargo fmt --check`, `cargo run -p xtask -- mainlines generate/check`, `cargo run -p xtask -- gates check`, `git diff --check`, and workspace clippy. S-profile online proof on `127.0.0.1:4042`: `adp-smoke` passed, success sample closed `runtime-turn-193` with `rounds=1`, and failed-tool sample closed `runtime-turn-194-r2` with `rounds=2`, `tool_executions=1`, `failed_tools=1`, `schema_retries=0`.

- 2026-07-09: Multi-task Phase 2D WebUI projection is implemented and live-verified. `app.webui-smoke` owns the browser projection only: it queries `TaskBoard`, `AgentBoard`, `EventInbox`, `TaskHistory`, and `WorkerControl` through the ADP/runtime owner path, renders compact drawer cards, sends WorkerControl actions through protocol commands, then re-queries owner truth. It does not persist task/control truth in browser state. Command receipt user text is now safe on both WebUI and Android: `target_feature_id` and payload ids stay out of visible messages, known status codes map through an exact whitelist after stripping only parameter suffixes, and unknown receipts are shown as unsupported rather than a success fallback. Final S-profile browser proof on `127.0.0.1:4042`: `artifacts/webui-online/20260709-verify-4042-1783580948045/summary.json`, session `webui-session-20260709070942-4d8550f6`; checks `phase2TaskBoardMatchesService`, `phase2AgentBoardMatchesService`, `phase2EventInboxMatchesService`, `phase2TaskHistoryMatchesService`, `phase2WorkerControlMatchesService`, `phase2ProjectionVisible`, and `phase2NoRawInternalChrome` were all true. Same closeout revalidated Phase 2A/B/C owner samples after `restartS` with same task/execution/agent/cursor/control ids. Android true-device proof is not closed for this slice because ADB serial `100.104.163.65:5555` was unauthorized and `emulator-5554` was offline; Android JVM tests passed. Marker: `phase2d-webui-projection-closeout-1783580948045`.

- 2026-07-09: Multi-task Phase 2C `worker.control` no-UI foundation is implemented and live-verified. `task.orchestration` owns worker-control ledger/snapshot truth and task-state consequences; `ui.protocol` owns command/query DTO validation and UI-safe projection; `runtime.ui-command-dispatch` is a thin bridge; `app.cli-runtime-smoke` owns `worker-control-foundation-sample`. Implemented ops are `query_status`, `ask_at_safe_point`, `add_constraint`, `request_checkpoint`, `request_submission_now`, `pause`, `resume`, and `cancel`. `query_status` persists `observed`; safe-point ops persist `queued`; `pause`/`resume`/`cancel` route through Task Center first and persist `applied` only after the task consequence succeeds. S-profile proof on `127.0.0.1:4042`: sample created session `cli-worker-control-1783572587648756000`, task `task-cli-worker-control-FHPHASE2C1783572587648779000`, execution `exec-cli-worker-control-FHPHASE2C1783572587648779000`, agent `worker-cli-worker-control-FHPHASE2C1783572587648779000`, final `status=cancelled`, `control_events=8`, statuses `query_status:observed,ask_at_safe_point:queued,add_constraint:queued,request_checkpoint:queued,request_submission_now:queued,pause:applied,resume:applied,cancel:applied`; after `scripts/install-launchd.sh restartS`, verify mode against the same ids returned `worker_control_foundation_verify_ok` with the same task/control truth. Local proof included targeted worker-control tests in task/ui-protocol/runtime/cli, full package tests for those four packages, workspace clippy with `-D warnings`, `fmt`, mainline generate/check, gates check, and `git diff --check`. Marker: `phase2c-worker-control-closeout-1783572587648779000`.

- 2026-07-08: Multi-task Phase 2B EventInbox/MasterPoll no-UI foundation is implemented and live-verified. `task.orchestration` owns EventInbox projection, four-part event cursor truth (`timestamp:task_id:seq:event_id`), legacy three-part cursor compatibility, and MasterPoll persisted cursor advancement; `ui.protocol` owns `QueryEventInbox`/`RunMasterPoll` DTO validation including `replay_from_start`; `runtime.ui-command-dispatch` is a thin bridge; `app.cli-runtime-smoke` owns `master-poll-foundation-sample`. S-profile proof on `127.0.0.1:4042`: `freehand-cliS master-poll-foundation-sample` created task `task-cli-master-poll-FHPHASE2B1783528034427562000`, execution `exec-cli-master-poll-FHPHASE2B1783528034427562000`, worker `worker-cli-master-poll-FHPHASE2B1783528034427562000`, replay-drained `inbox_events=187`, then owner-backed reread showed `poll_events=0` and persisted cursor `00000000001783528036:task-cli-phase1-review-FHPHASE11783500244602888000:00000000000000000006:task-cli-phase1-review-FHPHASE11783500244602888000:6`. After `scripts/install-launchd.sh restartS`, verify mode against the same task/execution/worker/cursor returned `master_poll_foundation_verify_ok` with `inbox_after_cursor_events=0`, `poll_events=0`, same cursor, and task status `review_submitted`. Local proof included task/ui-protocol/runtime/cli package tests, workspace clippy with `-D warnings`, mainline generate/check, gates check, `fmt`, `diff --check`, and JSON parse checks. Marker: `phase2b-master-poll-closeout-1783528034427562000`.

- 2026-07-08: Multi-task Phase 2A closeout was revalidated and indexed into MemoryPalace. Current S-profile proof on `127.0.0.1:4042`: `freehand-cliS master-worker-foundation-sample` closed task `task-cli-master-worker-FHPHASE2A1783516065760449000`, execution `exec-cli-master-worker-FHPHASE2A1783516065760449000`, worker `worker-cli-master-worker-FHPHASE2A1783516065760449000`, with `blocked_seen=true`, `review_ready_seen=true`, and `lifecycle_state=closed`; after `scripts/install-launchd.sh restartS`, verify mode against the same ids returned `master_worker_foundation_verify_ok` and the same ordered durable events. Safe MemoryPalace corpus `/Volumes/extension/code/memory/freehand-phase2a-mempalace-corpus-safe-1783516065760449000` had zero sensitive-marker hits, mined 24 source-only files into `wing=freehand`, and marker search for `phase2a-master-worker-closeout-1783515402294813000` returned `phase2a-master-worker-closeout.md` rank 1.

- 2026-07-08: Multi-task Phase 2A no-UI master/worker task execution loop is implemented and live-verified. The owner path is `ui.protocol` command DTO/validation -> `runtime.ui-command-dispatch` thin dispatch -> `task.orchestration` mutation truth plus `agent.lifecycle` typed projection. `master-worker-foundation-sample` proves create worker, create waiting task, assign, claim-next with durable execution id, progress, blocked, recovering, review ready, reject, retry progress, second review, approve, and close without UI-local state or model prose. S-profile proof on `127.0.0.1:4042`: task `task-cli-master-worker-FHPHASE2A1783515402294813000`, execution `exec-cli-master-worker-FHPHASE2A1783515402294813000`, worker `worker-cli-master-worker-FHPHASE2A1783515402294813000`, final `status=closed`, `blocked_seen=true`, `review_ready_seen=true`, `lifecycle_state=closed`, ordered events `TaskCreated,TaskWaitingAgent,TaskAssigned,TaskResumed,TaskHeartbeat,TaskExecutionRecorded,TaskBlocked,TaskResumed,TaskHeartbeat,TaskExecutionRecovering,TaskReviewSubmitted,TaskReviewRejected,TaskResumed,TaskHeartbeat,TaskExecutionRecorded,TaskReviewSubmitted,TaskReviewApproved,TaskClosed`. After `scripts/install-launchd.sh restartS`, verify mode against the same task/execution/worker ids returned the same closed task history and closed lifecycle truth. Local proof: `cargo fmt --check`, task/runtime/ui-protocol/cli package tests, sequential `xtask mainlines generate/check`, `xtask gates check`, and `git diff --check`. Marker: `phase2a-master-worker-closeout-1783515402294813000`.

- 2026-07-08: Single-agent headless task lifecycle is now deterministic over ADP task mutation commands, not model-prompt dependent. `ui.protocol` owns `CreateTask`, `SubmitTaskReview`, `ApproveTaskReview`, and `CloseTask` command DTO/validation/routing; `runtime.ui-command-dispatch` routes them thinly to `task.orchestration` and publishes task list projection; `freehand-cli task-lifecycle-sample` sends those ADP commands then verifies task list/history truth. S-profile proof on `127.0.0.1:4042`: task `task-cli-FHTASK1783481386960124000` closed with history `TaskCreated,TaskAssigned,TaskReviewSubmitted,TaskReviewApproved,TaskClosed`. Same closeout also verified ADP smoke, success turn sample, failed-tool multi-round sample (`rounds=3`, `failed_tools=1`, `schema_retries=1`), same-session continuation (`restored_closed_turns=1`), and schema mismatch sample (`schema_retries=1`). Provider-retry remains not closed: model prose claiming retries is not evidence; closure requires provider-domain retry truth from fixture/error injection/runtime projection.

- 2026-07-08: Provider retry closeout must use provider-domain truth, not model prose. `scripts/verify-provider-retry-online.sh` now provides repeatable S-profile proof by temporarily redirecting provider config to a local Anthropic-compatible HTTP 500 fixture, running `freehand-cliS adp-turn-sample --sample provider-retry`, requiring exactly five `/v1/messages` fixture attempts, querying provider-domain error-center rows, and restoring `~/.freehand/config.toml` plus `~/.freehand/daemonS.env`. Verified session `cli-adp-sample-provider-retry-1783482215008491000`, turn `runtime-turn-178`, `mock_attempts=5`, error-center rows `retry_same_step` x4 plus `fail_turn` x1, concrete code `anthropic_http_status_500`, session truth `failed`. Post-run config query restored to `base_url_host=api.minimaxi.com`, `default_model=MiniMax-M3`, `auth_source=inline`.

- 2026-07-08: WebUI online verification now owns its Settings provider-save fixture instead of depending on stale local daemon env. `scripts/webui_verify_online.mjs` backs up S-profile `~/.freehand/config.toml` and `~/.freehand/daemonS.env`, injects verifier-only `FREEHAND_WEBUI_VERIFY_CREDENTIAL`, restarts only `com.freehand.daemonS`, runs browser/CDP proof, then restores config/env in `finally`. Verified artifact `artifacts/webui-online/20260708-verify-4042-1783483076297/summary.json`: session `webui-session-20260708035801-6cee1118`, ADP `turn_ids=runtime-turn-181,runtime-turn-182,runtime-turn-182-r2`, all checks true including Settings restart-required, no secret leak, submit clearing, refresh preservation, terminal no-live, mobile layout/drawers/gesture, and clean new session. Post-run config returned `base_url_host=api.minimaxi.com`, `default_model=MiniMax-M3`, `auth_source=inline`, and daemonS env had no verifier credential residue.

- 2026-07-08: Repaired-failure prompt context economy is now locked at the runtime restore boundary. `provider.reason-live-bridge` rebuilds same-session future prompt context from effective persisted turns, groups multi-round logical turns by runtime ordinal (`runtime-turn-N` / `runtime-turn-N-rM`), and admits only the latest repaired round into `SessionMemory`; superseded failed repair attempts remain durable in persisted turn/UI/debug/error truth but do not enter default future provider context. Regression `effective_context_uses_last_repaired_round_without_raw_failed_attempt` proves `runtime-turn-7` failed text is excluded while `runtime-turn-7-r2` repaired summary and unrelated `runtime-turn-8` remain. Verified with targeted test plus `cargo test -p freehand-runtime -- --nocapture` (78 passed) and `cargo fmt --check`.

- 2026-07-08: Single-agent restart recovery proof must re-query the same ids after daemon restart. Current S-profile proof on `127.0.0.1:4042`: `session-continue-sample` created `cli-session-continue-1783484106591493000` with `runtime-turn-183,runtime-turn-183-r2,runtime-turn-184`; after `scripts/install-launchd.sh restartS`, `adp-session-query --session cli-session-continue-1783484106591493000` returned the same three turn ids and `success`. `task-lifecycle-sample` closed `task-cli-FHTASK1783484127920815000`; after the same restart, `adp-task-query --history task-cli-FHTASK1783484127920815000` returned `TaskCreated,TaskAssigned,TaskReviewSubmitted,TaskReviewApproved,TaskClosed`.

- 2026-07-08: Single-agent closeout baseline is complete for the current goal scope. Direct `cargo test --workspace` repeatedly returned no tool output while no real cargo/rustc/test child process existed, so the verified equivalent baseline used `cargo metadata --no-deps --format-version 1 | jq -r '.packages[].name'` and ran `cargo test -p <pkg>` for all 22 workspace packages: contracts, blocks, config, provider-core, provider-openai, provider-anthropic, reason, debug, metadata, ui-protocol, control, node, instructions, task, tools, runtime, gates, testkit, cli, server, daemon, xtask. Result `PACKAGE_TESTS_EXIT=0`. Final gates also passed: `cargo build --workspace`, `cargo fmt --check`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo run -p xtask -- mainlines check`, `cargo run -p xtask -- gates check`, and `git diff --check`.

- 2026-07-08: Multi-task foundation design direction is now split into Task Center plus Agent Lifecycle before attaching capabilities to master/worker agents. Task Center owns global task truth: BigTask, SubTask, Execution, Review, EventInbox, SchedulerTick, agent task indexes, registration, synchronization, query, and restart recovery. Agent Lifecycle owns every agent's live runtime semantics: state, current/last activity, elapsed time, model/tool/error counters, error profile, runtime control channel, and AgentBoard. Framework owns timers, scheduler ticks, timeout/stale/blocker detection, event cursors, and projections; Master owns decisions from TaskBoard/AgentBoard; Worker owns execution progress/block/submission/retry updates. Phase 1 scope is one active BigTask with multiple SubTasks and explicitly excludes multiple independent BigTasks and cross-session context switching.

- 2026-07-08: Phase 1 multi-task tool/action surface must stay small and owner-scoped. Semantic action categories can be many in prompts/docs/tests, but exposed tools should be few logical surfaces with typed `op` parameters. Baseline remains `task(op=...)`; `agent` and `worker_control` are allowed only if owner-map/test-design analysis proves separate owner surfaces are needed. Tool name defines owner surface, `op` defines operation, typed args define payload. Unknown op, invalid args, forbidden transition, or permission mismatch must return a paired tool/action validation result to the model, not fallback, not guessed normalization, and not provider/system failure.

- 2026-07-08: `tool.registry` now has a red lock for the Phase 1 task-management tool/action contract. Task-management semantic categories such as `query_task_board`, `dispatch_subtask`, `approve_submission`, and `close_big_task` must not appear as standalone exposed tool names; the registry must expose `task` with required string `op` instead. This lock is scoped to task-management actions, not the general code tool surface (`read_file`, `write_file`, `grep`, `bash`, etc.), which remains aligned with current Codex/Reasonix-style tool categories and UI display semantics.

- 2026-07-08: Agent Lifecycle is an intrinsic agent property, not a default model-facing tool. Lifecycle truth should be reduced from runtime/provider/tool/error/task events into agent state and projected through AgentBoard, scheduler inputs, ADP/debug query surfaces, and master context summaries. `worker_control` is separate only for safe-point control of an already running worker execution: status query, ask-at-safe-point, add constraint, checkpoint/submission request, pause/resume/cancel. It must not own task create/assign/review/close, prompt-history mutation, raw transcript rewrite, or workspace/session truth mutation.

- 2026-07-08: Multi-task foundation Phase 1 execution target is `docs/goals/multi-task-foundation-phase1-loop.md`. First implementation loop must deliver owner/maps closeout, TaskBoard skeleton, AgentLifecycle skeleton, ExecutionFact sync, scheduler tick skeleton, and headless ADP/CLI proof with restart same-id query. Phase 1 explicitly excludes WebUI/Android task dashboard, worker autoscaling, cross-machine workers, multiple independent BigTasks, cross-session master context switching, standalone model-facing `agent` lifecycle tool, and general code-tool surface redesign.

- 2026-07-08: Multi-task Phase 1 D1 owner/map closeout decision: `task.orchestration` owns TaskBoard, ExecutionFact sync, and SchedulerTick surfaces; new `agent.lifecycle` owns AgentLifecycleSnapshot, AgentBoardProjection, and lifecycle reducer truth. `agent.lifecycle` is initially implemented under `crates/freehand-task` and may split only if implementation proves a separate crate is required. D1 docs are `docs/function-maps/agent.lifecycle.md`, `docs/testing/agent.lifecycle.md`, `docs/mainline-calls/agent.lifecycle.json`, generated `docs/wiki/agent.lifecycle.md`, plus expanded `task.orchestration` function map/test/mainline/wiki entries.

- 2026-07-08: Multi-task foundation Phase 1 headless foundation is implemented and verified on S-profile. `task.orchestration` now exposes TaskBoard query truth, ExecutionFact sync, and SchedulerTick durable facts; `agent.lifecycle` exposes AgentLifecycleSnapshot and AgentBoard projection; `ui.protocol` and `runtime.ui-command-dispatch` route Phase 1 query/command surfaces; `freehand-cliS phase1-foundation-sample` proves the end-to-end owner path. Online proof on `127.0.0.1:4042`: create sample returned `blocked_task=task-cli-phase1-blocked-FHPHASE11783500244602888000`, `review_task=task-cli-phase1-review-FHPHASE11783500244602888000`, `execution=exec-cli-phase1-FHPHASE11783500244602888000`, `agent=master`, `blocked=1`, `review_ready=1`, `stale=1`, `recovering_event=true`, `lifecycle_state=blocked`; after `scripts/install-launchd.sh restartS`, verify mode against the same ids returned `phase1_foundation_verify_ok` with the same counts/state. Local validation passed: `cargo test -p freehand-tools`, `cargo test -p freehand-cli`, `cargo test -p freehand-ui-protocol`, `cargo test -p freehand-task`, `cargo test -p freehand-runtime`, `cargo run -p xtask -- mainlines generate`, `cargo run -p xtask -- mainlines check`, `cargo run -p xtask -- gates check`, and `git diff --check`.

- 2026-07-08: Framework-mediated Agent/Task operation design is now explicit. `docs/design/framework-mediated-agent-operations.md` is the durable boundary: task operations go through Task Center / `task(op=...)`; agent registry is resource registration, not lifecycle; Agent Lifecycle is intrinsic state/projection from typed events, not a model-facing mutation tool; future `worker_control(op=...)` is only safe-point runtime control and must not mutate task truth directly; Agent-to-Agent communication should flow through Task Center/EventInbox and worker-control inbox queues. `docs/goals/multi-task-foundation-phase2-gap-plan.md` defines Phase 2 order: 2A worker execution loop, 2B master poll loop + EventInbox, 2C worker_control channel, 2D UI projection. Validation for this docs closeout: `git diff --check`, `cargo run -p xtask -- mainlines check`, and `cargo run -p xtask -- gates check`.

- 2026-07-07: WebUI mobile focused composer and Final summary closeout is verified on S profile `127.0.0.1:4042`. `app.webui-smoke` keeps focused phone/tall/tablet portrait composer compact and prevents the full attachment/CWD/model/status strip from occupying the primary conversation input surface; Final rows now render through dedicated structured `.final-summary` blocks instead of generic paragraph rendering. Online proof: `artifacts/webui-online/mobile-summary-1783427720726/summary.json`, screenshots `01-focused-composer.png` and `02-final-summary.png`; checks show `layoutShape=tall_phone`, focused composer height `110`, control strip/tray/status `display=none`, no `no draft attachments`/CWD/model visible, `finalSummaryCount=1`, `finalSummaryItemCount=3`, no browser errors. Local proof: `node --check`, `cargo test -p freehand-server -- --nocapture` 13 passed, `cargo fmt --check`, `git diff --check`, `xtask mainlines check`, `xtask gates check`; S-profile health/ADP smoke passed and served JS/CSS hashes matched workspace.

- 2026-07-07: Android old native notification banners are removed from the conversation surface. `StatusBannerController` is scoped to blocking native-shell connection/configuration errors only; APK update and file-picker statuses go to the drawer/status surface, and remote WebUI load hides native banner chrome. Final true-device release proof used `FREEHAND_ANDROID_APK=/Users/fanzhang/Documents/code/freehand/dist/android/freehand-android-release-unsigned.apk` against `100.104.163.65:5555`; evidence `artifacts/android-device/20260707T115207Z-100.104.163.65_5555-61741/summary.json` passed with Freehand foreground, `shape=tall_phone`, `conversationPrimary=true`, fixed/offscreen drawers, and screenshot showing no old native top notification. Android device verifier now waits for installed package availability and requires current resumed/focused Freehand activity; historical dumpsys mentions are not foreground truth.

- 2026-07-05: Same-session continuation history is now restored into provider context. `provider.reason-live-bridge` rebuilds `reason.session-history` base context from effective persisted turns (`ReasonPersistence::restore_turn_snapshots_for_ui`) before starting a restored same-session follow-up, so the next provider request includes prior user/assistant turn truth. Regression lock `live_bridge_restores_same_session_history_into_follow_up_provider_request` proves the second provider request contains `Historical turn 1`, prior user prompt, prior assistant answer, and current prompt. Runtime failure projection now replaces only the failed session transcript and preserves other restored sessions (`live_dispatch_failure_preserves_other_session_transcripts`). Online S-profile proof on `127.0.0.1:4042`: `artifacts/webui-online/20260705-history-4042-1783254901868/summary.json`, session `webui-session-20260705123503-9d9824e6`, turns `runtime-turn-64,runtime-turn-65`, second turn recovered first-turn-only token `FHCTX-1783254901867` and refresh preserved both turns. WebUI online gate proof: `artifacts/webui-online/20260705-verify-4042-1783254821320/summary.json`.

- 2026-07-05: Corrected WebUI online gate split: default development/alpha verification is S profile `127.0.0.1:4042` through `freehand-cliS` / `com.freehand.daemonS`; release/global verification is explicit `127.0.0.1:4041` through `freehand-cli` / `com.freehand.daemon`. `make verify-webui-online` now requires 4042 health, launches real Chrome/CDP through parameterized `scripts/webui_verify_online.mjs`, and writes `verify-4042` artifacts; `make verify-webui-release-online` is the only 4041 wrapper. `xtask gates check` now locks the default/release URL, ADP URL, CLI, and profile split so alpha cannot silently drift back to 4041. Online proof: `artifacts/webui-online/20260705-verify-4042-1783248727371/summary.json`, session `webui-session-20260705105207-a9295d35`, turns `runtime-turn-53,runtime-turn-54,runtime-turn-54-r2`, all WebUI checks true, ADP query exit 0. The earlier `20260705-verify-4041-*` alpha note was wrong-profile evidence and must not be used for development/alpha closeout.

- 2026-07-05: Anthropic live provider default output budget is now `DEFAULT_ANTHROPIC_MAX_TOKENS=8192`, not 512. `crates/freehand-runtime` consumes the provider-owned constant when constructing the live Anthropic executor, and `crates/freehand-provider-anthropic` request rendering tests assert `max_tokens=8192`. Verified with Anthropic adapter tests, targeted runtime max_tokens interruption regression, mainline generate/check, gates check, S-profile restart on `127.0.0.1:4042`, ADP smoke, real S-profile success sample `cli-adp-sample-success-1783219259178878000` / `runtime-turn-42`, and targeted clippy for provider/runtime.
- 2026-07-05: Freehand now has the first `instruction.capability-loader` slice in `crates/freehand-instructions`. It compiles an index-only deterministic manifest for global `~/.freehand/AGENTS.md`, global `~/.freehand/skills/**/SKILL.md`, local `AGENTS.md` from project root to cwd, and local `.agents/skills/**/SKILL.md` from project root to cwd. Manifest entries carry scope, precedence, normalized paths, content byte counts, content hashes, parsed skill name/description, explicit compile errors, and a manifest fingerprint. Runtime/context-planner consumption is intentionally pending; future injection must consume this compiled manifest and convert entries into typed context segments with token budgets, not scan authoring directories from runtime/UI/provider code. Verified with `cargo test -p freehand-instructions`, `cargo run -p xtask -- mainlines generate/check`, `cargo run -p xtask -- gates check`, `cargo fmt --check`, `cargo test -p xtask`, `cargo test --workspace` (433 passed), and `cargo clippy --workspace --all-targets -- -D warnings`.
- 2026-07-04: Freehand now has two coexisting macOS daemon install profiles. Release/global mode remains `freehand-*`, `com.freehand.daemon`, `~/.freehand/daemon.env`, and fixed `127.0.0.1:4041`. Development symlink mode uses `freehand-cliS` / `freehand-serverS` / `freehand-daemonS`, `com.freehand.daemonS`, `~/.freehand/daemonS.env`, and fixed `127.0.0.1:4042`; host commands symlink to `target/debug/*`, while `freehand-daemon-launchdS` is a prefix-local wrapper copy because launchd refused to execute a symlink wrapper with `Operation not permitted`. Verified with `scripts/install-launchd.sh installS`, `scripts/install-launchd.sh restartS`, 4042 health, `freehand-cliS adp-smoke --url ws://127.0.0.1:4042/adp`, 4041 health, `freehand-cli adp-smoke --url ws://127.0.0.1:4041/adp`, and `make ci` exit 0. Development online validation should prefer S mode; global install/restart is for release/promotion closeout.
- 2026-07-04: Follow-up S profile launchd truth: after repeated debug rebuilds, launchd execution through the S wrapper or `freehand-daemonS` symlink could leave a daemon process that printed `listening` but refused 4042 connections, with stderr containing `Operation not permitted` / `getcwd`. Current S profile keeps `freehand-cliS` and `freehand-serverS` symlinked, but launchd uses a local debug daemon copy `~/.local/bin/freehand-daemonS-bin` and starts it via `/bin/bash -lc 'cd <repo> && exec ...'`. Verified with `scripts/install-launchd.sh installS`, 4042 health `ok`, `freehand-cliS adp-smoke`, and served/workspace WebUI JS hash match.
- 2026-07-04: WebUI chat transcript rendering now uses chat bubbles: user rows are right-aligned, assistant rows are left-aligned, tool rows are embedded semantic blocks inside assistant bubbles, model/request rows use italic reasoning typography, and lifecycle colors are blue running / green success / red failed. Selected-session startup/manual-click priority is locked: latest active ADP/SSE updates from other sessions must not replace a pinned selected transcript. Verified on S mode `127.0.0.1:4042` with browser artifacts `artifacts/webui-online/20260704-chat-bubble-sse-4042-click-proof-1783141455456/summary.json` and `artifacts/webui-online/20260704-chat-bubble-sse-4042-running-proof-1783141498366/summary.json`, plus ADP/session truth and served/workspace JS hash match.
- 2026-07-05: WebUI session CRUD plus Codex-style double-Esc rollback is verified on S mode `127.0.0.1:4042`. The owner chain is `ui.protocol` -> `runtime.ui-command-dispatch` -> `reason.persistence` -> `app.webui-smoke`; WebUI has visible rename/archive/archived-list/restore controls, double Esc sends protocol rollback, rollback is append-only session truth, and effective transcript excludes the rolled-back turn while raw truth remains auditable. Real browser evidence is under `artifacts/webui-online/20260705-session-crud-rollback-4042-1783188706/`: session `webui-session-20260704182300-fe1fceed`, marker `fh-crud-rollback-1783189377071`, first terminal `runtime-turn-38`, second terminal `runtime-turn-39`, double-Esc rollback restored the second prompt into composer and hid it from transcript, edited replacement terminal `runtime-turn-40`, and ADP before/after `restartS` both reported `turns=2`, `turn_ids=runtime-turn-38,runtime-turn-40`, status `success`. Browser page errors and console errors were zero.
- 2026-07-01: WebUI render lifecycle must go through `RenderConversation` / `RenderTurn` / `RenderRow` before DOM rendering. Live animation is allowed only for the current live turn; model wait clocks are keyed by session/turn/phase/detail in `lifecycleClocks`, and completed/failed tool timings are frozen per turn/tool identity so historical tool rows do not keep counting. Verified on fixed `127.0.0.1:4041` with Playwright evidence under `artifacts/webui-render-architecture-closeout/20260701-live/`: current bottom turn `runtime-turn-65` had `live=true`, historical turns had `live=false`, and terminal screenshot had `liveCount=0`.
- 2026-07-01: WebUI composer clear is not sufficient submit evidence. After send, the submitted text must remain observable in the conversation stream either as pending submit or as a materialized visible user row; pending input may only be cleared after matching user text appears in visible turn rows. A live turn with no public rows must render an explicit observable waiting row rather than a blank transcript. Verified on fixed `127.0.0.1:4041` with screenshot `artifacts/webui-history-disappearance/20260701-live/browser-4041-immediate-after-send.png`: composer empty, `blockCount=10`, `emptyCount=0`, latest card `runtime-turn-66` contains `immediate observable history proof`.
- 2026-07-01: WebUI submit-history verification must cover consecutive submits after later ADP refresh, not only immediate pending render. Latest-turn merge may not key only on `turn_id`; after daemon restart/runtime ordinal reuse, `runtime-turn-65` can refer to a new request. WebUI replacement now requires same `turn_id` plus same visible `user_text`, otherwise the latest turn is appended. Verified on fixed `127.0.0.1:4041` with `artifacts/webui-history-disappearance/20260701-double-submit-fixed-v2/03-after-4s.png`: `blockCount=32`, `emptyCount=0`, first request at index 30, second request at index 31 after 4 seconds.
- 2026-07-01: A WebUI ADP failure saying `reason ledger sequence is invalid` is a runtime persistence truth failure, not a UI disappearance bug. Verified online on fixed `127.0.0.1:4041`: `~/.freehand/ledgers/reason/master/webui-session-20260701131739-e31eb6cf.jsonl` line 338 has `expected=338, got=337`, and `runtime-session-master.jsonl` line 380 has `expected=380, got=379`. Evidence artifact: `artifacts/webui-ledger-sequence-failure/20260701-live/reason-ledger-sequence-scan.json`. Do not silently edit/delete ledgers; fix through `reason.persistence` owner or an explicit authorized repair workflow.
- 2026-07-01: WebUI selected-session transcript order must preserve ADP/session transcript order and append-or-replace the latest same-session turn; do not sort visible cards by `runtime-turn-*` ordinal. Runtime turn ordinals can reset after daemon restart or restore, and numeric sorting can move newly submitted turns above older conversation rows.
- 2026-07-01: WebUI selected-session rendering must derive visible conversation turns through one selector that merges the selected session transcript with the latest same-session turn. Do not let `sessionTurns.length > 0` suppress `state.turn`; ADP latest-turn events can arrive before `QuerySessionTurns` refresh, and stale transcripts must not hide in-flight or newly completed continuation turns. Verified on fixed `127.0.0.1:4041` with same-session WebUI/CDP continuation screenshots under `artifacts/webui-selected-session-render/20260701-continue/`.
- 2026-06-29: Session cwd is now protocol/runtime truth. `SubmitUserInput.cwd` is validated by `ui.protocol`; runtime canonicalizes and binds cwd per session, persists it on `TurnRecord.cwd`, restores it after bootstrap, and inherits it for later same-session submits. WebUI only selects/displays cwd and forwards it through ADP; it is not the cwd truth owner.
- 2026-06-29: Registry tool execution must use explicit per-call workspace root for session cwd via `freehand_tools::with_workspace_root`; do not implement session cwd by mutating process-global cwd or `FREEHAND_WORKSPACE_ROOT`.
- 2026-06-29: Session CRUD is now protocol/runtime/persistence truth, not WebUI local state. `ui.protocol` owns `CreateSession`, `RenameSession`, `ArchiveSession`, `RestoreSession`, and `DeleteSession`; `runtime.ui-command-dispatch` routes them to `reason.persistence`; `reason.persistence` stores `PersistedSessionMetadataEntry` under `~/.freehand/state/ui/<agent>/session-metadata.json`. `DeleteSession` is intentionally non-destructive delete-as-archive until physical deletion of authoritative turn truth has an approved lifecycle. Verified by owner tests plus live ADP create/rename/archive/restore/delete/unknown-session failure on current workspace daemon port `4092`.
- 2026-06-30: WebUI session bulk controls now include `Select all` alongside Clear/Delete, and the session bulk toolbar small text is intentionally non-bold (`font-weight: 500`) to avoid the earlier heavy-looking UI. Fixed-port `127.0.0.1:4041` served HTML/JS/CSS must be verified after reinstall before claiming the control exists.
- 2026-06-30: WebUI must not infer draft sessions from the `webui-session-*` id prefix. Persisted real sessions also use that prefix, so draft checks must use only the current in-memory `state.draftSessionId`; otherwise session bulk `Select all` skips visible real sessions.
- 2026-06-30: WebUI New conversation must render a clean draft session state: when `state.draftSessionId` exists and there are no persisted sessions, the sidebar must show the draft only, not a separate `no sessions` item, and the transcript must render a chat empty state instead of stale latest-turn content or a system-looking card.
- 2026-06-30: WebUI tool/result merge is semantic, not visual-only. `ui.protocol` public tool body should prefer semantic target/diff display for waiting/completed/failed tools, while success/failure outcome belongs in status/state; WebUI must not append `display.result_summary` as a separate success-result content line.
- 2026-06-30: WebUI Final cards default to summary-only display. Evidence, learned notes, and completion reason are debug details and must stay hidden unless the WebUI debug-details toggle is enabled; visible scenario buttons should read `Success`/`Failure`, not `Success sample`/`Failure sample`.

- 2026-06-29: WebUI layered control first slice is implemented without changing ADP framing. `app.webui-smoke` now renders composer controls for file/image/video attachments, attachment preview, selected-session refresh, and read-only runtime model selection. Attachment drafts are session-scoped UI metadata under `freehand-webui-attachment-drafts-v1`; current-page `File` handles stay available for retry, restored drafts are marked metadata-only, and submitted command text receives placeholder lines only. Successful ADP command receipt clears the current session draft; ADP dispatch failure restores the composer text and preserves draft attachments. WebUI ADP requests now have an explicit timeout to avoid silent dispatch hangs. Verified with JS syntax check, `cargo test -p freehand-server`, `cargo test -p freehand-ui-protocol`, `xtask mainlines generate/check`, `xtask gates check`, `make ci` exit 0, headless ADP smoke on `ws://127.0.0.1:4079/adp`, and Playwright screenshots under `artifacts/webui-layered-controls-e2e/20260629-layered-attachments-success-v2/` and `artifacts/webui-layered-controls-e2e/20260629-layered-attachments-failure-v2/`. Remaining scope is backend binary upload/session storage if future requirements require payload persistence across reload; current slice intentionally keeps transcript placeholder-only and does not alter ADP framing.
- 2026-06-29: Tool display classification is now a locked pure owner, not WebUI logic. `tool.display` lives in `crates/freehand-blocks/src/tool_display.rs` and exposes independent parser functions for read/list, file mutation, search, plan, shell, and generic tools. `ui.protocol` attaches `UiToolActivity.display` and updates it with tool result outcome; WebUI consumes `display` fields for action/target/parameter_summary/result/diff rendering and no longer classifies tool cards from raw tool strings. Tool parameters render as lower-weight secondary lines, e.g. read_file exposes `path=...`. Verified with `cargo fmt --check`, `node --check apps/freehand-server/assets/webui.js`, `cargo test -p freehand-blocks`, `cargo test -p freehand-ui-protocol -- --nocapture`, `cargo test -p freehand-server -- --nocapture`, `cargo test -p freehand-runtime -- --nocapture`, `cargo test -p freehand-cli -- --nocapture`, `cargo run -p xtask -- mainlines generate`, `cargo run -p xtask -- mainlines check`, `cargo run -p xtask -- gates check`, global install + launchd fixed-port restart, ADP smoke, and real WebUI screenshots under `artifacts/webui-tool-display-e2e/20260629-parameters/` showing `Read file` with `path=definitely-missing-freehand-file.txt`, final completed state, and no waiting text after reload.
- 2026-06-29: Provider request wait state is now protocol-owned. `UiTurnProjection.model_request` marks request-sent/model-response-waiting state, runtime maps `RuntimeLive02ProviderRequestBuilt` into `UiProtocolState::apply_model_request_waiting`, and WebUI renders an animated elapsed "waiting for model response" card from ADP turn projection. The state clears on semantic response, tool call, tool result, usage, terminal, or error projection, so errors and responses update the same lifecycle card instead of leaving a static wait. Verified with `cargo fmt --check`, `node --check apps/freehand-server/assets/webui.js`, `cargo test -p freehand-ui-protocol -- --nocapture`, `cargo test -p freehand-server -- --nocapture`, `cargo test -p freehand-runtime -- --nocapture`, `cargo run -p xtask -- mainlines generate`, `cargo run -p xtask -- mainlines check`, and `cargo run -p xtask -- gates check`.
- 2026-06-29: Tool result display is now protocol-owned instead of WebUI-local. `UiToolActivity.detail` carries completed/failed `ToolResultContract.output`, public tool summaries expose that detail, and WebUI renders it in the same tool card with status and elapsed execution time. WebUI also renders an animated elapsed "waiting for model" card after a tool completes/fails and before terminal/model continuation arrives. Verified with `node --check apps/freehand-server/assets/webui.js`, `cargo test -p freehand-ui-protocol -- --nocapture`, `cargo test -p freehand-server`, `cargo test -p freehand-runtime -- --nocapture`, `cargo run -p xtask -- mainlines generate`, `cargo run -p xtask -- mainlines check`, and `cargo run -p xtask -- gates check`.
- 2026-06-29: WebUI lifecycle progress display now covers submit/dispatch waiting and tool waiting. `submitStartedAt` drives an animated pending-submit card with elapsed dispatch wait time; tool waiting continues to use animated tool cards and now also exposes elapsed time in the main lifecycle status. The one-second refresh loop covers both pending submit and waiting tools, preventing static "waiting" stalls during normal turn lifecycle. Verified with `node --check apps/freehand-server/assets/webui.js`, `cargo test -p freehand-server`, `cargo run -p xtask -- mainlines generate`, `cargo run -p xtask -- mainlines check`, and `cargo run -p xtask -- gates check`.
- 2026-06-29: WebUI logical-turn collapsing now covers assistant text as well as tool cards. The browser transcript must render one visible assistant card per logical execution cycle, not one card per text chunk, while still preserving tool summaries, terminal cards, and raw completion-block stripping. Verified with `node --check apps/freehand-server/assets/webui.js`, `cargo test -p freehand-server`, `cargo run -p xtask -- mainlines check`, and `cargo run -p xtask -- gates check`.
- 2026-06-29: WebUI restart recovery now rebuilds display truth from reason-ledger per-turn snapshots, not only closed-turn files. This keeps earlier-round tool activity visible after daemon restart while still grouping same-execution-cycle `runtime-turn-N` / `runtime-turn-N-rM` rounds into one logical conversation item for display only. Assistant cards also strip raw `<freehand_completion>` schema blocks so completion schema text does not leak into the public stream. Verified with fixed-port daemon `127.0.0.1:4041`, real browser evidence under `artifacts/webui-visual-session/20260629-multiround-slowtool-success/` and `artifacts/webui-visual-session/20260629-final-restore-merged-clean/`, plus `node --check apps/freehand-server/assets/webui.js` and `cargo test -p freehand-runtime live_bootstrap_restores_multiround_tool_activity_into_ui_state`.
- 2026-06-28: WebUI shortcuts and slash commands are live input-layer affordances over the ADP path, not fake turns. Supported shortcuts are Cmd/Ctrl+Enter send, Esc cancel, Cmd/Ctrl+R refresh, Cmd/Ctrl+K focus, Cmd/Ctrl+1 success sample, and Cmd/Ctrl+2 failure sample. Supported slash commands are `/help`, `/sessions`, `/reload`, `/success`, `/failure`, `/cancel`, and `/clear`; slash commands clear the composer and use sticky command status so completed-turn live status cannot hide the local command feedback. Verified on fixed-port daemon `127.0.0.1:4041` with real Chrome screenshots under `artifacts/webui-session-alignment/20260628-233033/`, plus `node --check`, `cargo test -p freehand-server`, `cargo test -p freehand-ui-protocol`, full `scripts/install-global.sh`, launchd restart, ADP smoke, and sequential success/failure samples.
- 2026-06-28: WebUI session rendering now restores selected session from localStorage and renders persisted session transcript from ADP `QuerySessionList` + `QuerySessionTurns`; reload preserves `runtime-session-master` and historical turns without using UI-local truth as recovery. Live WebUI failure sample generated `runtime-turn-36-r2`: `read_file` failed as a tool result, the model continued to a successful terminal schema, and the page rendered the same tool failed card plus `FINAL COMPLETED`. Do not validate success/failure samples concurrently; the runtime dispatcher is single-active-turn and parallel sample submission can surface `command_dispatch_port_failure`, which is not valid positive-path evidence.
- 2026-06-28: Current Freehand live config is verified against Minimonth: RCC source `/Volumes/extension/.rcc/provider/minimonth/config.v2.toml` and runtime `~/.freehand/config.toml` both resolve provider `minimonth`, Anthropic Messages protocol, base URL `https://api.53hk.cn`, and model `MiniMax-M2.7`; `freehand-cli --agent master` prints the same selected provider truth. Completion evidence for the WebUI session goal includes `make ci` exit 0, fixed-port ADP smoke, sequential success sample `runtime-turn-38`, sequential failure sample `runtime-turn-39-r2`, and real Chrome screenshots under `artifacts/webui-session-alignment/20260628-continued/` proving daemon restart recovery plus tool waiting/completed states.
- 2026-06-28: Daemon now exposes protocol-owned ADP WebSocket at `/adp` on the fixed service port. `UiAdpRequest`/`UiAdpResponse` are the shared command/query/subscribe frame truth for WebUI, Android, CLI, and headless automation; subscribe returns explicit `SubscriptionAccepted` before events, query-as-command is rejected as `ingress_command_kind_mismatch`, and command dispatch does not block the connection's subscription event loop. Verified by `cargo test -p freehand-ui-protocol`, `cargo test -p freehand-server`, `cargo test -p freehand-daemon`, `cargo run -p xtask -- mainlines check`, `cargo run -p xtask -- gates check`, `make ci`, `scripts/install-global.sh`, `scripts/install-launchd.sh restart`, `curl -4 http://127.0.0.1:4041/health`, and a real Node WebSocket smoke against `ws://127.0.0.1:4041/adp`.
- 2026-06-28: WebUI tool lifecycle rendering is keyed by `tool_call_id`: `ui.protocol` upserts duplicate same-id tool calls into one activity, public tool summaries carry `tool_call_id`, and WebUI normalizes same-tool cards before rendering. Waiting tool cards animate, composer input clears immediately on submit, and command status is routed through one renderer. Verified by `node --check apps/freehand-server/assets/webui.js`, `cargo test -p freehand-ui-protocol`, `cargo test -p freehand-server`, `cargo run -p xtask -- mainlines check`, `cargo run -p xtask -- gates check`, `make ci`, and local WebUI smoke curl.
- 2026-06-28: runtime final multi-round UI projection only aggregates cross-round tool lifecycle (`tool_calls` / `tool_results`); final visible text, usage, errors, and terminal state come from the final round. Verified by `cargo test -p freehand-runtime` and `make ci`.
- 2026-06-28: WebUI debug SSE now distinguishes missing snapshot (`debug pending`) from transport failure (`debug stream reconnecting`). Verified by `cargo test -p freehand-server` and `make ci`.
- 2026-06-28: launchd wrapper now requires explicit `FREEHAND_DAEMON_BIN` in `~/.freehand/daemon.env` and refuses prefix mismatch instead of silently running an old prefix binary. Verified by `cargo test -p xtask` and `make ci`.
- 2026-06-27: Freehand release truth is `scripts/release.sh`: it runs `make ci`, Android JVM tests, Rust release binaries for `freehand-cli`/`freehand-server`/`freehand-daemon`, Android `assembleRelease`, and stages artifacts under `dist/`. Verified with exit 0 and staged host binaries plus `freehand-android-release-unsigned.apk`.
- 2026-06-27: Freehand global install truth is `scripts/install-global.sh`: it runs the release script and installs host binaries to `${FREEHAND_PREFIX:-$HOME/.local}/bin`. Verified with a temp `FREEHAND_PREFIX`; installed `freehand-cli`, `freehand-server`, and `freehand-daemon` were executable.
- 2026-06-27: Freehand macOS background daemon truth is `scripts/install-launchd.sh`: it installs binaries to `~/.local/bin`, writes `~/Library/LaunchAgents/com.freehand.daemon.plist`, loads `~/.freehand/daemon.env`, starts `freehand-daemon serve --agent master --bind 127.0.0.1:4041`, and uses launchd `RunAtLoad` + `KeepAlive`. Verified real install: `/health` 200 `ok`, `/` 200 WebUI HTML, exact-PID kill restarted from pid 55614 to pid 65923, logs under `~/.freehand/logs/daemon.*.log`.
- 2026-06-27: Reinstalling the launchd service via `scripts/install-launchd.sh install` re-copies the installed daemon/wrapper binaries, so macOS may re-evaluate them on each reinstall. Use `scripts/install-launchd.sh restart` for ordinary daemon restarts; it only runs `launchctl kickstart -k` and does not rewrite install state. The fixed WebUI bind remains `127.0.0.1:4041`.
- 2026-06-27: Installed daemon startup is config-selected from `~/.freehand/config.toml`. Verified with temp HOME: `freehand-daemon serve --agent master --bind 127.0.0.1:4059` returned `/health` 200 `ok` and `/` 200 WebUI HTML. First local topology requires paired agents to resolve the same pair token value.
- 2026-06-27: Android release APK packaging disables release lint checks via `apps/freehand-android/app/build.gradle.kts` because the local AGP/Gradle/SDK path repeatedly hung or failed in Android Lint Vital; release regression truth remains `make ci` plus Android JVM tests.
- 2026-06-15: Freehand architecture baseline uses `contracts + blocks + orchestrators` layering, with UI and provider semantics explicitly separated.
- 2026-06-15: Provider strategy is Rust-native first: OpenAI-compatible adapter may use `async-openai` or raw HTTP; Anthropic adapter defaults to raw HTTP/SSE until a stable Rust SDK is justified.
- 2026-06-15: Orchestrator crates are pure orchestration only. New helper or semantic functions must be searched for in existing libraries first and, when needed, added to `freehand-blocks` rather than `freehand-reason` or `freehand-node`.
- 2026-06-15: Freehand runtime home is `~/.freehand`; runtime logs, ledgers, replays, cache, and local state should converge there rather than leaking across ad hoc paths.
- 2026-06-15: Development and debugging must start from the function map owner and debug artifacts. If feature truth changes, update function map, architecture docs, project skill workflow, and durable memory in the same change set.
- 2026-06-15: Master/slave meaning is an input-permission configuration: whichever side is configured as master receives user input and dispatches tasks; slave in paired healthy mode only executes paired input and rejects unrelated direct input. Paired input source may be a user or another master.
- 2026-06-15: Each agent has a startup configuration file. Slave mode is chosen by startup config and currently confirmed to require at least listening interface, allowed pairing source IP, and pairing token.
- 2026-06-15: Feature work and bug fixes must both pass three self-checks before closure: information sufficiency, logic closure, and lifecycle completeness. If not satisfied, continue read-only tracing first and ask the user only when local truth cannot close the gap.
- 2026-06-15: Local multiple agents are managed by `config.toml`; one `config.toml` may define multiple local agents.
- 2026-06-15: `contracts.core` owns only cross-module shared semantic types plus shared IDs and error contracts. It excludes config schema, UI projection, and debug/trace envelope. Its types default to serializable, replayable, and persistable.
- 2026-06-15: `provider.semantic` v1 supports OpenAI-compatible and Anthropic. It requires both single-shot and stream interfaces, unified semantic outputs, debug-only raw event retention, explicit capability declaration, and recovery classification with periodic windows expressed in seconds and defaulting to half-hour, five-hour, or daily-midnight windows when provider hints are absent.
- 2026-06-15: `reason.turn` writes truth per turn, projects conversation view from turns, and is the only writer of session truth. Freehand does not stop merely because provider finish_reason is stop/end_turn; stopping requires valid completion schema evaluation.
- 2026-06-15: `node.master-slave` v1 is local one-master one-slave only, pairs via WebSocket handshake, uses fixed pairing source per slave config, and keeps slave listening after pairing loss for re-pairing. Master must be able to delegate work, query progress, directly talk, and subscribe to slave turn output.
- 2026-06-15: `ui.protocol` v1 supports CLI and WebUI, exposes full command/projection/query/subscribe truth, keeps query and subscribe separate, carries explicit source identity fields, shows only terminal projected text, and renders slave turn as WebUI-only separate sub-stream card.
- 2026-06-15: Config core v1 is fixed so far: only `~/.freehand/config.toml`; multi-agent layout uses `[agents.<name>]`; one process starts one agent by CLI-selected name; config changes are restart-only; `allowed_pair_ip` is optional and `pair_token` is an environment-variable reference.
- 2026-06-15: Freehand test policy is three-layered by default: module white-box, module black-box, and project black-box. Each feature map entry must declare all three required test groups, and compile/regression flow must run them under the workspace test umbrella plus architecture gates.
- 2026-06-15: Freehand splits feature ownership from code-call truth. `docs/architecture/feature-map.md` owns feature owner and validation routing; `docs/function-maps/` owns request/response/error mainlines, shared multi-reference function descriptions, and code-bound call tables. No feature implementation is closed unless the function-map binding stays in sync with code.
- 2026-06-15: `config.core` baseline is now implemented. `freehand-config` loads only `~/.freehand/config.toml`, validates `[agents.<name>]`, requires `name`, `mode`, and `pair_token`, enforces `name == table key`, and resolves `pair_token` as an environment-variable reference during selected-agent startup. `freehand-cli --agent <name>` is the current project black-box startup harness for this module.
- 2026-06-15: `contracts.core` baseline is now implemented. `freehand-contracts` provides shared ID newtypes, first-pass request nodes, semantic response event types, tool call/result contracts, token usage, terminal status, classified error contracts, and a shared request validator. These contracts are JSON-serializable/replayable/persistable by default and now serve as the first real cross-module semantic truth.
- 2026-06-15: `provider.semantic` baseline now explicitly supports OpenAI `responses` plus Anthropic Messages-family semantics at the shared semantic layer. `freehand-provider-core` now owns provider family/protocol descriptors, raw-retention policy, unified semantic request building, normalized adapter-event mapping, and provider error classification. Provider protocol reference snapshots are stored under `docs/references/provider-protocols/`, with a local `provider-protocols` skill for implementation-time lookup.
- 2026-06-15: shared tool-call and usage contracts were tightened for provider work. `ToolArgument` now preserves structured JSON values instead of string-only payloads, and `TokenUsage` now carries optional `total_tokens`, `reasoning_tokens`, and `finish_reason` while keeping explicit cache counters for replay-safe accounting.
- 2026-06-15: `contracts.core` now carries typed context-planning request truth. `ReasonReq02ContextComposedInput` uses `context_segments` instead of ad hoc context pairs, `ReasonReq03ProviderPayload` uses `input_segments` instead of `rendered_input`, and shared contracts now define `ContextSegment`, kind/stability/cache-policy/role enums, provenance, and request validators for both composed input and provider payload boundaries.
- 2026-06-15: provider adapter baseline is now implemented. `freehand-provider-openai` supports OpenAI `responses` and `chat completions`; `freehand-provider-anthropic` supports Anthropic Messages. Both adapters now render semantic requests into protocol-specific request bodies, parse single-shot and stream payloads into shared semantic outputs, accumulate partial tool-call/tool-use JSON until complete, and keep wire DTOs private to adapter crates. Live HTTP execution remains intentionally out of scope for this baseline.
- 2026-06-15: provider adapters now consume typed input segments and render them with shared segment rendering logic from `freehand-blocks`; provider adapters no longer depend on a single mixed rendered prompt string contract.
- 2026-06-15: `reason.turn` has not yet fully aligned with the referenced `../Deepseek-reasonix` context orchestration. Before changing the real context planner, inspect reasonix and document cache-relevant ordering. Target direction is stable/volatile typed context segments, stable segment reuse across turns, and debug-visible cache boundaries.
- 2026-06-15: Reason and provider modules must remain implementation-independent. `freehand-reason` owns context orchestration, turn truth, completion control, and broadcast; provider adapters own protocol rendering/parsing only. `freehand-reason` must not depend on provider adapter crates, and provider adapters must not depend on `freehand-reason`.
- 2026-06-15: Cross-module metadata and request data require hard isolation. Metadata/debug/provider/cache fields must use separate types and builders from request-chain content fields. Metadata must not be smuggled into prompt/request text, and request content must not be recovered from metadata/debug fields.
- 2026-06-15: `reason.turn` baseline is now implemented. `freehand-reason` owns per-turn truth, provider-output materialization, tool-result re-entry ownership, session projection from turns, and non-blocking semantic broadcast. Completion-schema validation lives in `freehand-blocks`; provider finish reasons do not stop turns by themselves. Valid complete and blocked submissions terminalize the turn, invalid submissions are rejected, and `next_step` submissions remain non-terminal.
- 2026-06-15: `node.master-slave` baseline is now implemented as a local runtime semantic layer in `freehand-node`. `LocalNodeRuntime` validates master/slave bootstrap fields, models websocket pairing intent, materializes paired/rejected/listening node status, restricts slave input to the active paired source, preserves relisten behavior after pairing loss, stores delegated-task progress through `freehand-ui-protocol`, and publishes slave turn projections for subscribed master/UI consumers. Real websocket IO wiring and final wire schema remain intentionally out of scope for this baseline.
- 2026-06-15: `reason.context-planner` design is now locked. Freehand will combine Reasonix cache-first session discipline with Codex typed-fragment discipline: stable prefix remains stable across ordinary turns, context rewrites happen only through explicit rewrite gates, metadata/request payloads stay hard-isolated, and parent context is enriched from subagent work only through a typed final-conclusion segment rather than raw child transcript injection.
- 2026-06-15: `reason.context-planner` baseline is now implemented in `freehand-blocks`. `plan_context` owns typed segment admission, stable-before-volatile ordering, user-turn segment ownership, token-budget rejection, raw-subagent-transcript rejection by provenance, and cache diagnostics generation. `freehand-reason` now delegates turn-start request planning to this planner and keeps planner diagnostics outside request content. Rewrite mode/version sourcing and explicit compaction/rollback/resume gates are now closed by `reason.session-history`; remaining planner gap is runtime tool-schema fingerprint wiring.
- 2026-06-15: `reason.session-history` baseline is now implemented in `freehand-reason`. `SessionHistory` owns session base context, session-owned `rewrite_version`, one-shot `current_rewrite_mode`, rewrite ledger evidence, and persisted JSON/file snapshots. `ReasonTurnEngine::start_turn` now reads rewrite mode/version from `SessionHistory` and clears non-ordinary rewrite mode only after successful turn startup. Explicit `stage_compaction`, `stage_rollback`, and `stage_resume_rebuild` methods are the only baseline rewrite-version bump paths.
- 2026-06-15: `reason.persistence` design is now locked. Freehand reason persistence is split into three layers: authoritative snapshots under `~/.freehand/state/turns`, append-only reason/provider ledgers under `~/.freehand/ledgers`, and derived UI/index sidecars under `~/.freehand/state/ui` plus `~/.freehand/cache/session-index`. Only `freehand-reason` may write authoritative session/turn truth; provider raw ledgers and UI sidecars are never recovery truth.
- 2026-06-15: `reason.persistence` baseline is now implemented in `freehand-reason`. `ReasonPersistence` owns reason-ledger append, active-turn snapshots, terminal turn materialization, derived session sidecars/index rebuild, and restore from snapshot-plus-ledger-tail or ledger-only rebuild. CLI and shared harness smoke now prove persistence restore without treating provider raw ledgers or UI sidecars as recovery truth.
- 2026-06-15: `reason.rewrite-policy` baseline is now implemented in `freehand-blocks`. Compaction trigger policy follows Reasonix evidence for soft notice (`50%`), auto compaction (`80%`), force compaction (`90%`), tail cap (`min(window * 0.5, 16384)`), and auto-pause after two ineffective compactions. Freehand-specific rollback and resume-rebuild trigger policy is now explicit because the reference project does not expose equivalent persisted session-history gates. `ReasonRewriteRuntime` in `freehand-reason` is the single baseline consumer that applies policy decisions before calling `SessionHistory::stage_*`. Provider usage enters rewrite policy only through `prompt_tokens_from_usage`, which maps `TokenUsage.input_tokens` to prompt pressure and rejects invalid usage. `freehand-testkit::ReasonRuntimeHarness` now provides project black-box coverage from provider semantic output to turn truth to usage-driven rewrite policy. Production CLI/server runtime loop wiring with real provider usage events and persisted recovery payloads remains pending.
- 2026-06-15: `app.cli-runtime-smoke` baseline is now implemented in `apps/freehand-cli`. The binary now supports `reason-e2e --agent <name> --scenario <usage-compaction|recovery-block>`, which loads real config from `~/.freehand/config.toml`, selects one agent, and routes through the shared `freehand-testkit::ReasonRuntimeHarness` instead of duplicating runtime semantics in the app layer. This is the current app-boundary end-to-end smoke path and is covered by `cargo test -p freehand-cli`.
- 2026-06-15: `config.core` now owns the provider registry as well as the agent registry. `~/.freehand/config.toml` contains `[agents.<name>]` plus `[providers.<id>]`; each agent binds exactly one provider id, and `LoadedConfig::select_agent` is now the single runtime-selection path for pair token, provider protocol, and provider auth resolution. Provider protocol must be explicit in config and must not be guessed from provider type or model name. CLI/config projections must never print resolved provider secrets.
- 2026-06-15: `config.core` provider config must only contain fields with current owner and runtime meaning. Unused knobs such as `transport_backend` must not enter config truth, and provider entries reject unknown fields so unimplemented settings fail explicitly instead of being silently accepted.
- 2026-06-15: `provider.anthropic-adapter` now has live `minimonth` replay fixtures for Anthropic-compatible Messages API single-shot and SSE stream responses. Fixtures live under `crates/freehand-provider-anthropic/fixtures/` and lock thinking/text/usage/cache/terminal mapping through `AnthropicAdapter::parse_response` and `AnthropicAdapter::parse_stream_event`.
- 2026-06-15: `provider.anthropic-adapter` now has an HTTP executor baseline in `AnthropicExecutor`. It owns Anthropic-compatible HTTP POST wiring, required auth/version/content headers, non-success HTTP status errors, single-shot parsing through `AnthropicAdapter::parse_response`, and collected-SSE parsing through `AnthropicAdapter::parse_stream_event`. Default regression uses local mock servers, not live credentials.
- 2026-06-15: `provider.reason-live-bridge` is now implemented in `freehand-testkit` as the first config-selected live path into `reason.turn`. It bridges `SelectedAgentConfig` into provider descriptor + Anthropic executor config, starts one turn, builds provider semantic request from turn-owned payload, executes single-shot or collected-SSE Anthropic requests, and applies provider-neutral outputs back into turn truth while capturing broadcasts. `freehand-cli reason-live` reuses this path; `freehand-reason` still does not depend on provider adapter crates.
- 2026-06-15: Anthropic live stream execution is no longer collected-SSE only. `AnthropicExecutor::execute_stream_with` now reads SSE event boundaries incrementally from the HTTP response, parses each semantic batch immediately, and `provider.reason-live-bridge` applies those batches into `ReasonTurnEngine` before stream completion. Regression tests explicitly lock both halves: executor callback-before-release and bridge broadcast-before-release.
- 2026-06-15: live reason completion is now governed by a tagged JSON loop. Freehand requires `<freehand_completion>...</freehand_completion>` in model-visible text, parses it through `freehand-blocks`, strips the tagged block from visible text projection, rejects invalid schema with field-level feedback, treats `claim=continue` as the next execution signal, and fails the live request after 3 invalid schema retries. `freehand-reason` owns the failed terminal write; `freehand-testkit` owns the multi-round composition loop.
- 2026-06-15: Freehand now has a minimal real provider E2E on Anthropic-compatible Messages. `freehand-provider-core` carries provider-neutral tool schema/choice/exchange metadata, `freehand-provider-anthropic` renders them into Anthropic `tools` + assistant `tool_use` + user `tool_result`, and `freehand-testkit::run_live_reason_turn` restores/persists live turns through `ReasonPersistence`, executes deterministic `echo_json`, re-enters tool results, and closes only through valid completion schema. `freehand-cli reason-live` now supports optional `--session` and reports restore status plus tool execution count.
- 2026-06-16: UI next step is protocol-first. `ui.protocol` is the shared truth for CLI and WebUI, and `freehand-server webui-smoke` is the minimal app boundary proving a WebUI can consume `freehand-ui-protocol` without owning reason/provider logic. Client-specific slave-card visibility is centralized in `turn_projection_for_client`.
- 2026-06-16: UI architecture truth is broader than WebUI. Any UI is an input ingress plus a read-only consumer of reason-turn and debug-state projections. UI may submit commands, but only owner modules may mutate reason/debug/session truth.
- 2026-06-16: `ui.protocol` now has a minimal landed debug contract: UIs can query and subscribe read-only debug snapshots by `turn_id`, with source identity, one summary `status_text`, and ordered `detail_lines`. Raw debug ledgers and provider payloads remain outside UI protocol truth.
- 2026-06-16: `debug.core` baseline is now implemented in `freehand-debug`. It owns semantic debug position, scene debug position, trace envelope, and reusable debug snapshot shape. It is observation-only: it may be imported by other modules but must not own request, session, reason, provider, or UI rendering truth.
- 2026-06-16: Freehand problem location workflow is now explicitly routed by `Owner Routing Index` in `docs/architecture/feature-map.md`. Every feature or bug should map `problem area -> feature_id -> owner -> function map -> test-design doc` before code changes; grep is evidence after routing, not the owner source.
- 2026-06-16: `debug.core` runtime baseline is now landed. `freehand-debug` owns `DebugHub`, subscriber fanout, `StdoutDebugSink`, and `FileDebugSink`, while `freehand-reason` emits lifecycle debug observations into that hub without changing turn truth. Current durable limitation: sink-dispatch failures are explicit at `DebugHub::emit`, but reason-side integration still treats them as observation-only and does not surface them through a dedicated debug-error chain.
- 2026-06-16: After landing `debug.core` runtime fanout/sink and `reason.turn` debug emission, the full repo baseline verified clean with `cargo fmt --all`, `cargo build --workspace`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo test --workspace`, and `cargo run -p xtask -- gates check`.
- 2026-06-16: `ui.protocol` now owns the first receiver-side debug bridge. `UiProtocolState::apply_debug_event` and `UiProtocolState::drain_debug_receiver` ingest `freehand-debug::DebugEvent` and materialize only `DebugStateSnapshot` into protocol state. `freehand-server` remains protocol-only while rendering debug query output from that state.
- 2026-06-16: first WebUI transport baseline is now locked. `freehand-server` uses HTTP query plus SSE subscribe as a protocol-only service boundary over `UiProtocolState`; this transport is intentionally separate from node pairing WebSocket semantics and remains read-only for now.
- 2026-06-16: command ingress is now explicitly split from query/subscribe. `freehand-ui-protocol` owns `accept_command_ingress` plus rejection mapping, and `freehand-server` exposes POST `/ui/command` only as a validation/ack boundary. Mutation-intent commands may enter this route; query/subscribe commands are explicit misuse and must use their dedicated read endpoints instead.
- 2026-06-16: UI command ingress now has a protocol-owned dispatch-port baseline. `freehand-ui-protocol` declares owner routing through `build_command_dispatch_envelope` and `UiCommandDispatchPort`; current smoke routing is `reason.turn` for submit/cancel/resume and `node.master-slave` for direct slave messages. `freehand-server` still does not own business dispatch logic; it only calls a protocol-owned static smoke port and returns dispatch receipts.
- 2026-06-16: `runtime.ui-command-dispatch` is now a landed owner layer in `freehand-runtime`. It composes `ReasonTurnEngine`, `SessionHistory`, `LocalNodeRuntime`, and derived `UiProtocolState`, and it is the correct home for runtime-backed UI command execution. Current landed behavior is: submit/cancel -> `reason.turn`, direct message -> `node.master-slave`, resume -> explicit unsupported error. `freehand-server` remains protocol-only and is not yet injected with this runtime owner.
- 2026-06-16: first runtime-backed UI host baseline is now landed. `apps/freehand-server` was refactored into a reusable protocol-only transport library plus smoke bin, and new `apps/freehand-daemon` injects `freehand-runtime::RuntimeCommandDispatcher` into that shared HTTP/SSE/command transport. This closes the initial runtime-host gap while keeping `freehand-server` protocol-only and keeping `apps/freehand-daemon` free of direct reason/node/provider/config dependencies.
- 2026-06-16: runtime host bootstrap now has a config-selected baseline. `freehand-runtime` owns `RuntimeCommandDispatcher::from_selected_agent` and `::from_default_config`, so `apps/freehand-daemon` can start with `serve --agent <name>` without directly depending on `freehand-config`. `config.core` now owns explicit reciprocal peer topology with `node_id` and `paired_agent`; runtime bootstrap consumes configured local/paired node ids instead of deriving synthetic peer ids.
- 2026-06-16: `provider.reason-live-bridge` owner migration is now closed. The only live bridge owner is `crates/freehand-runtime`; `freehand-testkit` keeps harness/persistence smokes only. `apps/freehand-cli` and `apps/freehand-daemon` now both consume the runtime-owned bridge, and daemon command ingress must run injected synchronous runtime dispatch through `tokio::task::spawn_blocking` so protocol-only async transport does not panic when live provider executors create/drop blocking HTTP runtimes.
- 2026-06-16: daemon-path restore is now part of the runtime truth. `RuntimeCommandDispatcher::new` restores persisted live turns into `UiProtocolState` and resumes runtime turn ordinal allocation from persisted `runtime-turn-*` ids, so daemon restart can serve restored query/SSE output immediately and the next live submit continues at the next turn id instead of overwriting `runtime-turn-1`.
- 2026-06-16: real provider verification now exists for the daemon path. With local `~/.freehand/config.toml` updated to the locked peer-topology minimum (`master/worker`, reciprocal `paired_agent`, explicit `node_id`, shared pair-token env), both `freehand-cli reason-live --agent master` and `freehand-daemon` HTTP command ingress successfully drove the configured Anthropic-compatible `minimonth` provider through tool call, completion schema, persistence, restart restore, and post-restart second-turn continuation.
- 2026-06-16: WebUI static prototype visual correction
  - autonomous-region layout is the correct direction: top bar owns master/slave status, conversation owns the message stream, inspector stays secondary
  - user preferred the layout after spacing/sectioning was tightened, but rejected muddy warm colors
  - better palette direction is neutral canvas plus restrained blue/green/amber/red state colors, with tool/failure blocks using lighter fills and more white-card treatment
  - conversation must remain the visual center; side rails and inspector should stay visually subordinate
  - composer size is part of usability truth, not a micro-style detail; it must support multi-step operator prompts
  - semantic message cards should present state through whole-card border plus colored title/header treatment
  - white and black themes should both exist and preserve the same semantic color mapping
- 2026-06-16: `app.webui-smoke` implementation baseline is now beyond text-only smoke. `freehand-server` serves a real protocol-driven page shell from `/`, serves split theme assets (`assets/theme.css`, `assets/theme.js`) separately from layout/protocol-consumer assets (`assets/webui.css`, `assets/webui.js`), and the browser page consumes existing latest-turn/debug query+SSE endpoints plus POST command ingress without importing runtime/reason/provider semantics into the app boundary.
- 2026-06-16: current WebUI transport truth is now initial-snapshot plus continuous live-streaming. `freehand-ui-protocol` owns the subscription channel and incremental shared-contract turn projection updates; `freehand-runtime` streams reason/debug updates into that state during live turns; `freehand-server` keeps SSE connections open and forwards later matching updates. The WebUI still re-queries latest turn truth after submit receipt to cover browser timing, but continuous incremental streaming is now landed.
- 2026-06-16: `tool.registry` is now a distinct durable owner in `crates/freehand-tools`. Built-in tool names, schemas, `read_only` flags, and implemented-state truth live there only. Runtime may expose only `implemented_definitions()` and execute only through the registry owner; it must not hardcode demo tools. New tools are not allowed onto the live provider path until tool design, function map, test design, and owner code land together.
- 2026-06-16: the first real read-only tool batch is now implemented in `crates/freehand-tools`: `read_file`, `glob`, `grep`, `ls`, plus existing `todo_write` and `complete_step`. For v1, path-based tools are directory-locked to the canonical process current working directory, reject path escape outside that root, and keep runtime/tool ownership single-sourced. `freehand-runtime`, `freehand-cli`, and `freehand-daemon` now verify real registry-tool live loops without hardcoded `todo_write` tool choice.
- 2026-06-16: migrated function-map truth now has a three-part split. Human-readable function maps remain under `docs/function-maps/`, machine-readable mainline call truth lives under `docs/mainline-calls/`, and generated wiki artifacts live under `docs/wiki/`. `xtask mainlines generate|check` is the only supported path for wiki generation/freshness, and `xtask gates check` now rejects stale generated wiki artifacts.
- 2026-06-16: `ui.protocol` is now the second migrated feature on the machine-readable mainline-call path after `tool.registry`. For migrated features, `feature-map`, `function-map`, `test-design`, `mainline-call`, and generated `wiki` must move together; duplicated owner entries in `feature-map` are invalid and must be physically removed.
- 2026-06-16: `reason.turn` is now migrated onto the machine-readable mainline-call path. The reason-turn wiki is generated from `docs/mainline-calls/reason.turn.json`, so future changes to turn startup, provider-output materialization, completion schema terminalization, debug emission, or session projection must update the JSON source and regenerate wiki in the same change.
- 2026-06-16: `provider.reason-live-bridge` is now migrated onto the machine-readable mainline-call path. The runtime live bridge wiki is generated from `docs/mainline-calls/provider.reason-live-bridge.json`, so future changes to live provider execution, tool re-entry, schema retry, or persistence restore must update the JSON source and regenerate wiki in the same change.
- 2026-06-16: `provider.semantic` is now migrated onto the machine-readable mainline-call path. The provider semantic wiki is generated from `docs/mainline-calls/provider.semantic.json`, so future changes to request normalization, adapter event mapping, or recovery classification must update the JSON source and regenerate wiki in the same change.
- 2026-06-16: `provider.anthropic-adapter` is now migrated onto the machine-readable mainline-call path. The Anthropic adapter wiki is generated from `docs/mainline-calls/provider.anthropic-adapter.json`, so future changes to Messages request rendering, SSE parsing, tool-use accumulation, executor HTTP/SSE behavior, or Anthropic tool-result rendering must update the JSON source and regenerate wiki in the same change.
- 2026-06-16: `provider.openai-adapter` is now migrated onto the machine-readable mainline-call path. The OpenAI-compatible adapter wiki is generated from `docs/mainline-calls/provider.openai-adapter.json`, so future changes to Responses rendering, Chat Completions rendering, stream parsing, partial tool-call accumulation, or OpenAI finish-reason mapping must update the JSON source and regenerate wiki in the same change.
- 2026-06-17: `reason.persistence` is now migrated onto the machine-readable mainline-call path. The persistence wiki is generated from `docs/mainline-calls/reason.persistence.json`, so future changes to snapshot persistence, ledger append/replay, restore, or sidecar rebuild must update the JSON source and regenerate wiki in the same change.
- 2026-06-17: `reason.session-history` is now migrated onto the machine-readable mainline-call path. The session-history wiki is generated from `docs/mainline-calls/reason.session-history.json`, so future changes to rewrite gates, rewrite version ownership, persisted session truth, or turn-start rewrite sourcing must update the JSON source and regenerate wiki in the same change.
- 2026-06-17: `reason.rewrite-policy` is now migrated onto the machine-readable mainline-call path. The rewrite-policy wiki is generated from `docs/mainline-calls/reason.rewrite-policy.json`, so future changes to compaction thresholds, follow-up pause logic, recovery rewrite classification, or provider-usage-to-prompt-pressure conversion must update the JSON source and regenerate wiki in the same change.
- 2026-06-17: `reason.context-planner` is now migrated onto the machine-readable mainline-call path. The context-planner wiki is generated from `docs/mainline-calls/reason.context-planner.json`, so future changes to context segment admission, stable-prefix ordering, subagent conclusion admission, cache diagnostics, or metadata/request isolation must update the JSON source and regenerate wiki in the same change.
- 2026-06-17: `debug.core` is now migrated onto the machine-readable mainline-call path. The debug-core wiki is generated from `docs/mainline-calls/debug.core.json`, so future changes to debug snapshots, trace envelopes, hub fanout, sink behavior, or semantic/scene coordinate contracts must update the JSON source and regenerate wiki in the same change.
- 2026-06-17: `runtime.ui-command-dispatch` is now migrated onto the machine-readable mainline-call path. The runtime-dispatch wiki is generated from `docs/mainline-calls/runtime.ui-command-dispatch.json`, so future changes to command owner routing, config-selected bootstrap, live reason hook projection, runtime UI state restore, or node direct-message dispatch must update the JSON source and regenerate wiki in the same change.
- 2026-06-17: `foundation.workspace` is now migrated onto the machine-readable mainline-call path. The workspace-gate wiki is generated from `docs/mainline-calls/foundation.workspace.json`, so future changes to xtask gate policy, mainline generation, wiki freshness checks, or required-file enforcement must update the JSON source and regenerate wiki in the same change.
- 2026-06-17: `config.core` is now migrated onto the machine-readable mainline-call path. The config-core wiki is generated from `docs/mainline-calls/config.core.json`, so future changes to config load/validate, provider selection, peer-topology projection, or safe secret projection must update the JSON source and regenerate wiki in the same change.
- 2026-06-17: `contracts.core` is now migrated onto the machine-readable mainline-call path. The contracts-core wiki is generated from `docs/mainline-calls/contracts.core.json`, so future changes to shared IDs, request/response/error contracts, validation helpers, or serialization/replay boundaries must update the JSON source and regenerate wiki in the same change.
- 2026-06-17: `node.master-slave` is now migrated onto the machine-readable mainline-call path. The node-topology wiki is generated from `docs/mainline-calls/node.master-slave.json`, so future changes to pairing, pairing-loss relisten, direct-message guards, delegated progress, or slave-turn publication must update the JSON source and regenerate wiki in the same change.
- 2026-06-17: `app.cli-runtime-smoke` is now migrated onto the machine-readable mainline-call path. The CLI smoke wiki is generated from `docs/mainline-calls/app.cli-runtime-smoke.json`, so future changes to CLI config startup, harness-backed reason smoke, or blocked-recovery projection must update the JSON source and regenerate wiki in the same change.
- 2026-06-17: `app.cli-live-turn` is now migrated onto the machine-readable mainline-call path. The CLI live-turn wiki is generated from `docs/mainline-calls/app.cli-live-turn.json`, so future changes to runtime-owned live bridge reuse, safe live summary projection, completion-loop counts, or restore/tool summary reporting must update the JSON source and regenerate wiki in the same change.
- 2026-06-17: `app.webui-smoke` is now migrated onto the machine-readable mainline-call path. The WebUI transport wiki is generated from `docs/mainline-calls/app.webui-smoke.json`, so future changes to protocol-only HTTP/SSE routes, public conversation projection consumption, theme/layout asset split, or command-ingress receipt handling must update the JSON source and regenerate wiki in the same change.
- 2026-06-17: `app.runtime-daemon` is now migrated onto the machine-readable mainline-call path. The daemon host wiki is generated from `docs/mainline-calls/app.runtime-daemon.json`, so future changes to config-selected bootstrap, injected runtime dispatch, restore-before-serve behavior, or shared HTTP/SSE transport host wiring must update the JSON source and regenerate wiki in the same change.
- 2026-06-17: `tool.registry` now has a first real writable file-mutation set in `freehand-tools`: `write_file`, `edit_file`, and `multi_edit`. These tools are locked to the canonical workspace root, require existing parent directories, and write only through one owner-controlled atomic path. Future changes to writable tool semantics must update tool design, function map, test design, mainline JSON, and generated wiki in the same change.
- 2026-06-17: `tool.registry` now has a first real foreground command tool in `freehand-tools`: `bash`. It is foreground-only, starts from the locked workspace root, defaults to a 900-second timeout, captures combined stdout/stderr, and fails explicitly on timeout or non-zero exit. It does not yet claim filesystem/network sandboxing; `bg_jobs`, `kill_shell`, `wait_job`, and other higher-side-effect tools remain explicitly unimplemented. Tool-surface changes that affect live provider exposure now require one real config-selected `reason-live` smoke when credentials are available, and selected-agent bootstrap still requires the configured pair-token env even for CLI live-turn verification.
- 2026-06-17: Writable-tool lifecycle is now split explicitly. `tool.preview` is the durable owner for no-write writable-tool preview truth and preview/execute parity in `crates/freehand-tools`; `runtime.checkpoint-rewind` is the durable owner for workspace snapshot, restore manifest, and explicit rewind lifecycle in `crates/freehand-runtime`. Checkpoint state lives under `~/.freehand/state/checkpoints` plus `~/.freehand/ledgers/checkpoints` and is intentionally separate from `reason.persistence` authoritative session/turn truth.
- 2026-06-17: `tool.preview` first code slice is now landed. `freehand-contracts` owns shared preview contracts (`ToolPreviewChangeKind`, `ToolPreviewFileChange`, `ToolPreviewContract`), and `freehand-tools` now binds `BuiltinToolRegistry::preview` for `write_file`, `edit_file`, and `multi_edit`. Those three writable tools now share one semantic `plan_*` transform path between preview and execute; `delete_range` preview and runtime checkpoint consumption remain pending.
- 2026-06-17: `runtime.checkpoint-rewind` baseline is now implemented in `freehand-runtime`. `RuntimeCheckpointStore::new`, `::create_from_preview`, shared manifest/ledger writers, `execute_registry_tool_call`, and public `rewind_checkpoint(...)` now own live writable-tool checkpointing and rewind. Writable live tools now run `preview -> snapshot -> execute`; checkpoint manifests live under `~/.freehand/state/checkpoints/<agent>/<session>/<checkpoint>/manifest.json`, lifecycle ledgers live under `~/.freehand/ledgers/checkpoints/<agent>/<session>.jsonl`, and previewless writable tools are rejected explicitly instead of being executed.
- 2026-06-17: explicit checkpoint rewind is now wired through the shared UI/runtime transport chain. `freehand-ui-protocol` owns `UiCommand::RewindCheckpoint { checkpoint_id }` plus non-empty-id validation and owner routing to `runtime.checkpoint-rewind`; `freehand-runtime::RuntimeCommandDispatcher` executes that command only when a live runtime home is present; `freehand-daemon` HTTP command ingress now proves writable submit -> checkpoint creation -> rewind restore without making the app boundary own rewind logic.
- 2026-06-17: checkpoint visibility now has a read-only query/projection path. `freehand-runtime` owns `list_checkpoints` / `RuntimeCheckpointStore::list_summaries` from manifest plus checkpoint ledger truth; `freehand-ui-protocol` owns `UiCheckpointSnapshot` query projection; `freehand-server` only serves `/ui/query/checkpoints` from protocol state; WebUI renders a secondary checkpoint inspector and sends explicit `RewindCheckpoint` commands without parsing runtime files.
- 2026-06-17: normal WebUI conversation path is now verified through real daemon/provider HTTP/SSE. Blank latest-turn query remains snapshot-only 404, while blank latest-turn SSE stays open and emits the first turn. `UiTurnProjection.user_text` and `public_conversation[0]` preserve the original operator prompt; runtime multi-round continuation prompts must not leak into public UI projection. Real provider smoke on configured `minimonth` passed with `read_file` tool execution, command 202, final terminal completion, and original prompt preserved in final query.
- 2026-06-17: `bash` is executable but not checkpointable file-mutation preview truth. Runtime checkpointing is now limited to `write_file`, `edit_file`, and `multi_edit`; non-file-mutation tools such as `bash` execute through `tool.registry` without preview/checkpoint ledger creation and still fail explicitly on tool execution errors.
- 2026-06-17: WebUI/runtime stop is now a protocol-owned cancellation path, not local input clearing. `CancelTurn` targets a known turn, `CancelLatestActiveTurn` stops the newest active turn during the pre-SSE submit window, `ReasonTurnEngine::cancel_turn` writes `TerminalStatus::Cancelled`, and `UiTurnProjection.terminal_status` preserves cancelled status so public UI does not render cancelled/failed terminals as completed. Runtime live submit registers an active cancel token and releases the dispatcher mutex before provider IO, so cancel can enter concurrently and later provider success cannot overwrite cancelled UI projection.
- 2026-06-17: `foundation.workspace` stability gate now treats migrated mainline-call JSON files as compiled manifests. `xtask gates check` validates that each `docs/mainline-calls/<feature_id>.json` path, internal `feature_id`, function map path, test design path, generated wiki path, and feature-map links are deterministic and cross-linked; `cargo test -p xtask` includes positive and negative coverage for this verifier.
- 2026-06-17: `foundation.workspace` now validates migrated mainline call-table bindings. `xtask gates check` requires every `binding_status = "bound"` row to reference existing source files and symbols resolvable in those files, with Rust `Type::method` rows matched through method-tail lookup; `cargo test -p xtask` includes positive and negative coverage for this verifier.
- 2026-06-17: `foundation.workspace` now treats `make ci` as the canonical full local gate. `make ci` runs build, fmt, clippy, workspace tests, `xtask mainlines check`, and `xtask gates check`; pre-push, GitHub CI, and release workflows route through that full gate. `xtask gates check` validates this CI/CD command alignment and has positive/negative `cargo test -p xtask` coverage.
- 2026-06-17: `metadata.core` baseline is now implemented in `crates/freehand-metadata`. It owns internal control/provenance metadata envelopes, writer owner provenance, write-node provenance, trace/session/turn subject identity, validation, and an in-memory metadata center. Metadata entries reject request-data-like keys such as `request.*`, `payload.*`, `prompt.*`, `input.*`, `content`, and `text`. `reason.turn` is now the first producer: `ReasonTurnEngine::start_turn` and `ReasonTurnEngine::apply_provider_output` write owner/node-provenance metadata before session-history commit or turn mutation; runtime/provider/debug producers and persistent metadata ledgers remain pending.
- 2026-06-18: `debug.core` now owns a dedicated observation-failure stream for sink-dispatch failures through `DebugObservationFailure` and `DebugHub::subscribe_failures`. `reason.turn` producer integration proves those failures are observable without mutating turn truth, error truth, or terminal truth.
- 2026-06-18: `metadata.core` now owns durable metadata ledger append/reload in `crates/freehand-metadata`. `MetadataCenter::write` preserves failure atomicity by appending to the configured ledger before mutating in-memory records, and `reason.turn` producer tests prove persisted metadata stays request-text-isolated.
- 2026-06-18: `foundation.workspace` now enforces the first repo-wide static metadata/request isolation gate in `xtask`. The gate is intentionally narrow and source-level only: `ReasonReq*` structs in `freehand-contracts` must not add metadata/debug owner types or obvious metadata/debug fields, `Metadata*` owner types must stay inside `crates/freehand-metadata`, and metadata owner structs must not add request-payload field names or types. White-box tests cover both positive and negative cases, and `make ci` passed with the gate enabled.
- 2026-06-18: tool-schema fingerprint wiring is now closed across the owner chain `tool.registry` -> `reason.turn` -> `provider.reason-live-bridge` -> `reason.context-planner`. `freehand-tools` owns canonical implemented-schema fingerprint generation, runtime live rounds stamp that fingerprint into `TurnStartInput`, and planner diagnostics now include tool-schema-hash drift without moving tool schema semantics into reason owners. Docs/mainlines/wiki and full gate were refreshed in the same change.
- 2026-06-18: runtime-owned metadata producer wiring is now closed for `provider.reason-live-bridge`. `freehand-runtime` bootstraps a shared ledger-backed `MetadataCenter` at `~/.freehand/ledgers/metadata/<agent>/<session>.jsonl`, passes it into `reason.turn`, and writes runtime-owned restore/provider-request/tool/terminal lifecycle metadata with explicit owner and pipeline-node provenance. Metadata bootstrap/write failure is explicit `RuntimeLiveBridgeError::MetadataFailed`; provider/debug producers remain the next pending metadata slice.
- 2026-06-18: `debug.core` now has a second real producer beyond `reason.turn`: `provider.reason-live-bridge` in `freehand-runtime` emits runtime-owned restore/provider-request/tool/terminal lifecycle debug snapshots through the shared `DebugHub`. These snapshots are observation-only, exclude prompt text, provider payload text, and tool-result content, and terminal close snapshots are drained before the live bridge returns so hook consumers and UI/debug bridges can observe them deterministically.
- 2026-06-18: provider raw debug-ledger wiring is now closed across `provider.anthropic-adapter` -> `provider.reason-live-bridge` -> `reason.persistence`. `freehand-provider-anthropic` exposes raw response/error/event bodies through raw-capable executor callbacks before semantic parsing, `freehand-runtime` maps them into scene-provenanced writes, and `freehand-reason` persists them under `~/.freehand/ledgers/providers/<family>/<agent>/<session>/<turn>.jsonl`. These ledgers are debug-only, never recovery truth, and enabled-path write failure is explicit `RuntimeLiveBridgeError::ReasonPersistenceFailed` with no fallback.
- 2026-06-18: `provider.reason-live-bridge` now explicitly regression-locks runtime tool-loop failure truth for both unknown tool names and registered-but-unimplemented tool names. In both cases the bridge stops with `RuntimeLiveBridgeError::ToolExecutionFailed(...)`, preserves the provider-emitted tool call in active turn truth, and must not materialize tool-result truth or terminal-success truth.
- 2026-06-18: `reason.persistence` now explicitly regression-locks three recovery-failure truths: reason-ledger sequence gaps block recovery with `LedgerSequenceGap`, provider-raw-only artifacts do not mask missing authoritative truth, and UI-sidecar-only artifacts do not mask missing authoritative truth. Recovery still requires authoritative snapshots or reason-ledger truth.
- 2026-06-18: `reason.persistence` now also explicitly regression-locks snapshot-integrity failure truth: corrupt persisted snapshot JSON fails recovery with `JsonParseFailed`, incoherent snapshot/cursor state fails with `InvalidCursorCoherence`, and duplicate ledger sequence numbers block recovery instead of being silently tolerated.
- 2026-06-18: `metadata.core` now explicitly regression-locks four validation/error branches: empty `metadata_id`, empty `trace_id`, empty `entries`, and durable-ledger lines that parse but fail validation. These cases now fail explicitly instead of being treated as admissible metadata truth.
- 2026-06-18: `metadata.core` now also explicitly regression-locks the remaining owner/entry validation branches inside `freehand-metadata`: empty owner `feature_id`, `crate_name`, `module_path`, `symbol_path`, missing pipeline node, and empty entry keys are all covered as direct white-box failures. The broader metadata gap is now producer wiring in provider/debug/node owners, not core validation coverage.
- 2026-06-18: `reason.session-history` now explicitly regression-locks its documented error mainline inside `freehand-reason`: empty rewrite reasons, forbidden rewrite base segments, invalid persisted json, file-io failure on persist/load, and `ReasonTurnEngine::start_turn` session mismatches all fail explicitly. The remaining session-history gap is not error-branch ambiguity but final runtime-loop wiring of real usage metrics and persisted recovery payloads.
- 2026-06-18: `debug.core` now explicitly regression-locks file-sink persistence semantics inside `freehand-debug`: file sinks append replay-safe JSONL instead of overwriting, real file-sink io failures surface as explicit `DebugSinkError::Io`, and disabled hubs do not dispatch to subscribers or sinks. The remaining debug gap is still owner wiring and path policy, not sink error ambiguity.
- 2026-06-18: `node.master-slave` now explicitly regression-locks several documented rejection paths inside `freehand-node`: unauthorized pair source node, unauthorized pair source ip, empty delegated task status, and pre-pair or intruder slave-turn publication all fail explicitly and must not materialize status/progress/turn truth. The remaining node gap is transport heartbeat/reconnect policy and real websocket IO, not rejection semantics.
- 2026-06-18: live cancellation in `provider.reason-live-bridge` is now locked at the terminal-persistence boundary as well as earlier live-loop boundaries. If cancel lands after terminal broadcast but before runtime terminal metadata/persistence, `run_live_reason_turn` now returns explicit `RuntimeLiveBridgeError::Cancelled` and `ReasonPersistence` must not materialize either active-turn terminal truth or closed-turn truth. This behavior is covered in `freehand-runtime` by paired negative tests and validated again by full `make ci`.
- 2026-06-18: `runtime.checkpoint-rewind` failure truth is now regression-locked for missing manifest, missing blob, and corrupt checkpoint-ledger cases. `rewind_checkpoint(...)` must surface `MissingManifest` or `MissingBlob` explicitly, `list_checkpoints(...)` must fail explicitly on corrupt ledger lines instead of degrading to UI snapshot truth, runtime bootstrap must fail with `CheckpointProjectionBootstrap(...)` if checkpoint projection refresh reads corrupt ledger truth, and runtime dispatch must map missing checkpoint rewind manifests to `TargetNotFound`. Full `make ci` passed with these owner-bound negative tests and synced mainline/wiki/docs.
- 2026-06-18: `app.runtime-daemon` now black-box locks the transport projection of missing checkpoint rewind manifests. After a real daemon HTTP submit creates a writable checkpoint, deleting the manifest and calling daemon HTTP rewind must return an HTTP failure payload with code `command_dispatch_target_not_found`, `retryable=false`, and no false success projection. This proves the app boundary preserves runtime owner failure semantics on the real command ingress path.
- 2026-06-18: `app.webui-smoke` now black-box locks command-ingress failure projection at the protocol-only app boundary. If the injected dispatch port returns `DispatchFailed`, HTTP `/ui/command` must surface `command_dispatch_port_failure` with `retryable=true`; if the dispatch task panics and join fails, it must surface `dispatch_join_failed` with `retryable=false`. The `app.webui-smoke` feature-map owner entry was also deduplicated back to one truth, and the mainline call table now binds the failure path to the real symbol `handle_command_ingress`.
- 2026-06-18: `app.runtime-daemon` now black-box locks corrupt checkpoint-projection bootstrap failure at startup. If `~/.freehand/ledgers/checkpoints/<agent>/runtime-session-<agent>.jsonl` contains invalid ledger lines, `build_runtime_dispatcher_from_default_config` must fail before daemon transport serve with explicit checkpoint-projection bootstrap parse evidence; this startup-failure path is now synchronized in the daemon function map, test design, mainline JSON, and generated wiki.
- 2026-06-18: `runtime.ui-command-dispatch` now explicitly regression-locks three missing-target failure contracts: unknown `CancelTurn`, empty-state `CancelLatestActiveTurn`, and wrong-node `SendDirectMessageToSlave` must all return `UiCommandDispatchPortError::TargetNotFound` instead of degrading into generic dispatch failure or success projection. This closes a previously documented-but-unverified error path in the runtime dispatch owner.
- 2026-06-18: `foundation.workspace` now enforces feature-map seed-entry uniqueness in `xtask`. Duplicate `### \`<feature_id>\`` blocks inside `docs/architecture/feature-map.md` fail `xtask gates check`, with positive and negative `cargo test -p xtask` coverage added after a real duplicate `app.webui-smoke` owner block was manually discovered and removed.
- 2026-06-18: `node.master-slave` is now a real metadata producer. `LocalNodeRuntime::with_metadata_center` writes owner/write-node provenance for bootstrap, pairing, pairing-loss relisten, delegated progress, and slave-turn publication before node truth mutation; metadata write failure is explicit `NodeRuntimeError::MetadataWriteFailed(...)` and must not materialize rejected status, progress, or slave-turn truth.
- 2026-06-18: `runtime.ui-command-dispatch` live bootstrap now shares the node metadata ledger path at `~/.freehand/ledgers/metadata/<agent>/<session>.jsonl`. `RuntimeCommandDispatcher::new` injects a ledger-backed `MetadataCenter` into `LocalNodeRuntime` for live bootstraps, and owner tests plus `make ci` verified the persisted ledger contains node bootstrap and pairing records without pair-token leakage.
- 2026-06-18: `runtime.ui-command-dispatch` now explicitly regression-locks the negative side of shared node metadata bootstrap. If `~/.freehand/ledgers/metadata/<agent>/<session>.jsonl` is not writable during config-selected live bootstrap, `RuntimeCommandDispatcher::from_selected_agent_with_live(...)` fails as `NodeRuntimeInit(...)` before any dispatcher materializes; this path is now synchronized in runtime function map, test design, mainline JSON, generated wiki, and full `make ci`.
- 2026-06-18: `node.master-slave` is now a real `debug.core` producer. `LocalNodeRuntime::with_debug_hub` / `with_debug_hub_and_metadata_center` emit observation-only lifecycle snapshots for bootstrap, pairing acceptance/rejection, pairing loss, delegated progress, and slave-turn publication. White-box tests prove these snapshots exclude pair-token, user-turn, reasoning-text, answer-text, and terminal-text leakage, and debug sink failures remain observable through `DebugHub::subscribe_failures` without blocking node truth mutation.
- 2026-06-18: `xtask mainlines generate` and `xtask mainlines check` must run sequentially. Parallel execution can race on generated wiki artifacts and create false out-of-date failures even when JSON truth is correct.
- 2026-06-18: Freehand control semantics must stay extracted from data pipelines. `foundation.workspace` now enforces this as a broader `verify_data_control_boundaries` gate: `ReasonReq*` request nodes may not carry metadata/debug/control owner types or obvious control fields, and metadata owner structs may not carry request payloads or control execution payloads such as cancel tokens, retry policies, checkpoint payloads, route policies, or gate decisions. Control state must live in owner modules, ledgers, metadata, or debug channels, not ad hoc request/prompt/provider-payload rewrites.
- 2026-06-26: `MetadataKind` 物理删除 `Control` + `DebugLink` 两个死变体（生产代码 0 次使用）；debug 与 metadata 中心化保持物理隔离而非通过 DebugLink 变体交叉引用。`is_forbidden_metadata_field_type` 同步移除 `|| ty.contains("Control")` 规则以匹配新 enum。docs/architecture/architecture-gaps.md、docs/design/metadata-core-design.md、docs/testing/metadata.core.md、docs/function-maps/metadata.core.md、docs/mainline-calls/metadata.core.json 同步更新；`make ci` EXIT 0。
- 2026-06-26: provider adapter 错误路径接入 metadata 中心化。`freehand-runtime` 新增 `record_provider_error_metadata` + `emit_provider_error_debug`，在单次和流式 executor 错误返回路径写入 `RuntimeLive05ProviderError` pipeline node（MetadataKind::Provider），白盒测试验证 HTTP 500 → metadata ledger 写入。Gap 2 部分关闭：provider error 已覆盖，请求构造/响应解析成功路径依赖现有 raw capture callback 无需 metadata。
- 2026-06-26: tool.preview Gap 1 关闭。`delete_range` 从 `spec(implemented=false)` 改为 `implemented=true`，实现 `plan_delete_range` + `execute_delete_range`，preview/execute 共享 plan 路径。白盒测试 2 passed（inclusive 正确路径 + anchor-not-found 错误路径）。preview dispatch 已包含 `delete_range` match arm，runtime checkpoint gate 无需额外变更。docs/architecture/architecture-gaps.md Gap 1 已删除（管理规则第 3 条）。
- 2026-06-27: `app.android-client` is an existing native Android shell scaffold, not a future crate. Live render host is `apps/freehand-android/app/src/main/assets/bridge.html`; `apps/freehand-server/assets/mocks/android/mobile-mock.html` stays preview-only. `docs/design/multi-platform-ui-architecture.md`, `docs/design/android-client-v1-execution.md`, `docs/architecture/feature-map.md`, `docs/testing/app.android-client.md`, `docs/function-maps/ui.protocol.md`, and `.agents/skills/freehand-dev/SKILL.md` were aligned to that split. Verified: `cargo run -p xtask -- mainlines check`, `cargo run -p xtask -- gates check`, `cargo test -p freehand-server --lib`, and `cd apps/freehand-android && JAVA_HOME=/opt/homebrew/opt/openjdk@17 PATH=/opt/homebrew/opt/openjdk@17/bin:$PATH ./gradlew testDebugUnitTest` all passed.
- 2026-06-28: live tool execution failure must materialize failed turn truth before surfacing dispatch failure. `provider.reason-live-bridge` writes error + failed terminal truth for tool failures, `runtime.ui-command-dispatch` refreshes `UiProtocolState` from persistence before returning the error, and `ui.protocol` marks still-waiting tool activities failed when terminal truth is failed. Verified by runtime/UI protocol regression tests, `xtask mainlines/gates`, global install, launchd restart on fixed port `127.0.0.1:4041`, real `POST /ui/command` causing `ls ~/code/codex` failure, latest-turn query, and latest-turn SSE; final projection was `runtime-turn-17` with one current failed `ls` tool activity, failed terminal, and failed error card.
- 2026-06-28: WebUI default control/status path is now ADP WebSocket `/adp`. `apps/freehand-server/src/page.rs` advertises `data-adp-endpoint="/adp"` and `apps/freehand-server/assets/webui.js` uses ADP query/subscribe/command frames by default, rendering `subscription_accepted`, subscription events, command receipts, and ADP failures visibly. HTTP query/SSE/POST routes remain compatibility transport surfaces. Verified by `node --check`, `cargo test -p freehand-server`, `xtask mainlines generate/check`, `xtask gates check`, local page/JS smoke, and local no-browser WebSocket smoke observing `subscription_accepted`, `subscription_event`, `query_result`, and explicit `ingress_command_kind_mismatch` failure.
- 2026-06-28: Android live shell default control/status path is now ADP WebSocket `/adp`. `MainActivity` wires `AdpEventStream`, `HostConfig.adpUrl` and `ClientConfig.adpPath` make the endpoint explicit, and `TimelineProjector.applyAdp` consumes `query_result`, `subscription_event`, `subscription_accepted`, and `failure` frames with visible failure projection to `bridge.html`. `ProtocolClient` and `SseEventStream` remain compatibility transport classes, not the default Android shell path. Verified by Android JVM tests, `xtask mainlines generate/check`, `xtask gates check`, and Android mock route smoke.
- 2026-06-28: CLI/headless ADP smoke is implemented as `freehand-cli adp-smoke --url ws://.../adp`. It uses typed `freehand-ui-protocol` ADP frames, verifies `subscription_accepted`, `subscription_event`, `query_result`, and the query-as-command `ingress_command_kind_mismatch` failure, and gives no-UI diagnosis without WebUI/Android. Verified by `cargo test -p freehand-cli` local mock WebSocket smoke and a real local `freehand-server webui-serve-smoke` `/adp` run.
- 2026-06-28: `scripts/install-launchd.sh install` and `scripts/install-launchd.sh restart` now wait for `/health` readiness before returning success. The earlier immediate `restart && curl` failure was a startup-window race; bounded polling showed the daemon became healthy after a short delay, and the fixed port `127.0.0.1:4041` remained correct.
- 2026-06-28: WebUI and headless ADP now have paired success/failure samples. WebUI exposes `Success sample` and `Failure sample` composer buttons that load the same ADP sample prompts used by `freehand-cli adp-turn-sample --sample success|failure`; sending still uses the normal ADP command path. Verified installed fixed-port samples: success `runtime-turn-21` with `terminal_status=Success`, failure `runtime-turn-22` with `terminal_status=Failed`.
- 2026-06-29: Minimonth remains the active verified provider baseline for the fixed-port daemon and WebUI smoke. `~/.freehand/config.toml` and `/Volumes/extension/.rcc/provider/minimonth/config.v2.toml` both resolve provider `minimonth`, and the latest live sample smoke on `ws://127.0.0.1:4041/adp` returned `adp_turn_sample_ok` for success turn `runtime-turn-41`.
- 2026-06-29: WebUI tool rendering is compact and state-driven. Historical completed turns must not keep blinking; tool success/failure is conveyed by a compact color dot, and repeated terminal/result text is suppressed when the parameter or diff line already carries the target. Verified on real daemon `127.0.0.1:4102` with failure sample screenshot `artifacts/webui-state-render-fix/20260629-compact-tool-display-live-v2/01-live-failure-sample.png` and counts `wait_model_continue_count=0`, `compact_tool_state_count=1`, `tool_block_count=1`, `running_state_count=0`.
- 2026-06-30: WebUI session/task creation semantics were corrected after review. `New conversation` and `/new` create a global conversation draft without requiring cwd; `New task` and `/task` require a visible target cwd and create a protocol-owned cwd-bound task session through ADP `CreateSession`. The previous "new session requires cwd" UI rule was an application-layer mistake and must not be revived. Base agent cwd/session-directory work remains a separate architecture layer from WebUI application task orchestration.
- 2026-06-30: Fixed daemon port is `127.0.0.1:4041`. If it is occupied, prefer service-scoped stop first (`launchctl stop gui/$(id -u)/com.freehand.daemon`), then exact PID TERM/KILL only if the service does not exit. After install/restart, verify `curl http://127.0.0.1:4041/health` and `ws://127.0.0.1:4041/adp` before claiming the daemon is ready.
- 2026-06-30: WebUI session CRUD cannot be claimed from protocol-only support. The page must expose visible session management controls. Current WebUI has session multi-select checkboxes plus Clear/Delete toolbar; Delete sends protocol-owned ADP `DeleteSession` per selected session and refreshes session truth from ADP.
- 2026-06-30: WebUI tool execution display is one card per `tool_call_id`; waiting/model-result phases must update the same card status bar with animation/timing instead of adding separate cards. Shell `pwd` display is projected by `tool.display` as "Read current working directory" and must not expose `command=pwd` in UI-visible summaries.
- 2026-06-30: Fixed daemon port is `127.0.0.1:4041`. If it is occupied, prefer service-scoped stop first (`launchctl stop gui/$(id -u)/com.freehand.daemon`), then exact PID TERM/KILL only if the service does not exit. After install/restart, verify `curl http://127.0.0.1:4041/health` and `ws://127.0.0.1:4041/adp` before claiming the daemon is ready.
- 2026-06-30: Completion schema rejection feedback must be type-aware. Non-string values in required text fields such as `evidence`, `summary`, and `learned` must not be collapsed into `is required`; the parser now reports explicit `must be a string` feedback and guidance now says required completion text fields must be plain strings, not arrays or objects. Verified with `cargo test -p freehand-blocks -- --nocapture` and `cargo test -p freehand-reason -- --nocapture`.
- 2026-06-30: Completion schema retry must be user-visible lifecycle state, not hidden internal retry. Non-terminal schema rejection publishes a UI/ADP `model_request` waiting detail, but the user-visible text must stay compact: `schema retry #N: <field issue>` plus elapsed timing, not long internal feedback narration.
- 2026-06-30: Tool-result continuation waiting must be runtime/ADP truth, not WebUI inference. After tool execution results are paired for the next provider request, runtime publishes a model-continuation waiting projection such as `tool result returned: 1 failed / 1 total · waiting model`; WebUI must not infer "waiting model" from completed/failed tool cards. Submitted composer text stays cleared; Up/Down history is the retry/recall path.
- 2026-06-30: No-UI ADP multi-round validation is now locked by `freehand-cli adp-turn-sample --sample failure`. The command creates an isolated session, submits the failed `read_file` sample over ADP, then queries `QuerySessionTurns`; success requires terminal success plus transcript evidence for `rounds>=2`, unique `tool_executions>=1`, and `failed_tools>=1`. Verified on fixed `127.0.0.1:4041` after global install with `session=cli-adp-sample-failure-1782834192632660000`, `turn=runtime-turn-65-r2`, `rounds=2`, `tool_executions=1`, `failed_tools=1`, and command receipt `reason_live_turn_completed rounds=2 schema_rejections=0 tool_executions=1`.
- 2026-06-30: Provider executor/transport failure is a system failure, not a tool result. The live bridge materializes `provider_executor_failure` error truth plus failed terminal truth and closes the active turn before returning dispatch failure, so ADP/WebUI must show a failed terminal instead of a hanging active turn. Tool execution failures remain paired failed tool results returned to the model for continuation.
- 2026-06-30: WebUI must not label inactive text-only/restored turns as live streaming. Without terminal status, waiting tool activity, submit-in-flight, or protocol-projected model request truth, rendering must be neutral/non-animated rather than fake streaming.
- 2026-07-01: Completion schema retry is gated by terminal-candidate finish reasons only. Runtime must parse/reject `<freehand_completion>` only after normalized provider finish reasons such as `stop`/`end_turn`; `tool_use` and incomplete tool calls must produce paired failed tool results returned to the model, not schema retries or terminal turn failures. Verified on fixed `127.0.0.1:4041` with `adp-turn-sample --sample failure`: `rounds=2`, `tool_executions=1`, `failed_tools=1`, `schema_rejections=0`, terminal success.
- 2026-07-01: WebUI selected-session transcript renders chronological per-round lifecycle cards for `runtime-turn-N` / `runtime-turn-N-rM`, not one all-in execution summary card. Only the first round shows the user prompt; superseded rounds show `continued`; final summary appears in the terminal row at the end of the round sequence. Screenshot evidence: `artifacts/webui-reasoning-state/20260701-round-cards/04-fixed-4041-round-sequence-tall.png`.
- 2026-07-03: WebUI runtime helper binding must be verified in a real browser, not only by `node --check`. A live WebUI submit on fixed `127.0.0.1:4041` exposed `modelRequestPhase is not defined`; the fix added `modelRequestPhase(turn)` beside `modelRequestKind` / `modelRequestLabel` and locked the helper definition/call in server asset smoke. Verified with `node --check`, `cargo test -p freehand-server -- --nocapture`, release build/install/restart, fixed-port health, served JS hash `faab159c8376736ea66fd64c9041298aca9ff0a11e13c5cda4c948ea2135b00f`, Playwright screenshots/DOM under `artifacts/webui-online/20260703-webui-after-model-phase-fix/`, and ADP session truth showing selected session `cli-adp-sample-success-1782953474447457000` had `turns=2` / `turn_ids=runtime-turn-1,runtime-turn-6`.
- 2026-07-04: WebUI online terminal proof should use fixed ADP success/failure sample sessions when a freeform browser prompt remains non-terminal. Current closeout verified the changed WebUI/ADP/runtime surface with `scripts/install-global.sh`, `scripts/install-launchd.sh restart`, fixed health `ok`, ADP smoke, `adp-turn-sample --sample success` (`cli-adp-sample-success-1783100519808659000`, `runtime-turn-1`, `rounds=1`), `adp-turn-sample --sample failure` (`cli-adp-sample-failure-1783100523482624000`, `runtime-turn-2-r2`, `rounds=2`, `tool_executions=1`, `failed_tools=1`), and browser screenshots/JSON under `artifacts/webui-online/20260704-closeout-terminal-samples/` showing selected terminal sessions with composer cleared, `liveCount=0`, no page errors, and ADP transcript truth for success `runtime-turn-1` and failure `runtime-turn-1-r2`. The separate freeform browser prompt `webui closeout browser proof 1783101772921` stayed `waiting_model` and is not completion evidence.
- 2026-07-04: WebUI lifecycle color and restart-continuation closeout is verified on fixed `127.0.0.1:4041`. Inactive tool precursor cards must project from protocol tool status: completed tool rounds are success cards with green left border, failed tool rounds are failed cards with red left border, and running cards use blue left border/animation. Runtime live bootstrap must compute `next_turn_ordinal` from the maximum persisted `runtime-turn-N` ordinal across all sessions, including WebUI-created non-default sessions; using only the default runtime session can reuse turn ids after daemon restart and overwrite visible history. Verified by `live_restore_resumes_turn_ordinal_from_selected_non_default_session`, full `make ci`, `scripts/install-global.sh`, fixed-port restart/health, and clean WebUI session `webui-session-20260704025459-e136d862` evidence under `artifacts/webui-online/20260704-full-fix-e2e-1783133697/` with final ADP `turn_ids=runtime-turn-7,runtime-turn-8,runtime-turn-9,runtime-turn-9-r2,runtime-turn-10,runtime-turn-10-r2,runtime-turn-11`, `turns=7`, session `success`, `pendingCards=0`, and no page errors.
# 2026-07-04 WebUI duplicate/sample-button regression

- Verified `app.webui-smoke` follow-up: persistent `Success` / `Failure` composer buttons are not part of the WebUI surface; diagnostics remain hidden slash/shortcut affordances.
- WebUI transcript render must filter adjacent assistant bubbles by normalized visible text in addition to turn-id keyed de-duplication, because selected-session transcript and latest-turn refresh can transiently present equivalent assistant/final fragments under different turn ids.
- Online proof for this slice is `artifacts/webui-online/20260704-duplicate-buttons-followup-1783144225304/summary.json` on symlink profile `127.0.0.1:4042`.

# 2026-07-04 agent-framework control principle

- Freehand framework control must be passive and schema/tool-driven: framework defines status fields, prompt guidance, hard status tags, validation/retry policy, built-in action tools, and state executors; the model returns explicit status schema and calls explicit tools; runtime starts/stops/switches tasks only from accepted status/action truth, not from guessed user-request semantics.
- Cross-workspace task/subagent routing should therefore use a no-side-effect invisible status block such as `<<<freehand_status>>>...<<<\/freehand_status>>>` plus a compact built-in `task` action tool for mutation. Public UI strips status blocks, and schema repair/retry handles incomplete or malformed status feedback. Compatibility normalization may repair format shape but must not invent missing task intent.
- Task/subagent control must be centralized metadata/control truth, not data payload forwarding. Parsed status signals and task tool actions write first through `metadata.core` / control center with watermark provenance: writer owner, pipeline node, schema version, status/action, source model/agent/session/turn, timestamp, validation/retry/error trace. Runtime/task/node transitions consume this control-center truth and keep user/request/task data chains separate.
- The next Freehand refactor requires explicit planned owners `control.center`, `error.center`, and `task.orchestration`. Control center handles passive status admission and action-tool admission; error center owns classification/retry/repair/stop/fail/block/cancel decisions; both must write metadata-watermarked records before runtime/reason/task/node state transitions. Design truth: `docs/design/control-error-center-refactor.md`.
- Corrected control model: model feedback has two channels. Status schema is no-side-effect interaction state for reasoning rhythm (`simple_request`, `task_complete`+`evidence`, `next_step`, `blocked`, `needs_user_involvement`+`options`, learned/needs_record). Side effects must use compact built-in action tools, preferably one `task` tool with operation arguments and no more than three framework tools total. Status schema must never execute task mutations.
- Refactor starts with four fixed hook points, not a full-flow hook chain: after local tool result for precise request/tool-result checks; before model request for outbound control; after model response for raw response parsing; before client return for final projection/client-facing processing. Schema variants and action processors may evolve only inside these hook points, not by bypassing them.
- 2026-07-04: `control.center` first skeleton landed in `crates/freehand-control`. It parses hidden `<<<freehand_status>>>` schema v1, validates no-side-effect interaction status, derives basic rhythm decisions, writes watermarked hook metadata through runtime for `ControlHook01AfterLocalToolResult`, `ControlHook02BeforeModelRequest`, `ControlHook03AfterModelResponse`, and `ControlHook04BeforeClientReturn`, and strips hidden status blocks from UI public projection. Basic stopHook allows `simple_request=true` to close without legacy `<freehand_completion>` on terminal-candidate provider response. Verified with `cargo test -p freehand-control`, `cargo test -p freehand-runtime live_bridge_accepts_simple_status_stop_hook_without_completion_schema -- --nocapture`, and `cargo test -p freehand-ui-protocol public_conversation_strips_hidden_control_status_blocks -- --nocapture`. Pending: task action tool, task lifecycle persistence/dispatch, error.center, full status repair loop, selectable options UI.
- 2026-07-04: Task truth is now observable through runtime-backed ADP query, not only through provider tool results. `ui.protocol` defines `QueryTaskList` / `QueryTaskHistory`, task UI DTOs, and `UiRuntimeQueryPort`; `runtime.ui-command-dispatch` implements `RuntimeCommandDispatcher::query_runtime` by calling `TaskRuntime::list_tasks` and `TaskRuntime::task_history`; `freehand-server` ADP query asks the runtime query port before protocol-state queries while staying protocol-only; `freehand-cli adp-task-query` provides no-UI verification. Verified with runtime white-box task query, daemon ADP task list/history/missing-target test, full workspace tests, clippy, gates, and live S daemon on `127.0.0.1:4042` where `freehand-cliS adp-task-query --status waiting_agent` returned task list truth and missing history returned `command_dispatch_target_not_found`. Pending: task truth push subscription, WebUI task management panel, real worker process/channel.
- 2026-07-04: Task list truth is now observable through runtime-backed ADP subscription as well as query. `ui.protocol` owns `SubscribeTaskList`, `UiStreamKind::TaskList`, and `UiProjection::TaskList`; `freehand-server` uses the injected `UiRuntimeQueryPort` for task subscription initial snapshots; runtime publishes `UiProtocolState::publish_task_list_projection` after successful task tool mutation ops; `freehand-cli adp-task-subscribe` provides no-UI live verification. Verified with protocol subscription matcher test, runtime task-tool push hook test, daemon ADP task subscribe black-box test, full workspace tests, clippy, mainline/gates, and live S daemon on `127.0.0.1:4042` where `freehand-cliS adp-task-subscribe --status waiting_agent` returned `accepted=true`. Pending: WebUI task management panel, real worker process/channel, task history/worker debug push.
- 2026-07-04: Freehand loop governance is initialized under `docs/loops/freehand-framework-loop` as `L1 report-only`, manual trigger only. Required loop docs are `LOOP.md`, `STATE.md`, `loop-constraints.md`, `loop-budget.md`, `loop-run-log.md`, and `README.md`; L2/L3 are disabled until explicit approval. Bound to `foundation.workspace` and verified with xtask tests, fmt, mainlines generate/check, and gates. Important gate lesson: docs-only loop files must not be added as bound function-map call-table symbols because source-symbol gate will reject them; represent docs-only loop truth in mainline prose unless a real code symbol is added.
- 2026-07-04: `task.orchestration` first skeleton landed in `crates/freehand-task`. Task truth uses append-only task ledgers plus rebuildable snapshots under `~/.freehand/ledgers/tasks/<agent>/<task_id>.jsonl` and `~/.freehand/state/tasks/<agent>/<task_id>.json`; runtime memory state is rebuilt by `TaskRuntime::boot`. The single built-in `task` tool is registered with `op=create|query|list_agents|query_agent` and runtime routes it to the task owner. First slice supports required task fields, self-agent registry, create with self/auto assignment or WaitingAgent, query, and list/query agent. Verified with `cargo test -p freehand-task`, `cargo test -p freehand-tools`, `cargo test -p freehand-runtime task_tool_create_persists_and_queries_task -- --nocapture`, `cargo test --workspace`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo run -p xtask -- mainlines check`, and `cargo run -p xtask -- gates check`. Pending: real worker execution, leases/heartbeat recovery, review actions, UI task projection, error.center integration.
- 2026-07-04: `task.orchestration` lifecycle advanced beyond create/query. The `task` tool now includes `append`, `pause`, `resume`, `submit_review`, `approve`, `reject`, and `close`; `TaskRuntime` validates transitions before ledger/snapshot writes. Close before review approval is rejected, and review reject/resume/resubmit/approve/close is persisted and recovered. `docs/mainline-calls/task.orchestration.json` and generated `docs/wiki/task.orchestration.md` are now part of the mainline manifest/gate. Verified with `cargo test -p freehand-task`, runtime task lifecycle tests, `cargo test --workspace`, clippy, mainlines generate/check, and gates. Pending remains worker execution, leases/heartbeat recovery, UI projection, and multi-agent dispatch.
- 2026-07-04: `task.orchestration` Running state is now lease-backed. `resume_task` writes an active lease under `~/.freehand/state/task-runtime/<agent_id>/leases.json`; `task(op="heartbeat")` refreshes the assigned running task lease; leaving Running removes the lease; and `TaskRuntime::boot` interrupts Running tasks whose lease is missing, mismatched, inactive, or expired. Verified with `cargo test -p freehand-task` (positive lease/heartbeat plus negative non-running heartbeat and expired-lease recovery), runtime heartbeat task-tool black-box test, `cargo test -p freehand-tools`, `cargo run -p xtask -- mainlines generate/check`, `cargo run -p xtask -- gates check`, `cargo test --workspace`, and clippy. Pending remains real worker execution, UI task projection, and multi-agent dispatch.
- 2026-07-04: `task.orchestration` agent registry lifecycle skeleton now persists all agent snapshots and supports `task` ops `create_agent`, `close_agent`, `assign`, and `cancel`. `TaskRuntime::boot` loads all persisted agents; `assign_task` binds waiting/created/interrupted tasks only to available agents; assigned work counts as queued until resume/heartbeat; cancel/review/terminal states release assignee state; and busy agents cannot be closed. Verified with `cargo test -p freehand-task` (12 passed), runtime task-tool agent lifecycle black-box test, existing task runtime tests, `cargo test -p freehand-tools`, `cargo test -p xtask`, mainlines generate/check, gates, `cargo test --workspace` (403 passed), and clippy. Pending remains real worker execution/channel dispatch and UI task projection.
- 2026-07-04: `task.orchestration` now has a priority claim skeleton. `task(op="claim_next")` lets an agent claim its highest-priority assigned task into lease-backed `Running`; an agent may hold multiple queued assigned tasks; empty queue claim returns no-task without mutating truth. Verified with `cargo test -p freehand-task` (14 passed), runtime task-tool claim-next black-box test, existing task runtime tests, `cargo test -p freehand-tools`, `cargo test -p xtask`, mainlines generate/check, gates, `cargo test --workspace` (406 passed), and clippy. Pending remains real worker process/channel execution, UI task projection, and worker debug/turn streaming.
- 2026-07-04: `task.orchestration` now records worker execution progress without real worker process/channel execution. `task(op="record_execution")` writes `TaskExecutionRecorded` ledger events only for `Running` tasks, keeps the task running, updates `last_progress_at`, and rejects non-running tasks without sequence advance. Verified with `cargo test -p freehand-task` (16 passed), runtime task-tool record-execution black-box test, existing claim/heartbeat runtime tests, `cargo test -p freehand-tools`, `cargo test -p xtask`, mainlines generate/check, gates, `cargo test --workspace` (409 passed), and clippy. Pending remains real worker process/channel execution, UI task projection, and worker debug/turn streaming.
- 2026-07-04: `task.orchestration` task timelines are now queryable through `task(op="history")`. `TaskRuntime::task_history` reads the append-only task ledger and returns ordered lifecycle events; unknown task history returns explicit `TaskNotFound`. Verified with `cargo test -p freehand-task` (18 passed), runtime task-tool history black-box test, existing record/claim runtime tests, `cargo test -p freehand-tools`, `cargo test -p xtask`, mainlines generate/check, gates, `cargo test --workspace` (412 passed), and clippy. Pending remains UI task projection and worker debug/turn streaming consuming this history truth.
- 2026-07-04: `task.orchestration` queue/UI projection can query task snapshots through `task(op="list_tasks")`. `TaskRuntime::list_tasks` filters by status and assignee, returns priority-sorted snapshots, and is read-only. Verified with `cargo test -p freehand-task` (19 passed), runtime task-tool list_tasks black-box test, existing history/record runtime tests, `cargo test -p freehand-tools`, `cargo test -p xtask`, mainlines generate/check, gates, `cargo test --workspace` (414 passed), and clippy. Pending remains UI task projection and worker debug/turn streaming consuming this list/history truth.
- 2026-07-04: `error.center` first skeleton landed in `crates/freehand-control`. It owns `ErrorCenterObservedFailure`, `ErrorCenterDecision`, and `classify_error_center_failure` for schema/tool/provider errors. Runtime writes watermarked `error.center` metadata before completion-schema repair, failed-tool re-entry, and provider executor terminal failure materialization. Error-center rows carry domain/class/code/source/recovery/retry/visibility/owner-target/repair-fields/raw-hash and do not store raw error text. Verified with freehand-control tests, runtime schema/tool/provider error-center metadata tests, `cargo test -p freehand-runtime`, xtask tests, fmt, mainlines generate/check, gates, workspace tests, and clippy. Pending: task/node/UI error-center routing, ADP metadata query/subscribe, WebUI error cards, full status repair loop, selectable options UI.
- 2026-07-04: `error.center` metadata is now observable through runtime-backed ADP query and initial subscription projection. `ui.protocol` owns `QueryErrorCenterEvents` / `SubscribeErrorCenterEvents` plus UI-safe event DTOs; `runtime.ui-command-dispatch` reads watermarked metadata rows via `query_error_center_events_for_ui`; server/daemon ADP routes expose the read surface; `freehand-cliS adp-error-query` verifies it without WebUI. Online S-profile proof on `127.0.0.1:4042`: failure sample session `cli-adp-sample-failure-1783177452885366000` produced `runtime-turn-37-r2`, `rounds=2`, `tool_executions=1`, `failed_tools=1`; `adp-error-query` returned `count=1` with `tool:validation:repair_schema:tool_result_failed:451bc61e1a05e812`; `--domain tool` returned `count=1`, `--domain provider` returned `count=0`; raw ADP subscribe returned accepted plus the same initial event. Raw provider/tool/request text is not projected, only watermarked fields and `raw_hash`. Pending: live push on new error-center writes and WebUI error-center cards.
- 2026-07-04: S-profile `restartS` must refresh the launchd debug daemon binary copy before kickstart. Because launchd executes copied `~/.local/bin/freehand-daemonS-bin` rather than the `freehand-daemonS` symlink, plain kickstart can leave daemon behavior stale while CLI symlinks are current. `scripts/install-launchd.sh restartS` now runs `scripts/install-symlink.sh` first, then restarts only `com.freehand.daemonS`. Verified with `scripts/install-launchd.sh restartS`, health `ok`, `freehand-cliS adp-smoke`, and persisted error-center query still returning `count=1` on `127.0.0.1:4042`.
- 2026-07-05: Completion schema mismatch is model response polishing, not system schema repair and not provider failure. UI/user-visible lifecycle text is `schema polishing #N`; internal `repair_schema` remains an error-center recovery-action string for compatibility. Runtime tests lock that schema/no-schema mismatch writes schema-domain polishing metadata and never provider-domain/`fail_turn` truth. Provider/network executor errors are separate: recoverable non-stream Anthropic errors retry exactly five attempts with production backoff `1s,2s,4s,8s,16s`, then fail with concrete code such as `anthropic_http_status_500`. Verified with runtime/control/UI/server tests, `xtask mainlines/gates`, S-profile `127.0.0.1:4042` WebUI proof `artifacts/webui-online/20260705-verify-4042-1783252779663/summary.json`, and isolated temp-HOME CLI live provider fixture `artifacts/provider-retry/20260705-provider-retry-1783252973529124000/summary.json` observing five `/v1/messages` attempts and `anthropic_http_status_500`.
- 2026-07-06: WebUI responsive/mobile layout closeout is verified only by real browser online evidence, not pure classifier or static CSS checks. The current accepted S-profile proof is `artifacts/webui-online/20260705-verify-4042-1783269156414/summary.json`: success + failed-tool continuation samples completed in session `webui-session-20260705163237-e71a1a3e`, ADP reported `turn_ids=runtime-turn-80,runtime-turn-81,runtime-turn-81-r2`, stale historical live count was `0`, and the viewport matrix proved layout shape, composer visibility, and message-list visibility for phone portrait, tall phone, phone landscape, tablet portrait, foldable/tablet landscape, and desktop large. WebUI scroll-to-bottom must target the conversation stream, not global `documentElement.scrollHeight`, because sidebar/inspector height can otherwise scroll the composer out of view.
- 2026-07-06: Android daemon connection config is app-owned JSON truth bootstrapped from bundled `assets/config/client.json`; SharedPreferences host/port persistence is dead. Config load/validation errors must be surfaced as `daemon config error` with input disabled and must not runtime-fallback to default Tailscale. `defaultTailscale()` is allowed only as an editable drawer seed for user repair before writing a new explicit config. JVM tests and APK build passed, APK installed on serial `100.104.163.65:5555`, but true Android UI validation remains blocked until the Oplus device is manually unlocked; lockscreen/Dozing screenshots are blocker evidence, not acceptance evidence.
- 2026-07-06: Freehand implementation/debug search is source-only. Use `scripts/source-search.sh <pattern>` after owner routing; it searches source code, tests, maintained scripts, and canonical docs/function maps/test designs/mainline JSON, while excluding generated/runtime output, generated wiki, build products, MemoryPalace corpora, and project memory workpads. `.ignore` keeps default `rg` away from generated output, and `xtask gates check` locks `.ignore`, the wrapper, debug docs, and local skill snippets through `verify_source_search_policy`. Generated artifacts may be opened only as direct verification evidence after identifying the producer command, not as implementation-search roots.
- 2026-07-06: Source-only search is now hardened against bypass. `scripts/source-search.sh` rejects unsafe `rg` ignore-bypass options such as `--no-ignore`, `--unrestricted`, and `-u`; hard generated-output exclude globs are applied after caller args so user include globs cannot re-include artifacts. `xtask` has positive and negative source-search policy tests, including a missing unsafe-argument guard red test. Verified with source-search artifact/no-ignore/-u negative runs, `cargo test -p xtask`, `mainlines check`, and `gates check`.
- 2026-07-06: Android true-device UI validation now has a deterministic script at `apps/freehand-android/scripts/verify-device-ui.sh`. It requires an explicit ADB serial, captures install/start/activity/window/logcat/screenshot/summary evidence, and returns `passed`, `blocked`, or `failed` without broad-kill, silent unlock, or endpoint switching. Offline/unavailable ADB, lockscreen/dozing, and Freehand-not-foreground are blocker evidence only, not acceptance evidence. Current run with serial `100.104.163.65:5555` is blocked as `adb_state_unavailable`; true Android UI/WebView closure still requires a connected/unlocked device and a passing script run.
- 2026-07-06: Android verifier classification must distinguish app failure from device blocker. `apps/freehand-android/scripts/verify-device-ui.sh` preflights the configured launcher activity class in the APK via `apkanalyzer` when available, and after launch checks Freehand fatal/exception logcat before lockscreen/not-foreground blocker rules. Missing launcher activity class or app fatal logcat is `failed`; unavailable ADB, locked/dozing device, or no foreground Freehand without fatal evidence is `blocked`. A dirty/incremental APK once missed `com.freehand.android.ui.MainActivity` while source and compiled classes existed; `./gradlew clean assembleDebug` rebuilt a correct APK, so APK dex evidence is the runtime truth, not source presence alone.
- 2026-07-06: Mobile WebUI portrait/tall-phone/tablet-portrait acceptance requires conversation-first layout with session and detail surfaces as overlay drawers. Current S-profile online proof is `artifacts/webui-online/20260706-verify-4042-1783303750890/summary.json`; it locks composer clearing, prompt persistence after refresh, no stale historical live animation, viewport matrix coverage, mobile conversation primary, session/detail drawer open, and drawer close. Release daemon proof is fixed Tailscale `100.66.1.82:4041` with health `ok` and ADP smoke passing. Android app-owned config on serial `100.104.163.65:5555` is correct for `tailscale-main` `100.66.1.82:4041`, but true Android WebView acceptance is blocked while secure keyguard shows `NotificationShade`; blocker evidence is `artifacts/android-device/20260706T021041Z-webui-mobile-release-4041/summary.json` and screenshot. This is not Android UI acceptance until the device is manually unlocked and `verify-device-ui.sh` passes with Freehand foreground screenshot.
- 2026-07-06: S-profile launchd restart must health-check the env-backed bind, not a recomputed default. `scripts/install-launchd.sh restartS` now resolves bind precedence as explicit `FREEHAND_DAEMON_BIND` -> existing `~/.freehand/daemonS.env` `FREEHAND_DAEMON_BIND` -> S fixed default `127.0.0.1:4042`; Tailscale default detection must not move S off loopback. `xtask` locks this with CI/CD gate snippets plus a negative fixture. Verified by `scripts/install-launchd.sh restartS`, `curl -4fsS http://127.0.0.1:4042/health`, `freehand-cliS adp-smoke --url ws://127.0.0.1:4042/adp`, `cargo test -p xtask ci_cd`, mainline/gates, and online WebUI proof.
- 2026-07-06: WebUI online verification must follow the current `/new` dialog flow. After New Conversation began opening `new-session-dialog`, direct button-click automation sent prompts into a stale localStorage-selected archived session and created false refresh-history failures. `scripts/webui_verify_online.mjs` now waits for the dialog, confirms conversation mode, waits for the new draft session, then verifies both success and failed-tool prompts survive reload. Current accepted proof: `artifacts/webui-online/20260706-verify-4042-1783310624927/summary.json`, session `webui-session-20260706040346-0c5a9c53`, ADP `turn_ids=runtime-turn-98,runtime-turn-99,runtime-turn-99-r2`, all WebUI checks true.
- 2026-07-06: Android true-device WebView acceptance for the mobile closeout is now verified on serial `100.104.163.65:5555`. Release/Tailscale daemon endpoint `http://100.66.1.82:4041/health` returned `ok`, `freehand-cli adp-smoke --url ws://100.66.1.82:4041/adp` passed, and `apps/freehand-android/scripts/verify-device-ui.sh 100.104.163.65:5555` passed with `status=passed`, reason `freehand_activity_foreground_no_fatal_logcat`, package `com.freehand.android`, activity `.ui.MainActivity`. Evidence: `artifacts/android-device/20260706T040938Z-100.104.163.65_5555-64346/summary.json` and `screenshot.png`. Screenshot shows daemon-hosted Freehand WebUI mobile layout, not the old native/fallback desktop-style UI.
- 2026-07-06: Android WebView consumes the release/Tailscale daemon (`100.66.1.82:4041`), not the S-profile daemon (`127.0.0.1:4042`). After WebUI/mobile changes, Android screenshots can remain stale until `scripts/install-global.sh` and `scripts/install-launchd.sh restart` update the release daemon. Before claiming phone UI is updated, compare `apps/freehand-server/assets/webui.{css,js}` SHA-256 with `http://100.66.1.82:4041/assets/webui.{css,js}` and then rerun `apps/freehand-android/scripts/verify-device-ui.sh <serial>`. Verified updated release assets and Android screenshot at `artifacts/android-device/20260706T042507Z-100.104.163.65_5555-11074/screenshot.png`; logcat layout probe reported `shape=tall_phone`, `conversationPrimary=true`, and session/detail drawers offscreen.
- 2026-07-06: WebUI `/new` clean-state release verification must clear persisted sessions first and must not depend on secure-context-only browser APIs. The release/Tailscale surface is `http://100.66.1.82:4041`, where direct `crypto.randomUUID` can be unavailable; draft/session request ids must use `browserRandomId()` with `randomUUID` / `getRandomValues` / local suffix fallback. `scripts/webui_verify_online.mjs` now deletes existing sessions through ADP before creating a new conversation, then asserts `selectedTurn=-`, `messageCount=0`, clean empty-state text, and no old success/failure/tool/waiting-data leakage before submitting samples. Verified on release 4041 with `artifacts/webui-online/20260706-verify-4041-1783329891362/summary.json`: old session removed, ADP cleanup reported `sessions=0`, new draft `webui-session-20260706092451-c6f51f63` was clean, and the same run completed success plus failed-tool continuation with `turn_ids=runtime-turn-112,runtime-turn-113,runtime-turn-113-r2`.
- 2026-07-06: WebUI clean-session rendering must not use latest-active turn as a fallback after session-list truth has loaded. Root cause: `DeleteSession` is non-destructive metadata removal, so ADP can report `sessions=0` while `QueryLatestActiveTurn` still returns an old turn. WebUI now gates latest-turn query results, ADP latest-turn subscription events, latest-turn SSE events, and `SessionTurns` projections through one session truth rule: accept only listed sessions, the current draft session, or the current pending-submit session. Rejected stale projections clear local render truth instead of reselecting old sessions. Verified on release 4041 with browser/ADP evidence `artifacts/webui-online/20260706-verify-4041-1783332261674/summary.json`: old latest turn `runtime-turn-118` existed, all sessions were removed through ADP to `sessions=0`, new draft `webui-session-20260706100422-93a7a172` showed `selectedTurn=-`, `messageCount=0`, clean empty text, and no old content leakage; the same run completed success plus failed-tool continuation with `turn_ids=runtime-turn-119,runtime-turn-120,runtime-turn-120-r2`.
- 2026-07-06: ADP is internal transport terminology and must be transparent to WebUI/Android users. User-facing labels, status rows, failure cards, command-send errors, and diagnostic prompts must say connection/service/request/conversation, not ADP. WebUI now contains no uppercase `ADP` in `assets/webui.js`, and the online verifier submits `Online success/failure sample` so screenshots do not leak protocol terms. Android failure projection renders `Connection` / `connection failure`, and command-send errors say `service connection is not ready` / `request send failed`. Verified on release 4041 with `artifacts/webui-online/20260706-verify-4041-1783348272047/summary.json` where all checks passed and `messageTextContainsADP=false`; Android true-device verifier passed at `artifacts/android-device/20260706T143159Z-100.104.163.65_5555-81364/summary.json`.

- 2026-07-07: WebUI Settings now consumes an owner-backed read-only config projection instead of placeholder/browser-local config truth. `config.core` preserves `ProviderAuthSourceKind` (`inline`/`env`) on selected provider config while keeping resolved API keys runtime-only. `ui.protocol` owns `QueryConfigStatus` and `UiConfigStatusProjection`, rejects local protocol-state handling, and rejects command-ingress misuse. `runtime.ui-command-dispatch` projects active agent/provider/model status from selected live config, exposing provider host only and auth source type only. WebUI renders `master` / `minimax` / `api.minimaxi.com` / `MiniMax-M3` from the runtime query path and shows user-facing auth as `credential · inline`, with no API-key/secret text. Online S-profile proof: `artifacts/webui-online/20260706-verify-4042-1783376428677/summary.json`; headless proof: `freehand-cliS adp-config-query --url ws://127.0.0.1:4042/adp` returned `auth_source=inline` and no key material. Provider/model editing remains locked for a later owner-backed config write batch.

- 2026-07-07: WebUI Settings provider/model editing is now owner-backed and restart-only. The path is `app.webui-smoke` -> `ui.protocol` -> `runtime.ui-command-dispatch` -> `config.core`; WebUI does not parse/write `~/.freehand/config.toml`. `config.core` owns `ProviderConfigUpdate` and `update_provider_config_in_path`, validates provider id/type/protocol/base URL/default model/env-var auth, persists through temp-file rename, and rejects invalid updates without overwriting config. `ui.protocol` owns `UiCommand::UpdateProviderConfig` and serializes only `api_key_env`, never raw credential values. `runtime.ui-command-dispatch` returns `provider_config_saved_restart_required` and keeps the active runtime provider/model unchanged until daemon restart. Online S-profile proof: `artifacts/webui-online/20260707-verify-4042-1783388440427/summary.json` with invalid update visible, valid update restart-required, Settings credential scan clean, multi-round failed-tool continuation terminal, and mobile Settings drawer passing. After validation, real `~/.freehand/config.toml` and `~/.freehand/daemonS.env` were restored from backup and `freehand-cliS adp-config-query --url ws://127.0.0.1:4042/adp` returned `auth_source=inline`. Gate proof: config/ui-protocol/runtime/server/cli/xtask targeted tests, JS syntax checks, fmt, diff check, mainlines generate/check, and gates check all passed.

- 2026-07-07: Release launchd restart must refresh env/plist before kickstart, not only service-scope restart. Root cause from WebUI Settings release proof: S-profile `restartS` already rewrote env/plist, but release `scripts/install-launchd.sh restart` only called `restart_launchd`, so verification env changes did not enter the release daemon and owner-backed provider config update validation failed on 4041. `restart` now calls `write_launchd_env` and `write_launchd_plist` before `restart_launchd`. Release 4041 proof after fix: `artifacts/webui-online/20260707-verify-4041-1783390967374/summary.json`; served JS hash `61e92a96c82ce0d4456fcce7896b03b6ae93f69f1b17ae6d7e713b84d77c9f1f` and CSS hash `1ab81aa107b1ba52da0721d162d3f120fd519b81b2315b609c9cf1c719c83b03` match workspace; post-validation config query returned `auth_source=inline`. Android true-device proof for this batch is not closed when `adb devices -l` has no connected device.

- 2026-07-07: OpenMinis screenshots are functional IA references, not Freehand visual style sources. Useful future owner-backed surfaces are provider/model groups, token usage, appearance, skills, persona/SOUL.md, memory, MCP integrations, environment variables, storage/shared folders/mount external folder, permissions, background/notifications, logs, about/privacy/feedback. Do not implement editable UI for these before a unique owner, protocol projection/command, persistence truth, and online verifier exist.

- 2026-07-07: Do not map OpenMinis phone-local agent storage/mount semantics directly onto Freehand. Minis runs the agent on the phone; Freehand phone is a remote UI while execution and filesystem authority live on the computer daemon. Storage/shared-folder/mount UX, if needed, must be designed as computer-daemon workspace/files projection or permission/status management, not as phone-local task mounts.

- 2026-07-07: WebUI provider/model config editing is owner-backed and verified, but UI must map runtime dispatch receipts to user-facing config language without fallbacking unknown statuses into success. Audit found `provider_config_saved_restart_required -> config.core` leaking into the command status after a successful Settings save; WebUI now renders `Provider config saved. Restart required.`, throws `Config save returned an unexpected service status.` for unexpected provider-save receipts, and the online verifier rejects those internal tokens for the valid provider-update path. Evidence: `artifacts/webui-online/20260707-verify-4042-1783399680000/summary.json`; post-validation S-profile config restored to `auth_source=inline`.

- 2026-07-07: Batch 3 provider/model edit completion audit also requires restart-after-save projection proof, not only restart-required UI text. S-profile proof temporarily saved `default_model=MiniMax-M3-Restart-Proof` with env-var auth, restarted `com.freehand.daemonS`, and `freehand-cliS adp-config-query` still returned the updated model/auth source after restart. Cleanup restored real config/env and the final query returned `default_model=MiniMax-M3` / `auth_source=inline`.

- 2026-07-07: WebUI Final/Summary renderer must preserve the actual terminal `Summary` source format. Do not split one-line summaries by punctuation, domain words, or inferred business structure. Extract the complete `Summary` block until `Evidence`, `Learned`, or `Completion reason`; then render one block per actual source line, parsing only line-start labels/numbering/parenthesized labels that already exist. Verified on S-profile `127.0.0.1:4042` with `artifacts/webui-online/summary-format-1783430851153/summary.json`: plain ADP summary had one source line and DOM rendered one `.final-summary-item`; structured ADP summary had three source lines and DOM rendered three matching `.final-summary-item` blocks; debug fields stayed hidden.

- 2026-07-07: Phone/WebUI mobile cards must not spend horizontal space on left borders or inset-left shadows. `app.webui-smoke` now uses whole-card color-block backgrounds for mobile assistant/tool states and removes mobile `.final-summary-item` left border/padding. Focused mobile composer must reserve compact real height, not viewport-height placeholders: the locked focused padding is `calc(112px + env(safe-area-inset-bottom))`, composer card max height is `132px`, and the old `min(46svh, 330px)` path is rejected by asset smoke. Online S-profile proof `artifacts/webui-online/20260707-verify-4042-1783433140002/summary.json` passed `mobileNoLeftEdgeIndicators`, `mobileFocusedComposerCompact`, and `mobileFocusedNoLeftEdgeIndicators`; focused screenshot `26-mobile-focused-composer.png` measured composer card height `92`, input height `76`, padding `112px`, assistant/tool/final `borderLeftWidth=0px`, `boxShadow=none`, and final summary `paddingLeft=0px`. Release/phone proof: `scripts/install-global.sh` passed, release 4041 served matching workspace JS/CSS, ADP smoke passed, and Android device `100.104.163.65:5555` passed `verify-device-ui.sh` at `artifacts/android-device/20260707T142056Z-100.104.163.65_5555-51679/summary.json`.
- 2026-07-10: Master workspace authority is runtime-home-only. The live Master dispatcher accepts sessions with external CWD for task routing, but Master workspace tools execute under canonical `~/.freehand`; an external workspace or absolute target returns a paired failed tool result with `allowed_root`, requested target, and explicit `task(create_agent/create/assign)` guidance. Unsandboxed shell tools are omitted from the Master provider tool schema and denied if received; `task` remains a framework-scoped exception so external `target_cwd` work can be delegated. Checkpoint roots and launchd default workdir also use runtime home. Verified by `make ci` and S-profile session `master-boundary-live-1783650252`: external `AGENTS.md` read was blocked without content leakage, then Task Center recorded `task-1783650293` at `/Volumes/extension/code/freehand` with `TaskCreated,TaskAssigned,TaskResumed,TaskHeartbeat`. This proves boundary and delegation truth, not production Worker completion.
- 2026-07-10: Task terminal truth is authoritative across stale `TaskRuntime` instances. Before heartbeat/result writes, `TaskRuntime` reloads the persisted task snapshot (and leases for heartbeat) so an externally cancelled task cannot be overwritten by a Worker runtime that opened before cancellation. Verified locally by `cargo test -p freehand-task -- --nocapture` (46 passed) and `cargo test -p freehand-runtime production_worker_runner -- --nocapture` (9 passed). Verified online on S-profile `127.0.0.1:4042` with task `task-cancel-running-1783682850768`: event order ended `TaskCreated,TaskAssigned,TaskResumed,TaskHeartbeat,TaskCancelled`; no events appeared after cancellation; worker log rejected stale blocked write with `invalid task transition from `Cancelled` using `TaskBlocked`; worker service remained running and available.
- 2026-07-10: The current S-profile one-Master/one-Worker topology can complete manually started lifecycle tasks through several real online branches. Happy path task `task-manual-happy-1783683798565` reached `TaskReviewSubmitted,TaskReviewApproved,TaskClosed` and wrote `/tmp/manual-happy-1783683798565/result.md`. Persisted bad-review retry task `task-manual-reject-direct-1783684045392` reached `TaskReviewRejected`, was reassigned to the same Worker with new execution `exec-worker-worker-1783684063797699000-1019`, then closed with final result `FH-MANUAL-REJECT-DIRECT-1783684045392`. Missing-cwd blocked task `task-manual-blocked-1783684186916` reached `TaskBlocked,TaskProgressed` with a Master `blocked_decision` note instead of silent retry. Worker interruption task `task-manual-interrupt-1783684433320` was interrupted by service-scoped `launchctl kill TERM gui/$(id -u)/com.freehand.workerS`, recorded `TaskInterrupted` with `missing_or_expired_lease`, resumed under new execution `exec-worker-worker-1783684523540461000-22`, and closed. Remaining manual-use gap is primarily the WebUI/control surface and strict deterministic restart-recovery proof, not the basic lifecycle engine for these branches.
- 2026-07-10: Strict Master pending-review restart recovery is proven on S-profile. `freehand-cliS task-restart-seed-review` seeds review-ready Task Center truth through `TaskRuntime` API while daemonS is offline; it does not use ADP, approve/close, or write task JSON directly. Online proof stopped daemonS with service-scoped `launchctl bootout gui/$(id -u)/com.freehand.daemonS`, verified `127.0.0.1:4042/health` failed, loaded `~/.freehand/daemonS.env`, seeded `task-strict-restart-1783686325` with execution `exec-strict-restart-1783686325` and event order `TaskCreated,TaskWaitingAgent,TaskAssigned,TaskResumed,TaskHeartbeat,TaskReviewSubmitted`, restarted with `scripts/install-launchd.sh restartS`, then ADP TaskHistory reached `TaskReviewApproved,TaskClosed`. Local gate: `cargo test -p freehand-cli -- --nocapture` passed 26 tests.
- 2026-07-10: Strict rejected and blocked restart recovery are proven on S-profile. `freehand-cliS task-restart-seed-rejected` and `task-restart-seed-blocked` use `TaskRuntime` owner API while daemonS is offline; no direct JSON writes or ADP mutation. Rejected proof stopped daemonS and workerS, seeded `task-strict-rejected-1783688577` through `TaskReviewRejected`, restarted both services, then Worker resumed under new execution `exec-worker-worker-1783688747254581000-0` and TaskHistory reached `TaskReviewSubmitted,TaskReviewApproved,TaskClosed`. Blocked proof stopped daemonS, seeded `task-strict-blocked-1783689029` through `TaskBlocked`, restarted daemonS, then Master wrote `TaskProgressed` with a `blocked_decision` note. Final service state: 4042 health `ok`, workerS running, worker state `available`. Local gate: `cargo fmt --check`, `cargo test -p freehand-cli -- --nocapture` passed 26 tests.
- 2026-07-10: Background Master lifecycle runner is online-proven on S-profile. Current runtime changes add `ProductionMasterRunner`, persistent Master loop state under `~/.freehand/state/master-loop/<agent>.json`, retryable event cursor/attempt handling, daemon-side Master runner startup beside WebUI/ADP, and live-bridge target task decision boundaries. Proof marker `background-master-runner-review-proof-1781783690592`: daemonS was stopped, `task-master-bg-review-1781783690592` was seeded offline through `TaskReviewSubmitted`, daemonS was restarted, and ADP TaskHistory reached `TaskCreated,TaskWaitingAgent,TaskAssigned,TaskResumed,TaskHeartbeat,TaskReviewSubmitted,TaskReviewApproved,TaskClosed` without another user chat message. Local proof before online: `cargo test -p freehand-runtime production_master -- --nocapture` passed 11 tests, `cargo test -p freehand-runtime production_worker -- --nocapture` passed 9 tests, `cargo test -p freehand-daemon worker_mode -- --nocapture` passed 1 test, and launchd scripts passed `bash -n`.
- 2026-07-10: Background Master/Worker non-happy paths are online-proven on S-profile. Marker `background-master-worker-nonhappy-proof-1781783694885`: rejected retry task `task-bg-rejected-1781783693503` was seeded offline through `TaskReviewRejected`, then after daemonS/workerS restart reached `TaskAssigned,TaskResumed,...,TaskReviewSubmitted,TaskReviewApproved,TaskClosed`; blocked task `task-bg-blocked-1781783693672` was seeded offline through `TaskBlocked`, then after daemonS restart reached `TaskProgressed`; crash/restart task `task-bg-crash-1781783694885` was seeded as lease-backed `Running` with `--ttl-seconds 1`, then after daemonS/workerS restart reached `TaskInterrupted,TaskAssigned,TaskResumed,TaskHeartbeat,TaskReviewSubmitted,TaskReviewApproved,TaskClosed`. `freehand-cli task-restart-seed-running` now writes only owner API create/assign/claim/heartbeat truth and is used for deterministic Worker crash recovery proof. Local proof: `cargo fmt --check`, `cargo check -p freehand-cli`, `cargo test -p freehand-cli --test config_startup -- --nocapture` passed 26 tests, and `cargo test -p freehand-cli -- --nocapture` returned 26 passed.
- 2026-07-10: Normal S-profile master/worker E2E gate is online-proven as a repeatable pre-WebUI acceptance command. `scripts/verify-normal-master-worker-e2e.sh` restarts S-profile master/worker on fixed `127.0.0.1:4042`, runs SubmitUserInput-only master-worker autonomy fixture, then proves production rejected retry, blocked decision, and worker crash recovery through ADP TaskHistory. The autonomy fixture uses configured Worker id `worker` and no longer creates dynamic `worker-cli-master-autonomy-*` agents; provider fixture attempts are exactly 24 after removing `create_agent`. Task-scoped terminal truth is TaskBoard + TaskHistory + transcript tool execution; shared Worker AgentLifecycle is queryable but not a per-scenario terminal oracle because the configured Worker global state can be overwritten by other/background tasks. Online proof marker `normal-master-worker-e2e-gate-1781783700473`: final output `normal_master_worker_e2e_ok`; production branches reached `TaskReviewRejected -> ... -> TaskClosed`, `TaskBlocked -> TaskProgressed`, and `TaskInterrupted -> ... -> TaskClosed`. Post-proof S-profile config restored to `provider=minimax`, `base_url_host=api.minimaxi.com`, `default_model=MiniMax-M3`, `auth_source=inline`. Gates passed: `cargo fmt --check`, `cargo check -p freehand-cli`, `cargo test -p freehand-cli --test config_startup -- --nocapture`, `cargo test -p freehand-cli -- --nocapture`, `cargo run -p xtask -- mainlines generate/check`, `cargo run -p xtask -- gates check`, `cargo clippy --workspace --all-targets -- -D warnings`, and `git diff --check`.
- 2026-07-11: Current S-profile is ready for normal WebUI testing on `http://127.0.0.1:4042`. Headless preflight `scripts/verify-normal-master-worker-e2e.sh` passed again with autonomy `mock_attempts=24` and production branches `task-normal-rejected-1781783734686`, `task-normal-blocked-1781783734723`, and `task-normal-crash-1781783734746` reaching rejected-retry close, blocked decision, and crash recovery close respectively. Browser page proof `make verify-webui-online` passed with artifact `artifacts/webui-online/20260711-verify-4042-1783734855627/summary.json`, session `webui-session-20260711015428-f13dd4b9`, screenshot `27-phase2-projection.png`, and truth `27-phase2-truth.json`. Key WebUI checks were true: composer clears, live progress observed, terminal has no stale live rows, refresh preserves transcript, Phase2 TaskBoard/AgentBoard/EventInbox/TaskHistory/WorkerControl projections match service truth, Phase2 panel hides raw ADP/internal ids, and mobile focused composer layout remains compact. Post-proof config query stayed `provider=minimax`, `base_url_host=api.minimaxi.com`, `default_model=MiniMax-M3`, `auth_source=inline`. Remaining promotion-only gaps are release `4041` and Android true-device verification when targeting release/phone, not S-profile WebUI normal testing.
- 2026-07-11: Cwd/path authority is read/write split, not all-path hard blocking. The agent cwd/workspace root is `A`; a target path `B` is not automatically a new cwd. `read_file`, `grep`, and `ls` may inspect readable external absolute or parent paths, resolving relative paths from the current agent cwd. File-mutation tools (`write_file`, `edit_file`, `multi_edit`, `delete_range`) remain locked to the current agent cwd and must return a paired failed tool result when the target is outside cwd. The paired result must say `Write boundary denied`, must not claim the path is missing, and must instruct the agent to confirm/select the correct target workspace cwd and delegate through task/worker. Worker provider turns do not expose unrestricted `bash`; injected Worker shell calls return paired failed tool results until a real write-boundary sandbox exists. Online S-profile proof `path-policy-online-1783751556` used `/tmp/freehand-path-policy-online-1783751533-96433` as a true external path: external read returned `external-readable`, external write to `target.txt` stayed `original`, reason ledger contained `toolu_path_write` status `Failed` with `Write boundary denied`, no checkpoint `Applied` existed, and the mock provider third request had the failed result re-entered (`hasWriteBoundary=true`). Earlier proof `path-policy-write-1783751078` is invalid because it used `~/.freehand/tmp`, which is inside master runtime home.
- 2026-07-11: Timer prompt policy is locked: if the next useful Master wait exceeds 3 minutes, the model-visible guidance and `timer` schema instruct Master to call `timer(op="schedule")` instead of dead-waiting in the current turn, then continue other ready Master-side work. The persisted timer prompt must say what current truth to inspect, what waited condition to revisit, and what decision to make when fired. Verified locally by `cargo test -p freehand-tools timer_tool_schema_exposes_internal_schedule_contract -- --nocapture`, `cargo test -p freehand-runtime live_bridge_admits_long_operator_task_without_semantic_truncation -- --nocapture`, `cargo fmt --check`, `cargo run -p xtask -- mainlines generate/check`, `cargo run -p xtask -- gates check`, `git diff --check`, and `bash -n scripts/verify-timer-tool-online.sh`. Online S-profile `127.0.0.1:4042` proof: `scripts/verify-timer-tool-online.sh` passed with timer `timer-online-proof-1783775995-84734`, `mock_attempts=3`, `status=completed fired_count=1`, provider request 2 `sawToolResult=true`, provider request 3 `sawTimerWakeup=true`; `FREEHAND_TIMER_VERIFY_MODE=restart-due scripts/verify-timer-tool-online.sh` passed with timer `timer-online-proof-1783776198-96001`, persisted `status=active next_due_at=1783776221` before service-scoped restart, then completed after restart with `fired_count=1` and `sawTimerWakeup=true`. Post-proof config restored to `provider=minimax`, `base_url_host=api.minimaxi.com`, `default_model=MiniMax-M3`, `auth_source=inline`; fixture env grep returned 0 matches.
- 2026-07-11: WebUI submit command timeout/transport failure is ambiguous and must not clear the selected/draft session into an empty conversation. `app.webui-smoke` now creates a draft session before submit when none is selected, keeps `pendingUserInput`, `pendingSubmitSessionId`, `pendingSubmitError`, and draft attachments after submit failure, renders a pending user card plus "Dispatch status is unknown... Refresh service state before sending a duplicate", and preserves the draft session row. Local proof: `node --check apps/freehand-server/assets/webui.js`, `node --check scripts/webui_verify_online.mjs`, `cargo test -p freehand-server webui -- --nocapture`, `cargo fmt --check`, `cargo run -p xtask -- mainlines generate/check`, `cargo run -p xtask -- gates check`, and `git diff --check` passed. Online S-profile proof on `127.0.0.1:4042`: after `scripts/install-launchd.sh restartS`, health and `freehand-cliS adp-smoke` passed, served `webui.js` hash matched workspace `7c48fc3448ea7f80763901c10cf0784a35e152b08339142193a61f86a1ae333e`; Playwright set the browser offline and submitted from a new draft session `webui-session-20260711140007-7afeb94b`, marker `FH-WEBUI-DRAFT-OFFLINE-PENDING-1783778408982`; DOM proof had `hasMarker=true`, `hasUnknownDispatch=true`, `hasRefreshInstruction=true`, `hasDraftSession=true`, `showsNoSessionsOnly=false`, `emptyStateOnly=false`, `composerValue=""`, and `pass=true`.

- 2026-07-12: Worker tool failure-rate retest on the reused xiaozhi prompt found the Master path fixed but Worker tool quality still below the “almost all correct” bar before repair. For `worker-task-task-1783783921`, pre-repair ledger had 691 tool calls, 683 tool results, 24 failed tool results, 0 schema rejections, and 5 provider errors: 24/683 = 3.51% failed tool-result rate. Failure classes were 13 absolute-`glob` calls under `/Users/fanzhang/Documents/github/xiaozhi-esp32-2.2.4`, 8 `read_file` directory/missing/binary-file attempts, and 3 `ls` missing-path checks. `tool.registry` now owns the corrected semantics: `glob` is locked-workspace scoped, accepts relative patterns and absolute patterns only under the canonical locked workspace root, canonicalizes the non-glob prefix to avoid macOS `/var` vs `/private/var` false violations, and rejects `~`, `..`, and external absolute patterns; `ls` now reports one file entry for existing file paths; `read_file`/`glob`/`ls` schemas now guide the model away from directories, guessed/generated paths, exact-file glob checks, and binary sidecars. Local proof passed: `cargo test -p freehand-tools -- --nocapture` (37 tests), focused glob/schema/ls tests, `cargo fmt --check`, `cargo run -p xtask -- mainlines generate/check`, `cargo run -p xtask -- gates check`, and `git diff --check`. Online S-profile proof after `restartS` + `restartWorkerS`: config stayed minimax/MiniMax-M3 inline auth with no fixture env; same Worker task ledger advanced to 710 tool calls, 703 tool results, 25 failed results, 0 schema rejections, 5 provider errors; incremental repair-window delta was +19 tool calls, +20 tool results, +1 failed result, and the new failure was `read_file` on directory `main/boards`, with no new in-workspace absolute `glob` rejection observed after the new worker binary. The old xiaozhi HTML deliverable was not complete: `project_analysis.html` was still missing, task history had no review/close, and current progress was blocked by `TaskInterrupted -> TaskProgressed -> TaskAssigned` plus historical `anthropic_http_request_failed` interruptions to `https://api.minimaxi.com/anthropic/v1/messages`. Marker: `worker-tool-guidance-failure-rate-retest-1783818755`.

- 2026-07-12: Path-tool support for absolute symlink aliases is an explicit `tool.registry` contract, not only prompt guidance. Positive test `path_tools_accept_absolute_symlink_aliases_inside_locked_workspace` creates a real symlink alias to the locked workspace and proves `glob`, `grep`, `read_file`, `ls`, and `write_file` accept absolute alias paths that canonicalize back into the locked root. This locks the intended behavior for user-facing symlink paths such as `~/github/repo` resolving to canonical task cwd paths such as `/Users/fanzhang/Documents/github/repo`. Local proof: focused symlink test passed; `cargo test -p freehand-tools -- --nocapture` passed 38 tests; `cargo fmt --check`, `cargo run -p xtask -- mainlines generate/check`, `cargo run -p xtask -- gates check`, and `git diff --check` passed. Marker: `path-tools-absolute-symlink-contract-1783818755`.

- 2026-07-12: Resource-center direct source edges are now first-class manifest truth. `docs/resource-maps/core.json` has `source_edge_registry` for code-bound direct resource edges, separate from feature-local mainline rows. Each entry binds `edge_id`, `operation_id`, endpoints, mainline doc, call-table step, file path, symbol path, and status. `xtask gates check` validates registry entries against operation bindings and mainline call-table rows in both directions, and red test `resource_map_rejects_missing_source_edge_registry` proves a bound resource-operation row without registry truth fails. Pending resource operations must not fake source edges.

- 2026-07-12: Resource operation bindings are not implicit direct relation permission. Every `bound` operation source/target pair in `docs/resource-maps/core.json` must also have an `allowed_direct=true` `relation_rules` entry. Direct relation rules must keep `via_resources` empty; indirect rules must declare required `via_resources`. `xtask gates check` validates this and red test `resource_map_rejects_missing_direct_relation_rule` proves a bound operation pair without a direct relation rule fails.

- 2026-07-12: Resource-level allowed operations are now authoritative for resource operation bindings. `operation_id` must use `<source_resource>.<operation>` format, the prefix must match `source_resource`, and the operation suffix must be listed in that resource's `operations` array. `xtask gates check` validates this, rejects duplicate operation names inside a resource, and red test `resource_map_rejects_operation_not_declared_on_source_resource` proves unlisted operation bindings fail.

- 2026-07-12: Feature-map resource ownership backlinks are now explicit. `docs/architecture/feature-map.md` has `## Resource Ownership Index`, listing each owner feature and owned `resource_type` with backlink to `docs/resource-maps/core.json`. `xtask gates check` parses this section and requires every resource `owner_feature_id` in the resource map to list its resource in the feature map. Red test `resource_map_rejects_missing_feature_map_resource_backlink` proves missing owner backlinks fail.

- 2026-07-12: Feature-map resource ownership is now unique and resource-map-bound. `xtask gates check` rejects `Resource Ownership Index` rows that list unknown resources, rejects the same `resource_type` under multiple feature owners, and rejects feature-map owner/resource pairs that do not match the resource map's `owner_feature_id`. Red tests `resource_map_rejects_unknown_feature_map_resource` and `resource_map_rejects_duplicate_feature_map_resource_owner` lock those cases.

- 2026-07-12: Resource owner crate backlinks are now checked across resource map and feature map. `xtask gates check` parses feature-map seed `owner` lines and requires each resource map `owner_crate` to appear in the owning feature's seed owner text. Red test `resource_map_rejects_feature_owner_crate_mismatch` proves a resource whose `owner_crate` drifts away from the feature-map owner fails.

- 2026-07-12: The core resource map now has an explicit required resource set gate. For `resource_map_id=freehand.core-resource-map`, `xtask gates check` requires `config`, `session`, `turn`, `request_context`, `provider_request`, `provider_response`, `tool_call`, `workspace_path`, `task`, `agent`, `timer`, `error`, `metadata`, `debug_trace`, `ui_projection`, `runtime_command`, `checkpoint`, `node_pairing`, and `instruction_capability`. Red test `resource_map_rejects_missing_required_core_resource` locks the missing-core-resource failure.

- 2026-07-12: Bound Resource Operation Test Coverage rows now require command-style verification entries. `xtask gates check` rejects `bound` coverage cells that only contain prose, using red test `resource_map_rejects_bound_coverage_without_command_entry`; docs and the local Freehand skill now require white-box, module black-box, and project black-box cells to include entries such as `cargo test ...`, `cargo run -p xtask ...`, `make ...`, `scripts/...`, `node ...`, `bash ...`, `jq ...`, `grep ...`, `freehand-cli...`, or `./gradlew ...`. Current proof passed: placeholder grep over `docs/testing/*.md` returned no matches, `jq empty docs/resource-maps/core.json`, `cargo fmt --check`, `cargo check -p xtask`, `cargo test -p xtask resource_map_ -- --nocapture` (23 focused tests), `cargo test -p xtask -- --nocapture` (47 tests), `cargo run -p xtask -- mainlines generate`, `cargo run -p xtask -- mainlines check`, `cargo run -p xtask -- gates check`, and `git diff --check`. Remaining gap: the gate proves command-style entries are present, not that every command listed in every coverage cell has been individually parsed and executed.

- 2026-07-12: Resource-center top-down refactor is not yet globally complete even though the current skeleton gate stack passes. Current `docs/resource-maps/core.json` evidence is 19 required resources, 14 operation bindings, 13 bound operations, 1 pending operation (`instruction_capability.admit_request_context`), 21 relation rules, 7 forbidden direct relations, 5 broad source shortcut gates, and 2 precise source-edge gates. There are 0 deferred forbidden relation gates. Full objective residual gaps: the pending instruction-to-request-context operation is not code-bound; `xtask gates check` does not yet automatically discover every undeclared Rust source edge across the whole repo; test-design coverage rows have command-style entries but those commands are not individually parsed/executed by the gate; and business-code resource-owner refactor should not be claimed until a specific resource operation is selected and verified through its mapped tests.

- 2026-07-12: `source_edge_registry` rows are now directly code-bound by the resource map gate. `verify_resource_map` validates each registry file path exists and each registry symbol path resolves in the listed source files, using the same segment splitting as mainline call-table bindings. Red test `resource_map_rejects_source_edge_registry_missing_symbol` proves a stale registry/mainline pair cannot mutually backfill a missing source symbol. Current proof passed: `cargo test -p xtask resource_map_rejects_source_edge_registry_missing_symbol -- --nocapture`, `jq empty docs/resource-maps/core.json`, `cargo fmt --check`, `cargo check -p xtask`, `cargo test -p xtask resource_map_ -- --nocapture` (24 focused tests), `cargo test -p xtask -- --nocapture` (48 tests), `cargo run -p xtask -- mainlines generate`, `cargo run -p xtask -- mainlines check`, `cargo run -p xtask -- gates check`, and `git diff --check`. Remaining gap: this strengthens declared registry rows; it still does not automatically discover every undeclared Rust resource edge.

- 2026-07-12: Bound Resource Operation Test Coverage command entries now have repo-owned target validation. `verify_resource_map` extracts backticked commands from each `bound` coverage cell and validates cargo `-p/--package` names against repo Cargo manifests, `scripts/...` entries against real script files, and `make <target>` entries against the repo `Makefile`. Red test `resource_map_rejects_bound_coverage_unknown_cargo_package` proves a non-existent cargo package fails. Current proof passed: focused red test, `cargo test -p xtask resource_map_ -- --nocapture` (25 tests), `cargo fmt --check`, `cargo check -p xtask`, `cargo test -p xtask -- --nocapture` (49 tests), `jq empty docs/resource-maps/core.json`, `cargo run -p xtask -- mainlines generate`, `cargo run -p xtask -- mainlines check`, `cargo run -p xtask -- gates check`, and `git diff --check`. Remaining gap: the gate validates command entrances and repo-owned targets; it still does not execute every command listed in every coverage row.

- 2026-07-12: Pending resource operations now require explicit closure contracts. `verify_resource_map` rejects `binding_status=pending` operation bindings unless they declare non-empty `pending_reason`, `pending_closure_doc`, and `pending_verification`, and the closure doc must exist. Production pending operation `instruction_capability.admit_request_context` now names its reason, `docs/design/instruction-capability-loader-design.md` as closeout doc, and required closure verification before becoming bound. Red test `resource_map_rejects_pending_operation_missing_contract` proves a vague pending operation fails. Current proof passed: focused red test, `jq empty docs/resource-maps/core.json`, `cargo fmt --check`, `cargo check -p xtask`, `cargo test -p xtask resource_map_ -- --nocapture` (26 tests), `cargo test -p xtask -- --nocapture` (50 tests), `cargo run -p xtask -- mainlines generate`, `cargo run -p xtask -- mainlines check`, `cargo run -p xtask -- gates check`, and `git diff --check`. Remaining gap: `instruction_capability.admit_request_context` is still pending; the gate makes it owned and auditable, not closed.

- 2026-07-12: Resource-center planning docs must use current source-truth paths: machine-readable mainline call truth lives under `docs/mainline-calls/*.json`, and test designs live under `docs/testing/*.md`. The top-down resource-center goal plan now records a current audit section with 19 core resources, 14 operation bindings, 13 bound operations, 1 pending operation, 21 relation rules, 7 forbidden direct relations, 5 source shortcut gates, and 2 precise source-edge gates. Residual gaps remain explicit: `instruction_capability.admit_request_context` is pending, whole-repo undeclared Rust resource-edge discovery is not automatic, and coverage command entries are target-validated but not executed by the gate.

- 2026-07-12: `instruction_capability.admit_request_context` is now bound resource truth. `freehand-contracts` has `ContextSegmentKind::InstructionCapability`; `freehand-blocks::plan_context` validates it as session-stable/cacheable developer context; `freehand-instructions::render_instruction_capability_context` renders compiled AGENTS.md and skill manifest entries; `freehand-runtime` admits that owner output as the `instruction-capability` segment before provider request building. Runtime/provider/UI code must not scan AGENTS.md or skills authoring directories directly, and provider adapters must not patch instruction content into wire payloads directly. Resource map now has 14 operation bindings and all are `bound`; source edge `instruction.capability-loader#07` binds `instruction_capability.admit_request_context` to `crates/freehand-runtime/src/lib.rs::instruction_capability_segment`. Proof passed: `cargo test -p freehand-instructions`, `cargo test -p freehand-blocks`, `cargo test -p freehand-runtime live_bridge -- --nocapture` (41 tests), xtask resource-map/full tests, mainlines generate/check, gates check, fmt, and diff check. S-profile `127.0.0.1:4042` online proof session `cli-adp-sample-success-1783840670133249000` persisted `planned_context` and `provider_payload.input_segments` with `kind=InstructionCapability`, `source=instruction_capability`, and `<freehand_instruction_capability>` content; config remained minimax/MiniMax-M3 inline auth and fixture env grep had 0 matches. Remaining resource-center risk: gates still do not automatically discover every undeclared Rust resource edge and do not execute every coverage-row command.

- 2026-07-12: The original top-down resource-center refactor goal is completion-audited against current evidence. `docs/resource-maps/core.json` is the first truth for 19 required core resources, 14 bound operation bindings, 22 relation rules, 7 forbidden direct relations, 5 source shortcut gates, 2 precise source-edge gates, and 14 source-edge registry rows. Feature map, function maps, mainline call maps, test designs, generated wiki, local skill, and xtask gates are aligned to resource operations. `docs/goals/resource-center-top-down-refactor-plan.md` now contains a requirement-by-requirement completion audit. Residual risks are future gate-strengthening items, not blockers for the original goal: automatic discovery of every undeclared Rust resource edge and execution of every coverage-row command inside gates.
- 2026-07-12: Runtime submit failure observability is locked for early live provider/protocol failure. `RuntimeCommandDispatcher::prepare_live_submit_user_input` must not swallow selected-session/cwd errors and fall back to non-live submit. `finish_live_submit` restores bridge-materialized failed turn truth, or persists/projects an early failed selected-session turn when provider/protocol fails before recovery truth exists; corrupt persistence still fails explicitly. Regression `live_dispatch_materializes_failed_turn_when_provider_fails_before_persistence` passed, as did existing failure-preservation coverage. Online S-profile proof on `127.0.0.1:4042` used fixed session `online-fixed-observable-submit-proof`: after 2s ADP `QuerySessionTurns` showed `runtime-turn-287` with original user_text pending, final query showed one successful terminal turn, and AgentBoard exposed Worker `state=blocked` for `task-1783787230` with `openai_http_request_failed` to `https://api.anyint.ai/openai/v1/responses`. ADP submit validation must use internally tagged JSON with `kind=command|query|subscribe` and must query selected session plus TaskBoard/AgentBoard/TaskHistory in parallel; receipt waiting alone is not proof.
- 2026-07-12: macOS launchd install/restart now has first-launch file permission preflight. `scripts/freehand-file-permission-preflight.sh` checks runtime home, launchd workdir, Documents/Desktop/Downloads, optional extra paths, writes `~/.freehand/state/file-permission-preflight.json`, opens Full Disk Access settings on denial, and fails by default unless `FREEHAND_FILE_PERMISSION_PREFLIGHT=warn` is explicitly set. `scripts/install-launchd.sh` runs it before install/restart for master and worker profiles. S-profile `restartS` proof showed preflight ok and status JSON `{status:"ok", runtime_home:"/Users/fanzhang/.freehand", workdir:"/Users/fanzhang/.freehand"}` before service health passed.
- 2026-07-12: Fixed-session ADP submit observability now has a standard script: `scripts/verify-adp-fixed-session-observability-online.py --url ws://127.0.0.1:4042/adp --session <fixed-id>`. It uses the correct internally tagged ADP envelope, queries the same selected session while pending and after receipt/timeout, and includes TaskBoard/AgentBoard owner truth in one JSON proof. Proof session `online-fixed-observability-standard` passed: pending `runtime-turn-288` kept original user_text with no terminal yet, final had `terminal_status=Success`, and Worker owner truth exposed `worker state=blocked current_task_id=task-1783787230 current_execution_id=exec-worker-worker-1783866078571451000-1266` with `openai_http_request_failed` to `https://api.anyint.ai/openai/v1/responses`. Use this script instead of ad hoc WebSocket snippets for fixed-session submit checks.
- 2026-07-13: Master parent-session completion is now runtime-gated against child Task Center truth. A user-session Master `claim="complete"` is rejected while any child task with the same `parent_session_id` is not `Closed`; the provider receives schema-repair feedback and the user-facing turn must not project `TerminalStatus::Success` for mere dispatch/waiting. Local proof `live_master_rejects_complete_while_parent_child_task_open` passed; online S-profile proof `online-master-three-worker-e2e-current` showed premature final completion was blocked while beta/gamma were still open, then alpha/beta/gamma eventually closed. Remaining product gap: all child tasks closing does not yet automatically resume the parent session and synthesize the final user-facing answer; a parent-session aggregator/resume path is still required.
- 2026-07-13: The parent-session aggregator/resume path is now implemented and verified. `ProductionMasterRunner` handles `task_closed`, waits until every Task Center child sharing the same `parent_session_id` is `Closed`, deterministically collects the latest accepted `TaskReviewSubmitted` summary/deliverables/evidence from each child, and runs one follow-up Master turn in the original persisted parent reason session. A deterministic parent/session/child-version marker plus successful reason-persistence lookup closes the crash window where the aggregation turn is durable but the Master cursor/state is not; replay/restart does not call the provider twice. Runtime-backed `QuerySessionTurns` refreshes the requested transcript from reason persistence so background aggregation is immediately query-visible; `<freehand_parent_aggregation ...>` synthetic prompt text is hidden from user projection while the final assistant synthesis remains visible. Isolated online proof `online-master-three-worker-aggregator-1783916209` dispatched and closed alpha/beta/gamma, produced successful `runtime-turn-2` containing all three worker result tokens, and after isolated Master restart retained exactly one aggregation with no duplicate summary. Global `~/.freehand` was not mutated to hide its unrelated historical EventInbox/TaskBoard inconsistency.
- 2026-07-13: Parent-session aggregator/resume was reverified after continuation. Focused runtime coverage passed (`production_master_runner_` 14/14, `live_master_rejects_complete_while_parent_child_task_open`, `runtime_query_session_turns_restores_background_parent_aggregation`, `live_bootstrap_restores_all_persisted_sessions_into_ui_state`), as did `cargo fmt --check`, `cargo clippy -p freehand-runtime --all-targets -- -D warnings`, `xtask mainlines generate/check`, `xtask gates check`, and `git diff --check`. Isolated online proof `online-master-three-worker-aggregator-1783918267` closed alpha/beta/gamma, generated parent `runtime-turn-2` with all three worker result tokens, and restart proof stayed `aggregation_count=1`. Full `freehand-runtime` package remains non-green at 132 passed / 12 failed in adjacent live-tool/checkpoint/autonomy/task-list tests, so do not claim package-wide green.
- 2026-07-13 correction: all current child tasks being `Closed` is only a trigger for parent-session evaluation, not proof of overall goal completion. The Master must evaluate original user objective history together with every decomposed task's goal/deliverables/acceptance and accepted Worker review truth, then decide whether to create correction/improvement/new child tasks, record a real blocker, or finally complete. A follow-up that merely aggregates Worker results and forbids new task mutations is architecturally wrong because it can report progress without finishing the user objective. Online acceptance must prove at least one next-round task decision before final completion, not only three child closures followed by a summary.
- 2026-07-13: The corrected parent-session evaluation loop is implemented and online-verified. `task_closed` for the current all-closed child set triggers `ParentEvaluated`, not final aggregation. Evaluation truth contains deduplicated root user objective turns only, each child task's content/goal/deliverables/acceptance, and latest accepted review summary/deliverables/evidence. The Master may create correction/improvement/new tasks or block explicitly; final completion is allowed only after overall-goal evaluation. Persisted Success/ToolPending/Blocked evaluation turns are idempotent decisions because they may already have created next-round tasks. Online session `online-master-three-worker-evaluation-1783921598` proved beta rejection and rework, first-set evaluation creating an integration task, integration review/approval/close, second evaluation final success at `runtime-turn-3`, and restart `final_evaluation_count=1`. Focused Master runner coverage passed 16/16; full runtime remained 134 passed / 12 pre-existing adjacent failures.
- 2026-07-13: A review-only multi-agent WebUI static prototype now lives at `docs/prototypes/freehand-agent-dashboard/index.html`; it does not modify product WebUI/protocol/runtime truth. Desktop uses a Codex-like session rail plus a middle-top review dashboard that shows the total goal, Master phase, Worker task/review states, and next-decision queue. Tablet portrait and phone remain conversation-first with a session drawer, compact agent strip, and agent bottom sheet. The dashboard semantics follow the corrected Master loop: evaluate Worker quality against the total/decomposed goals, reject/rework or create next-round work, and claim overall completion only after goal evaluation; it is not a Worker-result aggregation surface. Playwright verification passed at `1440x900`, `834x1112`, and `390x844` with no horizontal overflow, all seven lifecycle states, drawer/sheet open-close, and mobile composer clearance.
- 2026-07-13: The confirmed visual direction for the multi-agent WebUI/mobile design is black/white/gray with minimal blue/green accents. `docs/prototypes/freehand-agent-dashboard/index.html` was updated from the warmer palette to grayscale base, blue for active/running/evaluation cues, green for accepted/OK cues, and gray for neutral/rework/review states. Jason confirmed the mobile version should be implemented following this prototype direction. Future product implementation must still bind the mobile surfaces to owner-backed UI projections and verify with the normal WebUI/mobile online gates.
- 2026-07-13: The production mobile Agent Dashboard is implemented under `app.webui-smoke`. Phone, tall-phone, and tablet portrait keep the conversation primary, use a left session drawer plus compact top Agent strip and Agent bottom sheet, and preserve desktop inspector behavior. The sheet renders Master quality evaluation, Worker task/review/rework state, Agents, review history, and Worker control from existing TaskBoard, AgentBoard, EventInbox, TaskHistory, WorkerControl, and selected-session turn projections only. Closed Worker children alone render `Awaiting Master evaluation`; `Goal complete` requires selected parent Success truth. Session drawer presentation uses one expandable Master group containing persisted parent sessions and TaskBoard-parented temporary Worker child rows. New conversation now persists protocol-owned session metadata through `CreateSession` before submit/reload, because queryable turns without session-list metadata are rejected by the session-list truth gate. Final daemon/browser proof passed at `artifacts/webui-online/20260713-verify-4042-1783933067766/summary.json`; all checks were true for drawer swipe/hierarchy, strip/sheet open-close/scrim/mutual exclusion, state preservation, owner-truth matching, internal-id/aggregation rejection, scroll lock, phone/tablet portrait, and desktop regression. Final reviewed screenshots are `19-mobile-session-drawer-open-swipe.png`, `32-mobile-agent-dashboard-main.png`, `33-mobile-agent-dashboard-sheet.png`, `37-tablet-agent-dashboard-sheet.png`, and `38-desktop-agent-dashboard-regression.png`. Local proof passed freehand-server 13/13, JS checks, fmt, mainlines generate/check, gates check, and diff check; S-profile restored to `cc`/OpenAI Responses/`api.anyint.ai`/`gpt-5.5`/env auth.
- 2026-07-13: WebUI lifecycle dashboards must be selected-session scoped, not global Task Center scoped. Root cause from phone surface showing `Blocked · 144 task(s) · 31 blocked` after clearing sessions: mobile Agent Dashboard used a fallback from parent-scoped tasks to all TaskBoard tasks when the selected session had no children, and desktop lifecycle panes still displayed global AgentBoard/EventInbox rows. `app.webui-smoke` now derives current TaskBoard cards/counts, AgentBoard rows, EventInbox rows, TaskHistory target, WorkerControl target, and mobile Agent Dashboard from TaskBoard `parent_session_id` / selected parent session task ids only; selected sessions with no child tasks render `0 current task(s)`, `0 current agent(s)`, and `0 current event(s)` instead of old blocked/review/test history. Verified locally with JS syntax, targeted freehand-server asset smoke, `xtask mainlines check`, `xtask gates check`, and `git diff --check`. Online S-profile browser artifact `artifacts/webui-online/20260713-verify-4042-1783939658253` proves a new selected session stays at zero current lifecycle rows while the daemon Task Center still contains 144 historical tasks. Full `scripts/verify-webui-online.sh` did not complete because a later phase2 dashboard wait timed out, so use the artifact only for this scope fix, not as a full WebUI gate pass.
- 2026-07-13: WebUI lifecycle-scope fix follow-up: the online verifier failure after the selected-session UI fix was verifier/runtime-environment related, not a regression to global Task Center display. Hidden failure samples now use Master-safe `task` query against `definitely-missing-freehand-task` instead of illegal `read_file`; progress detection accepts a selected visible dispatching turn before live cards appear; `scripts/install-launchd.sh` health wait is configurable and defaults to 60s. Local proof passed JS checks, targeted freehand-server asset smoke, fmt, `xtask mainlines check`, `xtask gates check`, and `git diff --check`. S-profile was restored to provider `cc`, OpenAI Responses, host `api.anyint.ai`, model `gpt-5.5`, env auth, with no `FREEHAND_WEBUI_VERIFY_CREDENTIAL`. Latest online artifact `artifacts/webui-online/20260713-verify-4042-1783943684651/03-first-materialized.json` again shows a new selected session with `0 current task(s)`, `0 current agent(s)`, and `0 current event(s)`. Full online WebUI gate is still blocked by live provider `openai_http_status_402` insufficient credits from `api.anyint.ai`; do not claim full online green until provider credit/connectivity is restored and the gate completes.
- 2026-07-13: WebUI full online gate passed when rerun against temporary S-profile minimax provider. `scripts/verify-webui-online.sh` exited 0 with artifact `artifacts/webui-online/20260713-verify-4042-1783944723612/summary.json`; every summary check was true. The proof session `webui-session-20260713121233-ebc85775` showed selected-session-scoped desktop lifecycle counts `0 current task(s) · 0 blocked · 0 review · 0 stale`, `0 current agent(s) · 0 active`, `0 current event(s) · updated`, and mobile Agent Dashboard counts `0 current task(s) · 0 blocked · 0 review · 0 stale`, `0 agent(s) · 0 active`. The S-profile was restored afterward to provider `cc`, OpenAI Responses, host `api.anyint.ai`, model `gpt-5.5`, and health returned `ok`. The previous full-gate blocker was provider credit/connectivity on `cc`, not the WebUI lifecycle scope fix.
- 2026-07-13: WebUI submit observability must treat absolute path-leading text such as `/Volumes/...` as normal user input unless the first token exactly matches a known slash command. `apps/freehand-server/assets/webui.js` now falls unknown slash-leading text through to `SubmitUserInput`, renders the submitted user text immediately, and on ADP submit timeout/error shows `dispatch status unknown · refresh needed` in both command status and turn status instead of leaving stale `dispatching...`. Online proof artifact `artifacts/webui-online/slash-path-observable-1783947943516/state.json` shows `promptVisible=true`, `unknownSlashVisible=false`, and both status fields changed to unknown/refresh-needed after timeout. Local proof passed JS syntax, targeted freehand-server smoke, `cargo fmt --check`, and `git diff --check`.
- 2026-07-13: Provider route truth supports explicit per-agent primary/backup configuration through `[agents.<name>] provider` plus `fallback_provider`; both provider ids must exist, be enabled, and differ. `config.core` resolves each route independently, including protocol, model, endpoint, and auth. Runtime failover is provider-neutral and non-stream only: eligible primary HTTP/network/stream-read failures retry the primary when retryable, then switch once to the configured fallback; adapter, invalid-config, and callback failures do not switch. A successful switch records `provider.route=fallback`, `provider.failover_from`, `provider.failover_to`, `provider.failover_error_code`, and the fallback model. Error Center records the primary failure as recoverable with `recovery_action=failover_provider`, never `fail_turn` when fallback succeeds.
- 2026-07-13: Controlled online failover proof passed on production S daemon/ADP. Session `cli-adp-sample-success-1783953723678094000`, turn `runtime-turn-347`: provider id `cc` was temporarily routed to a local fixture that returned exactly one OpenAI Responses 402, then the real `minimax` fallback completed with persisted `model=MiniMax-M3`, `terminal=Success`. Metadata contained `error.code=openai_http_status_402`, `error.recovery_action=failover_provider`, `provider.route=fallback`, `provider.failover_from=cc`, and `provider.failover_to=minimax`; ADP error query returned one recoverable provider event. This proves controlled primary-402 to real fallback, not a real `api.anyint.ai` 402. Final S profile was restored to `cc` / OpenAI Responses / `api.anyint.ai` / `gpt-5.5` / env auth, with master and worker both configured `fallback_provider=minimax`, health `ok`, and no fixture env marker.
- 2026-07-13: Provider/WebUI closeout evidence: config tests passed 17/17, control tests 8/8, five runtime failover positive/negative focused tests passed, targeted clippy/fmt/mainlines/gates/diff checks passed, and full WebUI online gate passed at `artifacts/webui-online/20260713-verify-4042-1783952632179/summary.json` with every check true. The Settings verifier must edit the current primary route rather than changing primary to its configured fallback; identical primary/fallback is correctly rejected. Known boundary: stream failover is not implemented because partial output/tool-call rollback/resume lacks a typed contract. Full `freehand-runtime` is not green: 139 passed / 12 adjacent existing failures, so only focused failover behavior and online proof are closed.
- 2026-07-13: Multi-worker topology truth is ordered `paired_agents`, not singular `paired_agent`. Master agents may compile multiple reciprocal Slave Worker peers; Slave Worker agents must compile exactly one reciprocal Master. Legacy singular `paired_agent` is invalid and must remain rejected by config truth; do not add a compatibility parser, primary-worker field, reverse lookup fallback, or first-worker semantic shortcut.
- 2026-07-13: One Worker process executing three child tasks is not multi-agent proof. A valid local multi-agent proof must start independent Worker processes for independent configured agent ids, prove distinct live PIDs, bind one initial task to each Worker, verify TaskAssigned/TaskResumed/TaskHeartbeat/TaskReviewSubmitted identity never crosses the assigned Worker, and persist restart/query evidence.
- 2026-07-13: Controlled three-process proof passed for session `online-master-three-worker-evaluation-1783960928` with evidence dir `/tmp/freehand-three-worker-home.Zyn1U6/.freehand/tmp/three-worker-e2e-20260714T004208-50363`. The verifier ran in isolated `HOME/.freehand`, started Worker PIDs `51939/51940/51941` for `worker-alpha/worker-beta/worker-gamma`, mapped alpha/beta/gamma to their own Workers, forced beta reject/rework with a new execution id, required a next-round integration task after the first evaluation, and produced final Success only on the second parent evaluation. Restart proof kept `final_evaluation_count=1` and `restart_idempotent=true`; all verifier-started Worker PIDs were stopped explicitly.
- 2026-07-13: Task Center atomic JSON writes must use process-unique temp paths. A seconds-granularity temp path such as `index.tmp-<seconds>` can race when multiple Worker processes boot/write the same index concurrently: one process renames the shared temp file first and another then fails with `No such file or directory`. `write_json_atomic` must include process id, nanoseconds, and an atomic counter or equivalent uniqueness. Regression `atomic_json_write_survives_parallel_same_path_writers` locks this.
- 2026-07-13: Multi-agent convergence still has P3/P4 gaps after the controlled three-process proof. Not complete yet: launchd-managed three Worker service lifecycle, Worker health/restart owner truth, real-provider crash/recovery/takeover proof, cross-machine multi-peer node transport, and shared `leases.json` multi-process read-modify-write lost-update analysis/fix. Do not describe the current state as full production multi-agent convergence.
- 2026-07-14: Shared Task Center `leases.json` read-modify-write is now cross-process serialized by `TaskStore::with_lease_state_lock`. Atomic rename alone was insufficient: the red test `lease_state_rmw_preserves_parallel_distinct_writers` proved the prior implementation retained only 1 of 24 concurrent distinct leases. Lease create/refresh/remove now holds `leases.lock` across load, mutate, and atomic replace; boot reconciliation removes only invalid task ids from current locked truth instead of replacing the file from a stale map. Positive distinct-writer and negative concurrent refresh/removal tests passed, `freehand-task` passed 52 tests, and isolated three-process session `online-master-three-worker-evaluation-1783988351` completed alpha/beta/gamma plus beta rework and next-round integration with restart-idempotent final evaluation. Remaining production gaps are launchd-managed Worker health/restart, real-provider crash/recovery/takeover, and cross-machine multi-peer transport.
- 2026-07-14: Worker process health/restart owner truth is implemented under `agent.lifecycle`. `TaskRuntime::apply_agent_lifecycle_event` accepts typed `ProcessStarted` and `ProcessHeartbeat`, persists PID, process instance id, process start/heartbeat timestamps, and restart count, and rejects empty process instance or PID 0 without mutation. AgentBoard/AgentLifecycle query projection derives `alive` from `process_heartbeat_at + 5s TTL`; missing/stale process heartbeat does not fallback to task activity or AgentSnapshot status, and task/execution/activity history remains visible while `alive=false`. `ProductionWorkerRunner` writes process start at construction, heartbeat on every idle/active `run_once`, and active-loop process heartbeat via `WorkerHeartbeat::start`. ADP/UI exposes the truth as `agent.process`. Online isolated proof `online-master-three-worker-evaluation-1783993233` stopped explicit gamma PID `93601`, observed `alive=false` after TTL while retaining task `task-three-worker-1781783993233-gamma` and execution `exec-worker-worker-gamma-1783993239925554000-3`, then restarted gamma as PID `96753` with new process instance and `restart_count=1`; evidence dir `/tmp/freehand-three-worker-home.AwGYEs/.freehand/tmp/three-worker-e2e-20260714T094033-93242`. Local proof passed `freehand-task` 54/54, Worker runner 14/14, `freehand-ui-protocol` 56/56, daemon worker-mode 1/1, CLI test compile, targeted clippy, fmt, mainlines generate/check, gates, diff check, and online three-worker verifier. Remaining gaps: launchd-managed KeepAlive/crash restart proof, real-provider crash/recovery/takeover, and cross-machine multi-peer transport.
- 2026-07-14: Launchd-managed three Worker lifecycle is online-proven with isolated HOME and unique labels. `scripts/verify-launchd-three-worker-services-online.sh` runs the three-Worker parent-evaluation verifier with `FREEHAND_THREE_WORKER_WORKER_START_MODE=launchd`, starts `worker-alpha`, `worker-beta`, and `worker-gamma` through `scripts/install-launchd.sh restartWorkerS`, kills only gamma's explicit PID, lets KeepAlive restart the same configured agent, and verifies AgentBoard owner truth shows the same task/execution with a new PID/process instance and `restart_count=1`. `scripts/install-launchd.sh` keeps production `launchctl enable` behavior by default but supports `FREEHAND_LAUNCHD_SKIP_ENABLE=1` for temporary verifiers so unique labels do not leave persistent enable overrides. Latest online proof `online-launchd-three-worker-evaluation-1783994336-33327` used gamma label `com.freehand.verify.three-worker.skipenable.1783994336-33322.worker-gamma`, old PID `35572`, new PID `36923`, retained task `task-three-worker-1781783994336-gamma`, retained execution `exec-worker-worker-gamma-1783994347588322000-3`, and evidence dir `/tmp/freehand-three-worker-home.rDDvDe/.freehand/tmp/three-worker-e2e-20260714T095856-33335`; cleanup check for that prefix returned zero launchctl matches. Remaining gaps: real-provider crash/recovery/reassignment/takeover and cross-machine multi-peer transport.
- 2026-07-14: Provider retry/failover is transient same-turn model-request activity, not durable turn error truth when recovery succeeds. OpenAI-compatible successful responses may include `error:null`; field presence is not failure evidence, and only a non-null wire error object maps to provider error semantics. Runtime must publish provider request-built, retry-scheduled, and failover-switched debug projections immediately before provider IO/backoff/route continuation so WebUI can show progress during the actual pending window. Recovered retry/failover turns must have empty `turn.error_events`, clear provider activity on normal semantic response/terminal projection, and leave only metadata/error-center evidence outside public conversation truth. Verified with OpenAI null-error parser tests, UI provider-recovery projection test, runtime retry/failover tests including `live_bridge_publishes_provider_retry_before_next_attempt`, server asset smoke, and online S-profile fixture `artifacts/webui-online/provider-recovery-20260714T092758-10239/summary.json` proving 500/500/success, visible provider retry row, final Success, and ADP `errors=[]` after config/env restoration.

- 2026-07-14: Timer and Master/Worker closed-loop semantics corrected and unit-locked. Timer due with source-session truth injects the persisted wakeup prompt as a new next-ordinal turn in the original session; it never restores/resumes/reopens the source turn. Source-less timers still use internal `master-timer-*` wakeup turns. Master closed-loop evaluation is quality/goal evaluation, not aggregation: after child tasks close, Master compares original user goal, decomposed child requirements, and accepted Worker review truth, creates next-round work when the overall goal remains incomplete, and only finalizes after next-round work closes. EventInbox delivery now uses a deterministic v2 per-task sequence watermark (`event.seq > watermark[task_id]`) rather than timestamp/task-id cursor ordering; timestamp only orders returned batches. This exposed and fixed scheduler tick duplicate-seq truth: multiple scheduler facts for one task now apply each event before building the next event. Unit proof passed: timer source injection/internal/chained/failure tests, closed-loop next-round test, open-sibling/no-final-complete tests, v2 lower-task same-timestamp cursor test, legacy cursor migration test, attachment visibility tests, `cargo test -p freehand-task -- --nocapture` (56), `cargo test -p freehand-runtime master_runner::tests:: -- --nocapture` (25), fmt, mainlines generate/check, gates check, JS syntax, timer script syntax, and diff check. Online S-profile proof remains required before claiming live closure.
- 2026-07-14: Android launcher icon truth is repository `assets/logo.png`. `apps/freehand-android/scripts/generate-launcher-icons.sh` deterministically derives square and round mdpi/hdpi/xhdpi/xxhdpi/xxxhdpi assets at 48/72/96/144/192 px; `verify-launcher-icons.sh` locks dimensions and exact source-derived pixels. The debug APK built successfully and its packaged launcher resources matched the source. True-device installation remains a separate acceptance gate when an explicit ADB device is connected.
- 2026-07-15: Android product UI truth is the daemon-hosted WebUI at `http://<host>:<port>/?client=android-webview`. `apps/freehand-android` owns only endpoint bootstrap, WebView hosting/window insets, and Android system file-picker integration. Local `bridge.html`, native conversation/settings/update/status UI, Android ADP/SSE/HTTP projectors/clients, and server `/mock/android` are physically removed and must not be restored. Android app-owned config admits only active Tailscale profile identity plus host/port; removed transport/relay fields are strict errors, not ignored or migrated. Verified by Android JVM build/tests, server 13/13 tests including mock 404, APK inspection, launcher verifier, mainline/gates/fmt/diff checks, live canonical daemon HTML, and true-device PLZ110 proof `artifacts/android-device/20260715T000253Z-100.104.163.65_5555-46095`: foreground Freehand activity, no fatal logcat, `webuiShell=true`, `layoutClient=android-webview`, `layoutShape=tall_phone`, strict `100.66.1.82:4041` app-owned config, and screenshot manual review showing canonical WebUI mobile layout with no native fallback shell.
- 2026-07-15: Android startup readiness is a native platform-gate concern, not a second product UI. `MainActivity` shows a dark repo-logo startup overlay with a small progress animation before WebView readiness, and `WebUiStartupGate` removes it only after the canonical DOM probe reports `webuiShell=true` plus `layoutClient=android-webview`; `onPageFinished` alone is not success, and load/probe errors stay explicit on the overlay. Verified by the Android gate unit test, debug APK build/install on PLZ110, true-device startup screenshot `/tmp/freehand-android-startup-or-webui.png`, and canonical WebUI probe evidence.
- 2026-07-15: Configured Agent capacity is owned by `config.core`, separate from live AgentBoard process truth. `UpdateAgentResourceConfig` accepts only Master-owned `resource_count=1..=5`, atomically grows/shrinks deterministic Worker peer config, and currently applies the first Worker template's primary/fallback provider to all resources (`shared` provider mode). Runtime returns restart-required projection and must not synthesize live Workers; live availability still requires restart plus matching Worker processes. WebUI Agents sheet projects configured count/limit/provider and current live/delegated truth separately. Verified by config/ui-protocol/runtime positive and rejection tests, 13/13 server tests, live no-store WebUI HTML/JS, and mobile screenshot `/tmp/freehand-mobile-agent-resource-sheet.png`.
- 2026-07-15: Worker/subagent transcripts are runtime/reason projections, not browser-cleanup artifacts. `ReasonPersistence::restore_turn_snapshots_for_ui` now uses authoritative closed/active turn snapshots plus rollback-marker sidecar truth on the normal UI query path, coalesces repaired `-rN` rounds to the latest logical turn, and avoids replaying huge reason ledgers when authoritative snapshots exist. Runtime `QuerySessionTurns` searches configured Master/Worker reason namespaces, preserves Worker source attribution, and hides `worker-task-*` framework task/continuation prompts from `user_text`. WebUI session switching renders explicit loading/failure while a pinned `QuerySessionTurns` is in flight and must not flash chat-empty `New conversation`. Verified on real S-profile Worker session `worker-task-task-1784013898`: before reason fix the query took `75204ms` and returned `218` rows from a 7.4GB reason ledger; after fix it returned in `133ms`, `9291` bytes, `6` rows, `user_text` non-null `0`, forbidden prompt count `0`, source `worker`. Mobile WebUI CDP proof `artifacts/webui-online/session-switch-worker-1784128322135/summary.json` showed task click immediate loading with no chat-empty New conversation and final Worker transcript with userMessageCount `0` / fake prompt regex `false`.
- 2026-07-15: Framework `task` and `timer` card semantics are owned by `crates/freehand-blocks/src/tool_display.rs`, not by WebUI parsing. `Task` projects operation/task/title/assignee/status/cwd/dispatch; `Timer` projects operation/id/timing/reason/wakeup prompt; `ui.protocol` carries those first-class display kinds to clients. Live pending/cancelled and persisted Worker projections share the same internal-prompt hiding predicate, while normal user sessions retain their real user text. Current-session delegated-task inspection is uncapped and identity-bound: WebUI renders every TaskBoard child with `data-task-id` plus canonical `data-worker-session-id`, and `scripts/verify-worker-subtasks-online.py` enumerates every child for one parent and queries each Worker transcript. Verified after S-profile restart: `online-master-three-worker-e2e-current` returned three non-empty Success Worker transcripts with zero internal-user-text leaks; mobile CDP artifact `artifacts/webui-online/worker-subtasks-1784133884215/summary.json` proved sheet -> Worker transcript -> exact parent return with `userMessageCount=0` and no fake framework prompt.
- 2026-07-16: Master idle lifecycle attention is a two-phase durable contract. EventInbox admission stays source ordered and advances the cursor only after each source event is durably admitted to `pending_attention` or classified as non-attention. Dequeue is separate and deterministic: `severity_rank * 10000 + clamp(task_priority,-100,100) * 100 + admission_age * 5000`, with blocked showstoppers and critical/high semantic changes carrying large weight while admission-sequence aging prevents starvation without wall-clock timing. Retryable failures keep the same pending event, admitted sequence, and cursor; stale no-op attention is removed and selection continues in the same tick. Parent evaluation reads only the first round of the first persisted user turn from authoritative reason truth, not UI-coalesced repair/control projection. Verified with `cargo test -p freehand-runtime master_attention -- --nocapture` (4), `cargo test -p freehand-runtime master_runner::tests:: -- --nocapture` (31), `cargo test -p freehand-runtime production_worker_runner -- --nocapture` (18), and focused `freehand-task` attention/master-poll tests. Busy-Master safe-point suspension/checkpoint/typed-resolution/return path remains pending and must not be claimed closed.
- 2026-07-16: Busy-Master active-work truth is now a first-class `master_work` resource under `runtime.master-worker-loop`. It persists foreground Master work identity, priority, safe point, suspension state, typed attention resolution, and exact return identity in `~/.freehand/state/master-loop/<master>.active-work.json` guarded by a sibling lock. Runtime live submit registers before committing a new turn ordinal and clears on terminal/error/cancel; concurrent foreground Master submit fails explicitly without ordinal gaps. Live bridge publishes provider/tool safe-point phases using the original logical turn id, not repaired round ids. Master lifecycle runner now defers lower-priority attention, requests suspend during provider/tool effect in-flight, suspends only at declared safe points, resolves isolated attention into typed changed-task identity, rejects raw Worker/control transcripts and provider payload markers, and restores exact work/session/turn/trace identity. Verified with focused runtime busy/attention/resume/dispatcher tests, full `master_runner::tests::` (38), live cancel-token tests, `production_worker_runner` (18), runtime clippy, fmt, mainlines, gates, and diff check. Remaining gap: typed attention resolution is persisted but not yet injected into the original foreground reasoning continuation, so busy-Master live preemption is not online-closed.
- 2026-07-16: Busy-Master attention continuation is now focused-test bound through `master_work.admit_resolution_context`. The original foreground Master turn consumes one typed `AttentionResolution`, refreshes TaskSpaceSnapshot, admits the resolution as turn-volatile/no-cache developer context, and preserves original session/logical-turn/trace identity. Stale provider tool calls produced before the attention decision get paired failed tool results and no side effects; stale terminal candidates are discarded before terminal persistence and cannot become durable closed-turn truth. Critical bug lesson: when no pending tools exist, never call `Option::take()` on the resolution before checking the pending-tool condition, or the terminal continuation path loses the resolution. Verified with post-cleanup `production_master_resume` (3), `live_master_attention` (2), `production_master_foreground` (2), plus `master_runner::tests::` (42 before signature cleanup), `runtime_live_submit` (2), `freehand-blocks` (52), `freehand-contracts` (10), targeted clippy/fmt/mainlines/gates/diff checks. Remaining gap: full daemon/WebUI/Android online proof for suspend -> isolated decision -> typed continuation is still unclaimed, and `master.edge.handle_attention` remains pending until isolated control-turn binding is tested.
- 2026-07-16: `master.edge.handle_attention` is now focused-test bound. While the exact foreground `master_work` checkpoint is `SuspendedByAttention`, the selected attention executes through a task-scoped `master-lifecycle-*` request whose event/attempt-isolated session, turn, and trace ids are distinct from the foreground user session, logical turn, and trace. The control executor summary is not copied into foreground ReasonPersistence, `master_work`, or typed `AttentionResolution`; the negative test injects raw-control/provider sentinel text and proves those owner truths remain clean. Verified with `production_master_attention` (4), `production_master_resume` (3), `live_master_attention` (2), `production_master_foreground` (2), `production_master_busy` (4), full `master_runner::tests::` (44), targeted runtime clippy, fmt, mainlines, gates, and diff checks. Full S-profile daemon/WebUI busy-preemption proof and production Worker safe-point pause/resume remain unclosed.
