# Runtime Home

Freehand runtime home is `~/.freehand`.

## Rule

- repo files hold source and architecture truth
- runtime state and evidence live under `~/.freehand`
- new runtime subsystem should use documented paths under `~/.freehand`

