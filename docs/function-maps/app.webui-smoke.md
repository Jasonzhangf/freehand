# Function Map: `app.webui-smoke`

- feature_id: `app.webui-smoke`
- owner crate: `apps/freehand-server`
- owner module: `apps/freehand-server/src/lib.rs`
- resource map: `docs/resource-maps/core.json`
- mainline call source: `docs/mainline-calls/app.webui-smoke.json`
- generated wiki: `docs/wiki/app.webui-smoke.md`
- owner entry symbols:
  - `render_webui_smoke`
  - `assets::asset_response`
  - `build_webui_router`
  - `serve_webui_listener`
  - `handle_command_ingress`
  - `handle_query_checkpoints`
  - `handle_adp_socket`
  - `handle_adp_connection`
  - `handle_android_update_manifest`
  - `handle_android_update_apk`

## Resource Map Binding

- resource map: `docs/resource-maps/core.json`
- owned resources:
  - `ui_surface`: ephemeral WebUI shell and surface render projection
- touched resources:
  - `ui_projection`
  - `ui_surface`
  - `android_notification`
- resource operations:
  - `ui_projection.compose_shell` (`ui_projection` -> `ui_surface`)
  - `ui_projection.render_surface_contract` (`ui_projection` -> `ui_surface`)
  - `ui_projection.bind_protocol_call` (`ui_projection` -> `ui_projection`)
  - `ui_projection.render_shared_state` (`ui_projection` -> `ui_surface`)
  - `ui_projection.post_android_turn_finished_notification` (`ui_projection` -> `android_notification`)
- forbidden shortcuts:
  - `ui_surface` must not persist or infer session, task, lifecycle, config, or provider truth.
  - protocol calls must use generated ADP constructors and explicit receipt/failure handling.
  - WebUI must not post native notifications directly or own Android permission/channel policy.
  - Historical terminal projections must not be treated as newly completed live turns.

## Request Mainline

- app boundary receives a minimal WebUI smoke invocation
- app boundary consumes `freehand-ui-protocol` projection truth only
- app boundary stays decoupled from reason, provider, node, and config semantics
- app boundary may render query snapshot, debug snapshot, and separate slave-card projection without owning protocol semantics
- app boundary serves a real WebUI shell that loads protocol-consumer JS, split CSS assets, and the shared logo asset
- app boundary keeps theme assets separate from WebUI layout assets
- front-end default command/query/status path is ADP WebSocket `/adp`; WebUI sends a versioned first-frame handshake and waits for `handshake_accepted` before query/subscribe/command requests; latest-turn SSE is also consumed as a display-refresh mirror
- front-end command/query/subscribe payloads use the generated ADP constructor module, so JavaScript cannot handwrite command serde strings on the wrong frame class
- front-end session list separates global conversation and task creation through one New dialog: `/new`, `新会话`, and `新建任务` open the dialog; both global conversation and cwd-bound task creation persist protocol-owned session 元数据 through ADP `CreateSession`; global conversation creation omits cwd, while task creation requires a selected or typed target cwd
- front-end selected empty-session rendering binds empty `SessionTurns` to the selected session, clears previous active turn/debug state, and suppresses generic 等待中-data system cards so new sessions do not leak prior session content
- after the session list projection has loaded, the WebUI treats that list as the render gate: `QueryLatestActiveTurn`, latest-turn ADP subscription, latest-turn SSE, and `SessionTurns` projections are accepted only for a listed session, the current draft session, or the current pending-submit session
- front-end draft session ids must be generated in both secure localhost and non-secure Tailscale HTTP contexts; WebUI uses a browser-safe id helper instead of directly requiring `crypto.randomUUID`
- front-end Home session lists expose multi-select and remove via `DeleteSession`, while SessionDetail exposes protocol-owned current-session rename and double-Esc rollback controls without storing session truth locally; archive/restore affordances are intentionally not rendered in WebUI
- front-end composer command menu exposes normal product actions for new conversation/task, permission settings, session/service refresh, attachment preview, tool registry, and context compaction; debug/sample scenario commands are not rendered or accepted
- front-end composer exposes a persistent lower-left image attachment entry plus a command port for normal product actions; compression context remains one action inside that port rather than defining the port
- front-end composer control strip exposes attachment buttons, preview, selected-session refresh, cwd input, model selector, product actions, and keyboard shortcuts as UI-layer affordances over ADP
- front-end settings shell uses coarse first-level groups only: 模型, 智能体运行时, 连接, 可观测性, 外观, and 关于. 模型服务配置 and 模型服务切换/策略 remain separate second-level pages inside 模型; Diagnostics is owned under 可观测性; Worker-limit configuration is owned under 智能体运行时; 安卓 APK 升级 and platform permission status belong under 连接. LLM-specific rows must not be flattened into the first level.
- front-end settings status markers are small hollow squares with one fixed meaning: green is 权威真源 and implemented, orange is partially implemented, and red is placeholder/not implemented. A partial group must remain orange even when some child rows are green; UI-only review rows must not claim 权威真源 mutation until the corresponding owner path and verification are landed.
- front-end provider definition form submits `UpsertProviderConfig` through ADP without switching the selected provider; the primary/fallback selector submits `UpdateAgentProviderSelection`; provider test buttons submit `TestProviderWebSearch`; config mutations re-query `QueryConfigStatus` for restart-required saved projection. WebUI never reads or writes `~/.freehand/config.toml` directly and never accepts raw API-key values
- front-end 模型组 form submits `UpsertModelGroupConfig` through ADP without changing provider definitions; the active group selector submits `UpdateAgentModelGroupSelection`; config mutations re-query `QueryConfigStatus` for restart-required saved projection. WebUI never reads or writes `~/.freehand/config.toml` directly and never synthesizes provider/模型组 truth locally
- front-end layout classifier maps viewport width plus aspect ratio into explicit shape attributes (`phone_portrait`, `tall_phone`, `phone_landscape`, `tablet_portrait`, `tablet_landscape`, `foldable_unfolded`, `desktop_large`) and may only rearrange existing components
- server-rendered root shell pins `client=android-webview` first paint to `tablet_portrait` on `body` and shell layout attributes, so Android WebView does not flash the default desktop grid before JS refinement
- front-end mobile portrait layout keeps the conversation workspace as the primary surface; session CRUD/list and config detail surfaces are hidden in explicit overlay drawers opened from mobile controls, while the Agent lifecycle dashboard is a compact top strip plus bottom sheet and must not occupy the normal document flow
- front-end mobile drawers keep a sticky visible drawer header/close control while long settings or session content scrolls; Android/browser back intent first blurs focused form controls, then closes WebUI-owned dialogs, Header inline panels, Agent sheet, or mobile drawer before any app-level exit/navigation
- front-end mobile drawer state synchronizes the closed sidebar and inspector with the browser interaction tree through `inert`, keeps both drawers interactive on desktop, and releases only the opened drawer on mobile
- New dialog open/close uses scoped modal state classes with timed close cleanup and reduced-motion fallback; command status announcements use a polite aria-live region and a short visible updating transition without blocking normal rendering
- keyboard and touch affordances on the New dialog, drawer controls, and status surface use visible focus rings and 44px minimum touch-side targets on mobile layouts
- front-end mobile chrome is phone-first rather than a desktop surface scaled down: the visible topbar is a compact app bar, internal session/turn/cwd/worker/slave/protocol strips are absent from the conversation surface, verifier-only selected session/turn truth lives on shell `data-*` attributes, and focused composer expansion must not reopen the full attachment/CWD/model/status strip into the primary conversation flow
- front-end conversation scrolling keeps a user-scroll lock: live render updates may follow the bottom only when the operator is already near bottom or when a new local submit explicitly forces the current turn into view; if the operator scrolls upward, updates must not force the transcript back to the bottom
- front-end composer clearance is measured from the actual composer card height and exported as `--composer-clearance`, so the final transcript row stays visible above fixed/sticky composer chrome on phone, tablet portrait, landscape, and desktop layouts
- front-end tool result cards expose only copy; successful terminal summary cards expose copy and AddToMemory actions, while waiting/non-success terminal cards do not expose memory
- front-end mobile session drawer can be opened by the bottom session entry or a right-swipe gesture from the main interface content area; both are presentation-only and must not mutate ADP/session truth, transcript truth, composer draft, pending submit, scroll anchor, or lifecycle timers. The drawer owns visible multi-select/remove controls through `DeleteSession`; search is a separate header action.
- front-end global session drawer renders protocol-persisted user session summaries only. Worker/subagent task transcripts are temporary internal sessions and must not appear as top-level or drawer session entries; they may render only inside the owning persisted 主控会话 Header/session tree, using 任务面板 `parent_session_id` and `worker_session_id` 权威真源.
- front-end image attachment drafts are scoped by selected session, render a selected pool with preview/remove controls, send payload only through protocol `SubmitUserInput.metadata.attachments`, clear on command receipt, and remain available for retry after dispatch failure
- transport-facing app routes expose HTTP query for 最新活跃轮次 and per-turn debug snapshot
- transport-facing app routes expose HTTP query for 运行时-owned checkpoint summary projection
- app boundary also serves 守护进程托管 安卓 APK 升级 truth at `/android/update.json` and `/android/freehand-android.apk`; these routes are file/config-serving app surfaces only and must not invent a second release registry beyond explicit 运行时 env override plus compiled `dist/android/update.json` sidecar and signed staged APK artifact truth, and must send no-store cache headers so Android/relay clients cannot keep stale version manifests
- transport-facing ADP query route accepts only versioned connections that completed the first-frame handshake, then calls an injected protocol-owned 运行时 query port for read-only owner projections such as task list/history and error-center 元数据 before using protocol-state snapshots
- front-end Phase 2D 生命周期观察 queries TaskBoard, AgentBoard, EventInbox, TaskHistory, and WorkerControl through the existing ADP/运行时 query path; the browser only caches transient render data and must not persist task/control truth
- front-end 定时任务面板 queries `QueryTimerList` through the ADP/运行时 owner path, submits `ScheduleTimer` and `CancelTimer` as protocol commands, and must not store timer schedules, infer timer state, or encode timers as 任务中心/browser truth
- front-end 工具面板 queries `QueryToolRegistry` through the ADP/运行时
  owner path and renders only 权威投影 schema, examples, guidance,
  execution scope, and Master/Worker exposure. It must not execute tools,
  render session-specific tool turns, synthesize a local `web_search` tool, or
  store browser-local registry truth.
- front-end 搜索面板 queries `QuerySessionSearch` through the
  ADP/运行时 owner path and renders only 权威投影 persisted session
  search rows. It must not search browser-local session state, create a new
  session, or promote Worker/subagent sessions into top-level global results.
- front-end 可观测性 诊断 detail queries `QueryDiagnostics` through the
  ADP/运行时 owner path and renders only 权威投影 log file 元数据 and
  redacted tail lines. It must not read browser-local logs, create sessions, or
  render raw provider payloads, secrets, or absolute user paths.
- front-end mobile Agent Dashboard builds one presentation model from selected-session 任务面板/智能体面板 truth; the Header shows running Agent count plus Worker task lifecycle buckets (`running` / `review` / `blocked` / `closed`) and the highest-priority current task title, while the Header Worker rail shows every current child Worker as a compact status/duration row and the first sheet tap opens only the current session's complete child-task list
- front-end session relationship Header is the canonical Master/Worker navigation surface: collapsed state is a compact dashbar with Chinese lifecycle count labels (`活动` / `审核` / `阻塞` / `关闭`), selected Master/Worker identity, active-task summary, running-agent count, Worker limit, and a scoped Worker status rail; Worker rail rows show status plus duration derived from TaskBoard owner timestamps and expand inline details on click; expanded session relationship state remains an inline panel rooted at the persisted 主控会话 with TaskBoard-projected 工作器子项 sessions indented below it
- front-end context statistics render current and aggregate cache-hit percentages from raw cache-read and normalized total-input counters at exactly three decimal places; browser code must not derive hit rate from only cache-read plus cache-create counters
- front-end Master/Worker wait presentation follows owner lifecycle truth: Worker transcripts stay isolated until TaskBoard/TaskHistory/EventInbox returns typed child result truth; success/failure/blocked/interrupted child outcomes are rigid parent-visible notifications; the selected Master composer remains usable while open children or source timers can wake the lifecycle; browser refreshes owner projections but never synthesizes timer or child lifecycle state
- front-end session relationship locking consumes only protocol schema truth: persisted session 元数据 supplies the Master `session_id`, and `UiTaskSnapshotProjection.parent_session_id`, `attached_session_ids`, `worker_session_id`, and `task_id` supply the Worker relationship; DOM `data-session-id` and `data-task-id` are verification projections of those schema fields, not relationship truth
- front-end Worker labels derive from Worker-only resource ordinals; the Master 智能体面板 row must not offset configured Worker labels
- daemon-rendered HTML uses one server-owned WebUI asset version (`apps/freehand-server/src/assets.rs::WEBUI_ASSET_VERSION`); page/JS modules only embed `__WEBUI_ASSET_VERSION__`, and both root HTML and asset responses stamp/serve that version with `Cache-Control: no-store, max-age=0` so Android WebView relaunch cannot keep an older dashboard shell or script
- front-end lifecycle dashboards scope 任务面板 counts, Worker cards, 智能体面板 rows, 事件收件箱 rows, Worker control targets, and task-history targets to the currently selected persisted parent session; when that session has no child tasks, the projection is empty and must not fall back to global historical 任务面板/事件收件箱/智能体面板 truth
- selecting a Master or Worker node in the Header session tree switches only through the selected-session query path; Worker nodes use 任务面板 `worker_session_id`, and the visible `返回主控` control lives in the Header when a 工作器记录 is selected
- tapping a Worker task consumes its 任务面板-projected `worker_session_id`, immediately refreshes `QuerySessionTurns`, and renders that 工作器会话; WebUI does not derive or guess Worker session ids
- the mobile Worker-task sheet renders every current child task for the selected parent session, not a capped slice or global history fallback; each card carries `data-task-id` and `data-worker-session-id` so online verifiers and manual inspection can check child tasks one by one
- selected-session transcript refreshes pin the requested session id and discard late `SessionTurns` responses for a previous session, so a task click cannot be overwritten by an in-flight parent transcript query
- selected-session switches render an explicit conversation-加载中 state while the pinned `QuerySessionTurns` request is in flight; they must not show the clean `新会话` empty state until that selected session has actually returned an empty transcript
- when a 任务面板-projected active Worker session returns `has no persisted transcript` during selected-session refresh, WebUI renders a retryable 工作器记录 等待中 state with task id/status/assignee evidence, keeps refreshing the same 权威投影 `worker_session_id`, and does not create a persistent connection-failure card
- large 工作器记录s must arrive as reason-persistence logical-turn projections; WebUI may show 加载中/error while 等待中, but it must not trim semantic payloads in the browser to mask repeated repair-round rows
- transport-facing ADP subscribe route accepts protocol-owned task list and error-center subscriptions and obtains initial projections from the injected 运行时 query port
- transport-facing app routes expose SSE subscribe for latest turn and per-turn debug snapshot
- HTTP query and POST command ingress remain compatibility routes; SSE latest-turn subscribe is consumed by WebUI as a display-refresh mirror without owning command dispatch
- per-turn debug SSE is a live subscription and waits for late debug snapshots when turn projection arrives before debug projection
- transport-facing app routes expose POST command ingress for protocol-owned validation and dispatch-port-backed owner routing
- front-end cancel button and Escape key send protocol-owned `CancelTurn` commands through command ingress
- front-end Escape sends `CancelLatestActiveTurn` when submit is in flight but no concrete `turn_id` has reached the browser yet
- command-ingress transport failures must stay explicit at the app boundary and may not collapse into success projection
- the protocol-only transport implementation may be reused by a separate 运行时 host app, but it must remain protocol-only

## Response Mainline

- app boundary renders a protocol-driven WebUI page shell; live content is populated from ADP query/subscribe/command frames plus latest-turn SSE display refresh events
- app boundary serves `/assets/webui/generated/adp-protocol.js` and the WebUI imports `ADP_PROTOCOL_VERSION`, `ADP_HANDSHAKE_CAPABILITY`, `adpQueryOf`, `adpCommandOf`, and `adpSubscribeOf` from that generated module
- app boundary serves protocol-owned query and subscription payloads without becoming a reason/debug truth writer
- app boundary serves 运行时-query-port payloads without importing 运行时, task, 元数据, or error-center owner crates
- app boundary serves task list subscription initial snapshots, error-center initial snapshots, and later 运行时-published projection events without importing task, 元数据, or error-center owner crates
- app boundary serves protocol-owned command dispatch receipts without claiming truth mutation success
- app boundary keeps one selected session at a time, can synthesize a draft session entry before the first turn exists, keeps the selected transcript pinned to that session instead of falling back to global latest turn, and keeps a new draft session visually clean instead of rendering system feedback as chat content
- app boundary serves protocol-owned command dispatch failures and dispatch-task join failures explicitly when the injected dispatch port fails
- ADP subscribe returns an explicit accepted/等待中 state before later turn/debug events, so the WebUI can render 等待中 instead of appearing frozen
- SSE subscribe routes now emit one initial snapshot followed by continuous incremental projection updates over the same connection, and latest-turn subscribe must stay open on blank state until a turn exists
- debug SSE subscribe stays open when a debug snapshot is not available yet, while debug HTTP query remains snapshot-only and returns explicit 404
- front-end debug state distinguishes missing snapshot (`等待调试`) from debug SSE transport errors (`debug stream reconnecting`)
- WebUI submit success path still actively re-queries latest turn truth over ADP after command receipt to cover command-complete-before-browser-subscriber timing
- WebUI command menu actions open the existing protocol-owned new-session, settings, refresh, attachment, tool-registry, and compaction flows; debug/sample prompt injection is not a product affordance
- WebUI composer control strip renders low-noise controls below the composer without creating a second protocol or truth source
- WebUI settings shell renders only coarse first-level entries for 模型, 智能体运行时, 连接, 可观测性, 外观, and 关于 on initial open. 模型 first opens a second-level menu, then 模型服务配置, 模型服务切换与策略, and 模型组 render as mutually exclusive detail pages with explicit back controls. Diagnostics remains a separate 可观测性 detail page rather than a Provider child. Selector draft state follows 权威真源 until the operator edits a selector, provider/模型组 upserts must not clear the 权威投影 fallback selection, and provider test status shows 运行时 owner receipts or explicit provider failures. It does not render decorative 运行时/session status cards, flatten provider internals into first-level entries, parse `~/.freehand/config.toml`, expose credential values, infer provider capability locally, or send unsupported config mutation commands
- WebUI settings renders an 安卓 APK 升级 card for `client=android-webview`; the card calls only `window.FreehandAndroidApkUpdate.check()` and renders status returned through `window.__freehandAndroidApkUpdateStatus`, while desktop/browser sessions show the feature as 仅安卓 App.
- WebUI provider definition save and provider selection switch render field-level validation errors, disable duplicate actions while in flight, and show restart-required after a successful 权威真源 save
- WebUI 模型组 definition save and active group switch render field-level validation errors, disable duplicate actions while in flight, refresh 权威真源 config status, and show restart-required after a successful 权威真源 save
- WebUI Phase 2D desktop 生命周期观察 renders compact TaskBoard, AgentBoard, EventInbox, TaskHistory, and WorkerControl cards from owner query results; worker-control buttons submit `WorkerControl` commands and then re-query 权威真源 before updating the desktop detail drawer
- WebUI 定时任务面板 renders 权威投影 active/completed/cancelled/failed timer rows plus ledger history, disables duplicate schedule/cancel actions while a timer command is in flight, and re-queries `QueryTimerList` after every owner receipt
- WebUI 工具面板 renders 权威投影 tool registry rows, global
  guidance, schemas, examples, read-only/implemented state, scope, and
  Master/Worker exposure flags from `QueryToolRegistry`; repeated refreshes
  replace only the registry projection and never create sessions, tasks,
  timers, or tool executions
- WebUI 搜索面板 renders 权威投影 persisted session search rows
  from `QuerySessionSearch`; parent rows open the persisted Master/user
  session, child Worker matches are indented under that parent, and repeated
  searches never mutate session/task truth.
- WebUI 可观测性 诊断 detail renders 权威投影 `QueryDiagnostics` rows;
  repeated refreshes replace only the 诊断 projection and never create
  sessions, tasks, timers, tool executions, or unredacted log dumps.
- WebUI mobile Agent Dashboard renders a portrait-only Header summary and 工作器子项-task sheet from selected-session 权威真源; sheet open/close is UI-only and preserves selected session, transcript, composer draft, pending submit, scroll anchor, and lifecycle timers
- WebUI mobile drawer close/back behavior is UI-only: the settings and Sessions drawer header remains sticky above scrolled content, `window.__freehandHandleAndroidBack` closes focused inputs/dialogs/dropdowns/sheets/drawers, and Android uses that hook without adding native settings or transcript UI
- WebUI Master/Worker relationships render in the top Header, not the transcript body or debug panel: the dashbar is always compact, its expanded session tree is a dropdown capped at half the viewport with internal scrolling, and every Worker row is keyed by `UiTaskSnapshotProjection.worker_session_id` plus `task_id` rather than by label text, id prefix, or DOM position
- WebUI mobile Agent Dashboard and desktop 生命周期观察 show only lifecycle rows tied to tasks whose `parent_session_id` matches the selected parent session; deleting or switching sessions cannot expose unrelated blocked/review/stale task counts, worker rows, event rows, or history/control details through a global fallback
- WebUI mobile task clicks route through 任务面板 `worker_session_id` and immediately query that 工作器记录 instead of guessing top-level sessions
- WebUI selected-session switches show `Loading conversation` while 权威真源 is being queried; active 工作器记录-not-yet-persisted misses are shown as retryable Worker 等待中 with 任务面板 evidence, while non-retryable pinned query failures still show an explicit refresh-failure card; the clean empty state is reserved for a loaded empty session only
- WebUI mobile Agent header renders the config-owned Worker limit from `QueryConfigStatus`; system Config edits that limit within the 运行时 max `1..=5`, saves only through `UpdateAgentResourceConfig`, and never synthesizes live 智能体面板 rows before restart/process startup
- WebUI attachment tray renders session-scoped draft thumbnails with preview/remove controls; restored turn 元数据 stays visible but is not treated as rehydrated binary payload
- WebUI sends image bytes only through neutral submit 元数据, never appends raw base64 or synthetic attachment placeholders to user text, clears the composer after submit, and keeps submitted text recoverable through local Up/Down input history
- WebUI observes nonterminal-to-terminal turn transitions and invokes `FreehandAndroidNotifications.turnFinished` once per live completed turn when hosted by Android; historical terminal restore does not emit a notification
- WebUI exposes Android-only `window.__freehandOpenAndroidComposerForReadyHost`; after canonical ADP protocol state has refreshed, the bridge opens the valid persisted selected session through `home.open_session` or creates a protocol-owned conversation through `startNewConversation`, then retries composer focus until the DOM focus event proves the composer is actually focused after route `session_detail` and composer geometry are visible. It never auto-sends input, never shows a Home composer, and is inert outside `client=android-webview`.
- WebUI checkpoint panel renders protocol checkpoint summaries from query state and keeps checkpoint files out of app-boundary truth
- WebUI cancel path sends `CancelTurn` for the current nonterminal active turn, clears pending local input only after dispatch, and refreshes protocol truth
- WebUI cancel path uses `CancelTurn` when `turn_id` is known and `CancelLatestActiveTurn` during the submit-in-flight pre-SSE window
- WebUI Escape behavior is stateful: active/submit-in-flight cancels, non-empty composer clears, empty composer first Esc arms rollback, and second Esc calls `RollbackLatestSessionTurn` before refilling composer from the rolled-back user text
- WebUI per-turn actions render under every chat message. Copy writes the visible/user turn text to the clipboard, Edit from here calls persistence-owned `RollbackLatestSessionTurn` repeatedly until the selected logical turn is removed from the effective transcript and then refills the composer with that turn's user prompt, and 新建会话 creates a protocol-owned conversation session before prefilling the composer from the selected turn text.
- front-end script projects protocol-owned ADP `UiQueryResult`, `UiSubscriptionEvent`, and `DebugStateSnapshot` frames into semantic message cards and detail panes, including the user prompt
- front-end script projects latest-turn SSE events through the same turn projection path as ADP subscription updates so SSE refreshes the visible chat bubbles
- front-end script applies the layout shape to `body[data-layout-shape]` and shell `data-layout-shape` on load, resize, orientation change, and `visualViewport` resize without 会修改 selected session, transcript, composer draft, pending submit, scroll anchor, ADP state, or lifecycle timers
- front-end script updates `--composer-clearance` from the real composer card height on load, resize, visual viewport movement, and composer size changes; transcript padding must not be a stale hardcoded estimate when the composer expands or collapses
- front-end script applies UI-only mobile drawer state through `body[data-mobile-drawer]` / shell data attributes; drawer open/close never mutates ADP, session truth, transcript truth, composer draft, pending submit, or lifecycle timers
- front-end script applies `inert` to mobile drawers that are closed or not selected, so off-screen transforms cannot leave hidden session/settings controls keyboard- or accessibility-interactive
- mobile New dialog action buttons share a stable minimum height, equal flex width, and safe-area padding in portrait layouts so cancel/create controls stay aligned above the viewport edge
- New dialog modal transitions replay from a clean state after close, the status line exposes updates to assistive technology, and mobile quick-entry drawer buttons meet the 44px touch target
- front-end script installs a right-swipe recognizer for mobile drawer layouts that opens the existing sessions drawer state and ignores composer, form control, dialog, drawer, and button targets
- front-end script renders persisted session summaries as top-level rows, keeps session CRUD actions on protocol-owned persisted session ids only, treats task/global as display labels derived from protocol cwd, and renders worker task sessions as non-selectable child rows under their parent master session when 任务面板 parent truth is available
- front-end script derives visible conversation turns through one render selector that preserves transcript order, merges the latest same-session turn by replace-or-append, then builds a `RenderConversation` / `RenderTurn` / `RenderRow` model before DOM rendering so a stale transcript cannot hide an in-flight or newly completed continuation turn
- front-end script must keep the selected session pinned during startup, manual session clicks, ADP latest-turn subscription updates, and latest-turn SSE display refresh; 最新活跃轮次 may populate the page only when no selected session exists
- front-end script must reject stale latest-turn and stale `SessionTurns` projections after session-list truth is known, so non-destructive `DeleteSession` 元数据 cannot rehydrate hidden old turn truth into a newly created or refreshed session
- front-end script must not merge latest turn into transcript by `turn_id` alone; replacement requires same `turn_id` and same visible `user_text` so restarted/运行时-reused ordinals cannot overwrite historical rows with a new request
- front-end script renders protocol-projected tool lifecycle status from ADP turn projections so every tool call/result activity shows 等待中, completed, and failed states over the same WebSocket without browser-side same-tool coalescing
- front-end mobile tool blocks render as semantic rows inside assistant bubbles; status is conveyed by color background/state text, ordinary parameter/field details stay compact, and 权威投影 `result:` / `failure:` detail lines remain visible instead of being hidden with secondary parameter rows
- front-end script renders every `ToolSummary` item as a separate visible tool section, even when a later item has the same `tool_call_id`, and clears the composer input immediately after submit while keeping the pending user card visible
- front-end script renders submit/dispatch 等待中 as an animated pending card with elapsed time until a turn projection arrives, then switches visible lifecycle status to tool executing with elapsed time when tool activity is 等待中; timer state is scoped by render lifecycle keys, not a single global model-request clock
- front-end script treats submit command timeout/transport failure as an unverified receipt instead of proof that the backend did nothing: it first refreshes 权威真源 for sessions, selected transcript, latest turn, and lifecycle projections; if the submitted user text or same-parent 任务面板 task truth is materialized it clears pending state, otherwise it preserves the selected/draft session, pending user card, and attachment draft, shows service-truth verification, and tells the operator not to send a duplicate while verification continues
- front-end script renders every user/assistant chat message with a local-time
  label derived from protocol/receipt `created_at` when available; browser
  current time is allowed only for local pending/accepted receipts that do not
  yet have persisted turn truth
- front-end frozen terminal cycle cards preserve semantic content, but a later
  same-key owner transcript projection may replace a frozen card only to fill
  previously missing `created_at`, first-response timing, or total-elapsed
  timing independently; this keeps
  completed cards stable while allowing persisted wait/first-response/total
  timing to become visible after a projection race
- front-end script keeps the submitted user input observable after the composer is cleared: pending submit renders after existing history, pending state is cleared only after the same user text is materialized in visible turn rows, a live turn with no public rows renders an explicit observable 等待中 row, and the latest terminal/interrupted turn remains renderable when selected-session transcript state is empty instead of producing a blank transcript
- front-end script renders protocol-projected typed model request 等待中 as one visible model/request row for `Thinking`, `SchemaRetry`, and `ToolResultContinuation`; provider retry/failover are rendered only as transport substate details inside that model request row, never as a separate `Provider` reasoning-flow row, and selected-session transcript refresh projections with `model_request.transport` render static transport retry/switch detail instead of disappearing
- front-end Header, session list, and Agent observation surfaces derive active session/turn identity from persisted `UiSessionListPageProjection.active_turn_id`, selected transcript `model_request` truth, waiting tool activity, and owner projections for open task/timer lifecycle state; a later same-session terminal transcript turn or a closed owner projection must invalidate an older `active_turn_id`/`ToolPending` observation; when a session is selected, observation may use only that selected session or its parent Master and must not fall back to an unrelated globally active session; selecting an inactive 工作器子项 may still show its parent Master's live observation without relabeling that retry as Worker-owned
- front-end selected-session truth watchdog treats `ToolPending` as lifecycle refresh activity only when the turn still has waiting tool/model activity or the loaded owner projections show a lifecycle-wakeable task status or open timer for the session; after TaskBoard shows no lifecycle-wakeable task and TimerList shows no open timer, the same `ToolPending` turn renders as `等待用户选择`; absent waiting tool/model/task evidence is not enough to keep stale lifecycle wording while timer projection is still loading, normalizes stale visible `Waiting for lifecycle:` terminal-detail prefixes to user-choice wording, stops pretending an Agent/tool lifecycle is running, keeps blocked/failed/cancelled/closed/approved-only child tasks out of Home `正在运行`, and remains user-exitable. Older historical `ToolPending` cards do not keep polling after a later terminal selected turn is visible.
- front-end script renders the main transcript as chat bubbles: user input is a right-aligned user-colored bubble, assistant output is a left-aligned assistant bubble, and tool activity is embedded as a semantic tool block inside the assistant bubble instead of a separate conversation role bubble
- front-end script renders completed/failed tool lifecycle inside the assistant bubble and keeps the status/outcome in the tool block status line; the block body also shows success/failure result text and raw detail lines when owner projection provides them
- front-end tool-result cards render a dedicated `复制结果` action inside the tool body, copying the owner-projected tool summary through the existing clipboard helper without nesting controls inside the expandable header button
- front-end successful final summary cards render `加入记忆` and send the complete summary plus typed session/turn identity through generated `AddToMemory`; tool-result cards remain copy-only and the app never writes local files or session truth
- front-end memory surface renders SQLite-backed search, sort, paging, complete Markdown, and copy actions from `UiQueryResult::Memory`; it sends only generated `QueryMemory`/`AddToMemory` frames and never reads browser storage or filesystem paths
- front-end script renders tool rows as protocol-structured execution cards from `UiToolActivity.display`: action/kind/status in the header, action-to-target primary line, `target`, `parameter_summary`, `result_summary`, `summary`, all structured fields, command, diff before/after, and raw detail lines. JavaScript must not read obsolete `display.result`, classify tools from raw strings, collapse details into hidden sections, or truncate the displayed tool turn.
- front-end conversation projection orders each turn's tool activities before assistant text and the terminal final summary, preserving the observed tool-call -> summary sequence
- front-end script renders model/request continuation rows as italic reasoning text, while assistant/public answer text uses normal body typography
- front-end script renders inactive tool precursor rounds from protocol tool status: completed tool rounds become completed success cards, failed tool rounds become failed cards, and fully terminal continuation rounds remain separate success/failure cards
- front-end lifecycle rendering treats protocol terminal and `ToolPending` truth as authoritative before tool/model/submit-in-flight activity, so a final response cannot remain labeled or animated as `dispatching` while its command receipt is still returning
- front-end script uses explicit lifecycle color semantics: desktop may use left border accents, while phone/tall/tablet portrait surfaces must not draw space-consuming left borders or inset strips; mobile running assistant/tool bubbles use blue-tinted backgrounds, successful/completed bubbles use green-tinted backgrounds, and failed bubbles use red-tinted backgrounds
- front-end script renders 等待中/model continuation animation only for the current live turn; historical completed turns must not keep blinking or reuse the latest turn's lifecycle clock
- front-end script must not call `scrollIntoView` from ordinary render updates; scroll movement is constrained to the conversation scroll host and only when the user-scroll lock is not active
- front-end script must render a neutral non-animated 等待中 state when a turn has no terminal status, no 等待中 tool activity, and no protocol-projected model request; it must not invent `streaming` from text-only or restored inactive state
  - front-end script renders completed/failed tool cards with color-state plus every 权威投影 target, parameter, result, field, command, diff, and detail line; browser-side generic-result filtering and repeated-text compression are forbidden
- front-end script consumes `UiToolActivity.display` structured projection for tool category/action/target/result_summary/summary/fields/diff fields and must not classify tools from raw argument or result strings
- front-end script renders `runtime-turn-N` plus later `runtime-turn-N-rM` round projections as chronological per-round lifecycle cards instead of merging the whole execution into one summary card; each request/model round is wrapped by one `.turn-cycle-card` parent whose child chat messages show user, model 等待中, tool calls, tool results, and final rows; internal 运行时 continuation prompt text may be absent only when the protocol/运行时 projection owner already emits empty `user_text`
- front-end script keeps assistant text inside its owning round card, strips raw `<freehand_completion>` schema blocks from that card, and leaves final user-facing completion summary to the terminal card at the end of the round sequence
- terminal cards use protocol-projected status strings so cancelled and failed terminal states do not render as success
- terminal cards default to summary-only display; evidence, learned notes, and completion reason remain hidden unless WebUI debug details are enabled, and summary text renders through the dedicated final-summary display path instead of generic paragraph rendering
- main conversation cards render only `public_conversation`; internal reasoning, usage, raw completion schema, provider payload, and debug lines stay outside the public stream while the user prompt remains visible
- theme module owns white/black theme switching and is separated from WebUI layout/运行时 scripts
- CLI and WebUI divergence remains a rendering decision only, not a protocol decision
- the app is a render-only transport boundary, not a reasoning or provider boundary

## Error Mainline

- invalid smoke input or missing projection returns explicit app error
- transport/render wiring failures are surfaced explicitly
- ADP transport failures, decode failures, and protocol failure frames are rendered as visible failure cards and status text
- ADP command/query/subscribe request timeouts are explicit UI failures so the composer cannot stay in a silent dispatching state
- generated ADP constructors throw explicit client-side errors for unknown command serde names or frame-class mismatches before dispatching an invalid WebSocket frame
- ADP failure cards must not render ahead of the current conversation timeline; transport failure is secondary to the current turn order
- unknown static assets return explicit 404
- missing compiled Android update sidecar returns explicit 404 from `/android/update.json`, and missing staged Android APK returns explicit 404 from `/android/freehand-android.apk`; the update manifest must not fall back to a hardcoded old `versionCode` that pretends the installed APK is current, and successful manifest/APK responses must be explicitly non-cacheable
- cancel without an active turn clears only local input and does not invent a 运行时 mutation
- attachment dispatch failure preserves draft 元数据/file handles for retry and must not silently drop the selected files
- restored attachment metadata without a current page file handle is visible as metadata-only and must not pretend the binary payload was rehydrated
- transient missing debug snapshots are rendered as pending debug state, not command failure
- debug SSE transport errors are rendered as reconnecting state and must not be hidden behind stale pending state
- dispatch port failures and spawn-blocking join failures both surface explicit HTTP 500 failure payloads
- 运行时 query port failures surface explicit ADP failure frames and do not become app-owned fallback state
- task/error-center subscription initial query failures surface explicit ADP failure frames and do not become app-owned fallback state
- Phase 2D query and WorkerControl command failures render explicit user-visible status errors and must not backfill task/control state from local browser guesses
- 定时任务面板 `QueryTimerList`, `ScheduleTimer`, and `CancelTimer` failures render explicit user-visible timer status errors and must not backfill timer rows from local browser guesses or task state
- 工具面板 `QueryToolRegistry` failures render explicit user-visible
  status errors and must not backfill tool rows from local browser guesses,
  static arrays, or provider capability text
- 搜索面板 `QuerySessionSearch` failures render explicit user-visible
  status errors and must not backfill results from local browser guesses,
  DOM session rows, or worker id prefixes
- 可观测性 诊断 detail `QueryDiagnostics` failures render explicit user-visible
  status errors and must not backfill 诊断 from browser-local guesses,
  absolute filesystem paths, or raw log dumps
- unsupported agent/session/workspace/skills/files/tasks settings controls stay absent until their owners expose read/write contracts; provider definition edits use 权威真源 `UpsertProviderConfig`, provider selection uses 权威真源 `UpdateAgentProviderSelection`, and provider web_search tests use 权威真源 `TestProviderWebSearch`
- settings config query failures render an explicit 不可用 status inside 设置; WebUI must not backfill provider/model values from local guesses
- settings provider definition upsert and provider selection failures render explicit user-visible config errors and must not update local settings state as if save succeeded
- 安卓 APK 升级 bridge absence or JavaScript bridge errors render explicit settings status and do not pretend an APK check started.
- direct reason/provider/node/config coupling is a policy violation, not a fallback path

## Shared Multi-Reference Functions

- `turn_projection_for_client`
  - owner: `crates/freehand-ui-protocol/src/lib.rs`
  - purpose: keep client-specific slave-card visibility inside the protocol owner
  - allowed callers: CLI/WebUI adapters, tests
  - related tests: slave turn subscription smoke
  - why shared: app boundary must not duplicate client-specific projection logic
- `dispatch_port_failure`
  - owner: `crates/freehand-ui-protocol/src/lib.rs`
  - purpose: keep injected dispatch-port failures on one protocol-owned error projection contract
  - allowed callers: protocol-only app adapters, 运行时-backed HTTP hosts, tests
  - related tests: command ingress dispatch failure smoke
  - why shared: app boundary must not invent a second HTTP failure vocabulary

## Function Call Table

| step | symbol path | file path | responsibility | input semantic | output semantic | caller | callee | binding status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 01 | `render_webui_smoke` / `render_webui_smoke_for_client` / `handle_root` | `apps/freehand-server/src/page.rs` / `apps/freehand-server/src/lib.rs` | render protocol-driven WebUI shell and endpoint bindings, including Android WebView initial layout attributes from the root query client | static page request plus optional `client` query | HTML shell with default or Android-pinned first-paint layout | app entrypoint/root route | page module | bound |
| 02 | `assets::asset_response` | `apps/freehand-server/src/assets.rs` | serve split CSS/JS assets and shared logo PNG with explicit content type | asset path | CSS/JS/logo response or 404 | app asset route | embedded assets | bound |
| 03 | `build_webui_router` | `apps/freehand-server/src/lib.rs` | define shared protocol-only HTTP/SSE/ADP/static asset surface | protocol state + dispatch port | router with root/assets/query/subscribe/command/ADP routes | app entrypoint/tests/运行时 host | app router | bound |
| 03a | `handle_android_update_manifest` / `android_update_manifest_body` / `read_android_update_manifest_file` / `handle_android_update_apk` / `android_update_apk_path` | `apps/freehand-server/src/lib.rs` | serve the 守护进程托管 Android update manifest from explicit 运行时 env override or compiled sidecar truth, and serve the signed staged APK artifact from explicit path truth | 运行时 env override or `dist/android/update.json` sidecar + signed staged APK file path | JSON manifest or APK bytes / explicit 404/500 for missing or invalid distribution truth | Android updater / operator | process env + filesystem | bound |
| 03b | `WEBUI_ADP_PROTOCOL_JS` / `asset_response` | `apps/freehand-server/src/assets.rs` | serve the generated ADP protocol constructor module as a version-stamped no-store WebUI asset | `/assets/webui/generated/adp-protocol.js` request | generated ADP constructor module body with current WebUI asset version stamp | browser WebUI module loader | embedded asset table | bound |
| 04 | `handle_command_ingress` | `apps/freehand-server/src/lib.rs` | expose protocol-owned command-ingress transport endpoint backed by an injected dispatch port | HTTP JSON command | HTTP dispatch receipt/failure payload | WebUI transport | protocol owner | bound |
| 05 | `serve_webui_listener` | `apps/freehand-server/src/lib.rs` | serve shared protocol-only router on a listener | TCP listener + protocol state + dispatch port + 运行时 query port + shutdown future | live HTTP/SSE/ADP transport boundary | app entrypoint/tests/运行时 host | app server | bound |
| 06 | `turn_projection_for_client` | `crates/freehand-ui-protocol/src/lib.rs` | gate slave-card visibility by client kind | turn projection + client kind | client-specific projection | app boundary | protocol owner | bound |
| 07 | `initializeThemeToggle` | `apps/freehand-server/assets/theme.js` | switch white/black visual theme only | UI theme choice | body theme class + persisted localStorage setting | WebUI shell | theme module | bound |
| 08 | `subscription_event_stream` / `projection_to_sse_event` | `apps/freehand-server/src/lib.rs` | convert protocol-owned subscription updates into continuous HTTP SSE delivery, including 等待中 subscriptions for late debug snapshots | `UiSubscriptionEvent` receiver + selector | streamed SSE events | subscribe routes | protocol state | bound |
| 09 | `handle_adp_socket` / `handle_adp_connection` / `handle_adp_text_message` | `apps/freehand-server/src/lib.rs` | expose protocol-owned versioned ADP WebSocket frames, require first-frame handshake, and then route WebUI default query/subscribe/command control | versioned ADP WebSocket frames + protocol state + dispatch port + 运行时 query port | handshake accepted/failure, ADP response frames, and subscription events | WebUI shell | shared protocol transport | bound |
| 09a | `handle_adp_query` | `apps/freehand-server/src/lib.rs` | route ADP query frames to the injected 运行时 query port first, then protocol-state query when no 运行时 owner handles the query | ADP query frame + protocol state + 运行时 query port | ADP query result or failure frame | ADP socket route | 运行时 query port / protocol state | bound |
| 10 | `createAdpClient` / `ensureSocket` / `request` / `send` | `apps/freehand-server/assets/webui/app-shell/adp-client.js` | maintain the default WebUI ADP connection, send versioned handshake before requests, validate response `protocol_version`, and route `query_result`, `subscription_accepted`, `subscription_event`, `command_receipt`, and `failure` frames through the shell handler | versioned `UiAdpResponse` JSON frames | visible WebUI state updates or user-safe connection/service failure cards | WebUI shell | daemon `/adp` | bound |
| 10-foundation-protocol-generated | `ADP_PROTOCOL_MANIFEST` / `adpQueryOf` / `adpCommandOf` / `adpSubscribeOf` | `apps/freehand-server/assets/webui/generated/adp-protocol.js` | construct schema-checked query, command, and subscribe requests without handwritten frame-class strings | command or query name plus optional typed payload | generated ADP request payload | WebUI controls | `createAdpClient` | bound |
| 10-foundation-protocol-client | `adpProtocolCallContract` / `createAdpClient` / `settleAdpResponseFrame` | `apps/freehand-server/assets/webui/app-shell/adp-client.js` | bind generated requests to one request-id-correlated WebSocket lifecycle and settle query, command, subscribe, event, and failure responses before presentation callbacks run | generated ADP request plus versioned response frame | resolved/rejected pending request or subscription event for presentation | generated protocol constructors and WebSocket | `handleAdpFrame` presentation callback | bound |
| 10-foundation-root | `initializeMobileWebui` | `apps/freehand-server/assets/webui/bootstrap.js` | compose the top-level WebUI shell from registered routes, surfaces, layout, theme, and shared presentation-state contracts | protocol projection and browser viewport | initialized `ui_surface` shell with no browser-owned business truth | thin WebUI entry module | registered app-shell and surface contracts | bound |
| 10-foundation-surface | `surfaceContracts` | `apps/freehand-server/assets/webui/app-shell/surface-registry.js` | validate and register the exact split model/view/control responsibility contracts consumed by the WebUI shell | imported split surface contracts | schema-valid immutable registry with unique surface and DOM-root identities | shell bootstrap | `validateSurfaceContractRegistry` and split surface modules | bound |
| 10-foundation-shared | `renderSharedState` | `apps/freehand-server/assets/webui/app-shell/shared-states/view.js` | render explicit validated loading, empty, error, or confirmation models and reject missing action handlers | validated `SharedUiStateModel` plus required action handler for actionable state | shared state DOM projection | Home dashboard surface | browser DOM | bound |
| 11 | `refreshTurn` / `refreshAllProtocolState` / `refreshConfigStatus` / `syncSettingsProviderForm` / `syncProviderSelectionControls` / `updateProviderSelectionDraftFromControls` / `renderSettingsProviderRegistry` / `submitProviderConfigUpdate` / `submitProviderSelectionUpdate` / `renderSettingsNavigation` / `openSettingsPage` / `renderMessages` / `conversationTimelineItems` / `timelineItemChatCards` / `pendingSubmitTimelineIndex` / `acceptedSubmitReceiptTimelineIndex` / `firstCycleTurnIndex` / `renderTurnMatchesSession` / `renderTurnCreatedAtMs` / `conversationTurnsForRender` / `buildConversationRenderModel` / `buildRenderTurn` / `buildRenderRows` / `buildToolActivityRenderRow` / `buildModelRequestRenderRow` / `turnChatCards` / `userChatBubble` / `assistantChatBubble` / `appendChatMessageTime` / `timestampToMilliseconds` / `localChatTimeLabel` / `appendTurnActionBar` / `editAndRerunFromTurn` / `rollbackEffectiveTranscriptThroughTurn` / `startNewConversationFromText` / `renderToolSection` / `toolSummaryBodyLines` / `pushToolLine` / `splitToolDetailLines` / `turnIsCurrentLiveTurn` / `renderModelHasLiveLifecycle` / `logicalSessionTurns` / `isInternalRuntimePrompt` / `stripFreehandCompletionBlock` / `normalizePublicConversation` / `modelRequestKind` / `modelRequestTimingKey` / `classifyLayoutShape` / `applyLayoutShape` / `viewportDimensionsForLayout` / `setMobileDrawer` / `closeMobileDrawer` / `syncMobileDrawerForLayout` / `handleBackNavigationIntent` / `installMobileSessionSwipeGesture` / `setComposerFocused` / `openNewSessionDialog` / `submitNewSessionDialog` / `renderCommandStatus` / `renderSettingsShell` / `showInspectorPanel` / `refreshDebug` / `refreshAfterAmbiguousSubmitFailure` / `pendingSubmitAcceptedByTaskTruth` / `acceptedSubmitReceiptChatCards` / `acceptedSubmitReceiptForRender` / `startAmbiguousSubmitRecoveryPolling` / `stopAmbiguousSubmitRecoveryPolling` / `submitUserInput` / `setSessionList` / `setSessionTranscript` / `workerChildSessionsForParent` / `workerChildSessionForSessionId` / `selectedParentSessionSummary` / `renderSessionRelationHeader` / `renderSessionTree` / `renderWorkerSessionNavigation` / `renderSessionAgentGroup` / `renderSessionWithWorkerChildren` / `renderSessionItem` / `startNewConversation` / `startNewTask` / `renameCurrentSession` / `deleteSelectedSessions` / `rollbackLatestSessionTurn` | `apps/freehand-server/assets/webui/legacy-monolith.js` | consume ADP query/subscription turn payloads and 运行时 config projection, submit provider definition upserts and active provider selection switches through protocol commands, manage selected and draft sessions, keep settings provider selection draft inactive until an operator selector edit and reset it after 权威真源 save so initial refresh/upsert cannot clear fallback truth, keep settings root/detail navigation as UI-only state with one visible `data-settings-page` at a time and explicit back routes, split global conversation creation from cwd-bound task creation through a New dialog, expose protocol-owned Home multi-select/remove, SessionDetail current-session rename, double-Esc rollback, and per-turn edit-from-here rollback for persisted sessions only, preserve transcript order, keep selected-session transcript pinned ahead of 最新活跃轮次 during startup and manual session clicks, merge selected-session transcript with the latest same-session turn before rendering, build turn-scoped render models before DOM rendering, insert pending/accepted submit cards before matching same-session live/recent turns, preserve each render turn's `createdAt`, append a local-time label and action bar to every user/assistant chat message from owner/receipt timestamp truth, clear ambiguous submit state only from materialized transcript truth or same-parent 任务面板 task `created_at` truth, render an accepted service receipt card instead of a clean empty session while transcript truth is still absent, render chronological per-round chat bubbles without grouping same-ordinal 运行时 rounds into a superseded final card, render every protocol-projected request, response, tool activity, tool result line, and terminal row without browser-side text/tool-id coalescing or generic-result filtering, strip raw completion schema from assistant cards, reset timers when typed model-request phase changes, restrict animation to the current live turn, map viewport width plus aspect ratio into DOM layout shape attributes without 会修改 protocol/session state, keep mobile portrait session/detail/设置 surfaces in UI-only overlay drawers outside normal conversation flow, keep drawer headers sticky so close remains reachable during long 设置/session scrolls, expose a WebUI-owned Android back hook that blurs focused fields then closes dialogs/Header tree/Agent sheet/mobile drawers before native exit, open the session drawer with a presentation-only content-area right-swipe gesture, render persisted sessions inside one expandable Master agent group while excluding 任务面板-parented worker temporary sessions from the global drawer, render the Header dashbar/inline session tree from persisted session 元数据 plus `UiTaskSnapshotProjection.parent_session_id`/`attached_session_ids`/`worker_session_id` schema truth and expose those fields only as DOM verification anchors, render settings provider registry and primary/fallback selectors from protocol/运行时 config projection plus 权威真源 save results without config file writes or credential values, keep mobile focused composer state as presentation-only data attributes, keep user input right-aligned, assistant/final/model rows left-aligned, embed tool rows inside assistant bubbles, and keep missing debug snapshots pending instead of failed | `UiAdpResponse` frames + config status query result + provider registry/upsert/selection receipts/failures + debug snapshot/pending state + command dispatch receipt + protocol turn `created_at` / local receipt time + viewport size + mobile drawer button/设置 button/gesture events + settings nav/back clicks + Header session tree toggle/clicks + Android/browser back intent + per-turn action button clicks | `RenderConversation` / `RenderTurn` / `RenderRow` plus pending/accepted receipt timeline items -> DOM chat bubbles with `.chat-message-time` and `.turn-action-bar` + one active settings page + Header session dashbar/inline tree keyed by schema-projected session/task ids + command status + persisted-only global session list + layout/drawer data attributes + sticky mobile drawer header state | WebUI shell | ADP protocol frames + CSS layout rules | bound |
| 11h-adp-generated | `ADP_PROTOCOL_MANIFEST` / `adpQueryOf` / `adpCommandOf` / `adpSubscribeOf` | `apps/freehand-server/assets/webui/generated/adp-protocol.js` | provide generated WebUI constructors that validate ADP frame class before legacy and split surface modules send query, command, or subscribe payloads | serde command name plus optional payload | protocol-shaped query, command, or subscribe payload accepted by `createAdpClient` | legacy-monolith.js and split WebUI surface controls | ADP WebSocket client | bound |
| 11h-adp-verifier-shared | `adpVerifierRequest` / `requireQueryVariant` / `requireSessionListPage` | `scripts/lib/adp-verifier-client.mjs` | own verifier-side ADP v4 authentication, handshake, request-id correlation, timeout/failure settlement, and strict SessionListPage observation for online scenario scripts | endpoint/token options plus query or command payload and response frames | resolved query result/receipt, explicit handshake/socket/failure rejection, or schema-valid SessionListPage projection | all app.webui-smoke online verifier wrappers | daemon `/adp` WebSocket | bound |
| 11n-module-session-detail-recovery | `recoveredHistoricalWorkerFailureTurnIds` / `historicalFailureRecoveredLifecycle` / `historicalFailureRecoveredRows` / `historicalRecoveryProjectionChanged` | `apps/freehand-server/assets/webui/surfaces/session-detail/recovery.js` | classify only an older failed Worker turn as recovered when the same ordered Worker transcript has a later success and TaskBoard owner truth is closed; keep raw failure rows available in debug mode and refresh the frozen same-key cycle when recovery truth or recovery debug mode changes without mutating turn/task truth | ordered `SessionTurns` terminal statuses plus selected Worker `TaskBoard.status` | recovered historical turn ids, terminal recovery presentation lifecycle, and normal-mode recovery summary rows | `buildConversationRenderModel` / `buildRenderTurn` | SessionDetail cycle-card projection | bound |
| 11p | `setSessionTranscriptPage` / `loadOlderSessionTranscriptPage` / `renderConversationBottomButton` / `refreshSelectedSession` / `sessionTranscriptCacheEntry` | `apps/freehand-server/assets/webui/legacy-monolith.js` | own selected-session transcript paging, transient per-session cache, older-page incremental loading, and the bottom-to-latest control; merge incoming page turns ahead of existing cache for both Latest and Older, cache-hit refresh only after the Latest page has loaded, and switch the bottom button between live reasoning animation and terminal return-to-bottom arrow | `UiSessionTurnsPage` projections plus selected-session refresh intent plus scroll position | ordered selected-session transcript cache, one-page boundary queries, anchored older history, and bottom button live/terminal state | WebUI shell query/subscription callbacks and scroll listeners | `QuerySessionTurnsPage` through ADP plus browser DOM scroll | bound |
| 11r | `refreshSessions` / `appendSessionListPage` / `loadOlderSessionListPage` / `scheduleSessionListPrefetch` / `renderSessions` | `apps/freehand-server/assets/webui/legacy-monolith.js` | query the owner-backed Latest session-list page with limit 24, append explicit Older pages through the returned cursor, prefetch only when idle, expose 加载更早, preserve request ordering, filter top-level persisted sessions without timestamp/sort inference, and keep partial list absence from proving deletion; explicit delete receipts remain deletion evidence until refresh | `UiSessionListPageProjection`, operator load/prefetch/delete intent, and current page state | bounded Home/drawer history projection with an explicit older control, stale-response rejection, unavailable-session facts not blocking other rows, and no full transcript restore for listing | WebUI startup/foreground/session mutation callbacks | `QuerySessionListPage` through generated ADP constructors | bound |
| 11h | `renderMobileHomeDashboard` / `mobileHomeHistorySessions` / `compareSessionSummaryForDisplay` / `sessionSummaryTimeRank` / `taskKeepsSessionLifecycleRunning` / `activeSessionsForHome` / `renderMobileHomeActiveList` / `renderMobileHomeSessionList` / `mobileHomeHistorySessionNode` / `mobileHomeSessionButton` / `mobileHomeSessionMeta` / `mobileHomeSessionTimeLabel` / `renderHomeDashboardSurface` / `createHomeDashboardModel` / `createHomeSessionRow` / `renderToolsRegistrySurface` / `renderTimerDashboardSurface` / `createAdpClient` / `renderSettingsNavigation` / `openSettingsPage` | `apps/freehand-server/assets/webui/legacy-monolith.js` | implement Phase 1 mobile UI tree presentation only: consume runtime-filtered top-level sessions without a second internal-id classifier, render the home dashboard as exactly two page sections (`正在运行` and `历史会话`) with running/static list classes, keep the two lists disjoint, keep every lifecycle-wakeable live session visible without a fixed cap while blocked-only/task-terminal sessions stay in history, and never use a drawer or floating session overlay as the main session list; split Home, Tools, and Timer rendering into dedicated surface modules plus an ADP client shell, keep Timer and New Session as corner-entry surfaces outside the home body, keep settings first open as six coarse entries only, and use explicit drilldown/back navigation for detail pages | `QuerySessionListPage` projection plus selected-session/task projections plus settings nav/back click state | top-level session list free of `worker-task-*`, `master-lifecycle-*`, and `master-timer-*` by runtime owner contract; mobile home 正在运行 list excluding blocked-only stale lifecycle sessions; mobile home persisted 历史会话 list ordered by display time with no duplicate ids from the running list; no absolute/fixed session tree overlay; settings root without provider/model/运行时/detail controls, duplicate implementation audit tree, or extra settings status card; mutually exclusive detail page with hollow green/orange/red status markers and no phone-local storage-management concepts | WebUI shell | runtime-filtered `ui.protocol` projections plus reviewed prototype contract | bound |
| 11i | `openTimerDashboard` / `refreshTimerDashboard` / `renderTimerDashboard` / `renderTimerDashboardList` / `renderTimerDashboardHistory` / `scheduleTimerFromForm` / `cancelTimer` | `apps/freehand-server/assets/webui/legacy-monolith.js` | render the mobile 定时任务面板 only from the top-right Timer entry using 权威投影 `TimerList` truth, submit relative/absolute/recurring schedule forms through `ScheduleTimer`, cancel active timers through `CancelTimer`, refresh `QueryTimerList` after receipts, and keep timer schedules independent from 任务中心, browser truth, and the home dashboard | 定时任务面板 button/form fields plus selected source session id plus `UiQueryResult::TimerList` and timer command receipts | 权威真源 定时任务面板 rows, schedule/cancel status, visible timer history, or explicit timer error status | WebUI 定时任务面板 dialog | 运行时 timer owner projections and commands through ADP | bound |
| 11j | `syncModelGroupSelectionControls` / `syncSettingsModelGroupForm` / `renderSettingsModelGroupRegistry` / `fillSettingsModelGroupForm` / `submitModelGroupConfigUpdate` / `submitModelGroupSelectionUpdate` | `apps/freehand-server/assets/webui/legacy-monolith.js` | render 模型组 registry and active selection from 权威投影 `QueryConfigStatus` truth, submit 模型组 definition/selection changes through protocol commands, and re-query config status after owner receipts without parsing config files or storing config truth in the browser | config status projection plus 模型组 form/select fields and save/switch clicks | 权威真源 settings 模型组 registry, active-group selector, restart-required save status, or explicit validation/dispatch error | WebUI settings shell | 运行时 config owner projections and commands through ADP | bound |
| 11k | `openToolsDashboard` / `refreshToolsDashboard` / `renderToolsDashboard` / `renderToolRegistryGuidance` / `renderToolRegistryList` / `renderToolRegistryCard` | `apps/freehand-server/assets/webui/legacy-monolith.js` | render the 工具面板 from 权威投影 `ToolRegistry` truth, display registry guidance/schema/examples/scope/exposure, and refresh through `QueryToolRegistry` without executing tools or storing registry truth in the browser | 工具面板 button, refresh click, and `UiQueryResult::ToolRegistry` | 权威真源 工具面板 rows, guidance, schema previews, exposure badges, or explicit registry error status | WebUI mobile home 工具 entry | 运行时 tool registry owner projection through ADP | bound |
| 11l | `openSessionSearchDashboard` / `submitSessionSearch` / `renderSessionSearchDashboard` / `renderSessionSearchResult` / `openSessionSearchResult` | `apps/freehand-server/assets/webui/legacy-monolith.js` | render persisted-session 搜索 from 权威投影 `SessionSearch` truth, submit query text through `QuerySessionSearch`, show 工作器子项 matches only under parent sessions, and open parent sessions without browser-local search/index truth | 搜索 quick-entry click, query form submit, and `UiQueryResult::SessionSearch` | 权威真源 persisted session search rows, nested 工作器子项 matches, explicit empty/error status, or selected parent session switch | WebUI mobile home 搜索 entry | 运行时 session search owner projection through ADP | bound |
| 11m | `renderSettingsDiagnostics` / `renderDiagnosticLogRow` / `refreshDiagnosticsStatus` | `apps/freehand-server/assets/webui/legacy-monolith.js` | render the 可观测性 诊断 detail from 权威投影 `Diagnostics` truth, refresh through `QueryDiagnostics`, and show only redacted log 元数据/tail rows without browser-local log reads | Diagnostics refresh click and `UiQueryResult::Diagnostics` | 权威真源 诊断 rows, summary, 运行时-home label, explicit empty state, or explicit 诊断 error status | WebUI settings 可观测性 details | 运行时 诊断 owner projection through ADP | bound |
| 11e | `timelineItemCycleCard` / `cycleCardFromChatCards` / `cycleCardKey` / `reconcileCycleCardFragments` / `frozenCycleCardNeedsAuthoritativeMetadataRefresh` / `frozenCycleCardNeedsLifecycleClassificationRefresh` / `cycleCardMetaForTimelineItem` / `cycleCardIsTerminal` | `apps/freehand-server/assets/webui/legacy-monolith.js` | wrap every timeline item in exactly one chronological `.turn-cycle-card` parent, key pending/accepted receipts by submit id but every real protocol turn by session plus turn id, update only the same nonterminal turn cycle in place, reuse frozen terminal cycle nodes so later turns cannot appear to mutate old cards, and allow same-key frozen replacement only to fill missing owner `created_at`, first-response timing, total-elapsed timing, or a narrowed `ToolPending` lifecycle classification after owner projections load | `RenderConversation` timeline item with protocol turn, pending submit, accepted receipt, 加载中, or failure state | one uniquely keyed parent cycle card containing the existing user/assistant/tool/final chat-message rows and data attributes for order/freeze/timing verification | WebUI shell | ui.protocol projection + CSS layout rules | bound |
| 11f | `androidApkUpdateManifestUrlForDisplay` / `androidApkUpdateBridge` / `androidApkUpdateBridgeAvailable` / `androidApkUpdatePhaseInFlight` / `optionalAndroidApkUpdateNumber` / `normalizeAndroidApkUpdateStatus` / `receiveAndroidApkUpdateStatus` / `renderAndroidApkUpdateSettings` / `requestAndroidApkUpdateCheck` | `apps/freehand-server/assets/webui/legacy-monolith.js` | render the Android-only APK update settings card, display the native bridge-selected manifest URL, call only the native WebView bridge for manual checks, normalize native status payloads, and keep desktop/browser sessions explicitly 不可用 | settings button click plus native `ApkUpdateStatus` payload | visible APK update source/status/phase and disabled/enabled check button | WebUI settings shell | Android WebView JavaScript bridge | bound |
| 11b | `loadAttachmentDrafts` / `persistAttachmentDrafts` / `addAttachmentFiles` / `renderAttachmentTray` / `attachmentsForSubmit` / `textWithSubmittedAttachmentDisplay` / `clearCurrentAttachments` | `apps/freehand-server/assets/webui/legacy-monolith.js` | manage session-scoped image drafts, preview/remove thumbnails, emit neutral submit attachment metadata/base64, clear after accepted command, and render persisted 元数据 without rehydrating raw bytes | selected session + browser `File` handles/base64 + metadata-only restored turns | attachment tray DOM + `SubmitUserInput.metadata.attachments` + metadata-only transcript display | WebUI control layer | UI protocol submit 元数据 | bound |
| 11g | `maybeNotifyAndroidTurnFinished` | `apps/freehand-server/assets/webui/legacy-monolith.js` | notify the Android platform bridge once when an observed live turn transitions to terminal | prior nonterminal projection + terminal turn projection | `FreehandAndroidNotifications.turnFinished` payload | turn projection update | Android notification bridge | bound |
| 11q | `androidComposerHostReady` / `composerCanReceiveAndroidInput` / `focusAndroidComposerAfterReadyLayout` / `scheduleAndroidComposerFocus` / `openAndroidComposerForReadyHost` / `window.__freehandOpenAndroidComposerForReadyHost` | `apps/freehand-server/assets/webui/legacy-monolith.js` | expose the Android-only ready-host composer entry; wait for canonical ADP protocol state, open the valid persisted selected session or protocol-owned new conversation, and retry composer focus until the DOM focus event proves the composer is focused after route and composer geometry are visible | Android WebView ready-host bridge call after `refreshAllProtocolState` | route `session_detail` with proven focused visible composer, or explicit Android input-prep failure status | Android `MainActivity` startup readiness | `home.open_session` route dispatch / `startNewConversation` / browser DOM focus retry | bound |
| 11a | `toolSummaryBody` / `toolSemanticLines` / `derivePublicConversation` | `apps/freehand-server/assets/webui/legacy-monolith.js` | render protocol-projected `display` fields for embedded tool blocks without reclassifying raw tool text | `UiToolActivity.display` | low-noise semantic tool block body | WebUI shell | ui.protocol projection | bound |
| 11d | `turnLifecycleForRender` / `liveTurnStatus` / `renderTurnMeta` / `turnIsCurrentLiveTurn` / `turnIsWaitingForModelResponse` / `toolPendingRepresentsLifecycle` / `toolPendingStatusLabelForTurn` / `normalizeWaitingUserToolPendingTerminalBody` / `buildModelRequestRenderRow` / `modelRequestStaticStatus` / `modelRequestTransport` / `modelRequestTransportPhase` / `modelRequestTransportLabel` | `apps/freehand-server/assets/webui/legacy-monolith.js` | render every protocol-projected model/tool activity round, keep selected-transcript `model_request` rows statically visible when they are not current live, render provider retry/failover only as model-request transport substate, make terminal or `ToolPending` truth override outstanding submit/tool/model activity, and classify `ToolPending` as lifecycle only with waiting tool/model/open task/timer owner evidence after owner projections load while normalizing stale visible lifecycle-detail prefixes on user-choice ToolPending rows | `UiTurnProjection` lifecycle, `model_request`, `model_request.transport`, tool activity, TaskBoard/TimerList owner projections, and render state | visible per-round tool/model rows with no historical animation, no terminal response mislabeled as dispatching, no provider retry displayed as its own reasoning-flow row, and no closed/user-choice `ToolPending` mislabeled as a running lifecycle | WebUI shell | ui.protocol projection + owner projections through ADP | bound |
| 11c | `applyPhase2QueryResult` / `refreshPhase2Status` / `renderPhase2Dashboard` / `sessionSummaryById` / `compareTurnIdOrder` / `compareSessionTurnPosition` / `sessionTurnCandidates` / `latestSessionTurnMatching` / `latestClosedTurnForSession` / `closedTurnObsoletesObservableTurn` / `closedTurnObsoletesSessionSummary` / `activeTurnForSession` / `sessionLiveObservation` / `globalLiveSessionObservation` / `liveObservationLine` / `buildMobileAgentDashboardModel` / `renderMobileAgentSummaryStrip` / `renderMobileAgentSheet` / `renderMobileAgentTaskList` / `renderSessionWorkerRail` / `taskDurationLabel` / `headerWorkerRailNeedsClock` / `renderSystemAgentResourceConfig` / `submitAgentResourceConfigUpdate` / `setMobileAgentSheetOpen` / `renderWorkerSessionNavigation` / `switchConversationSession` / `selectedSessionIsLoading` / `loadingConversationBubble` / `workerTranscriptWaitingBubble` / `sessionRefreshFailureBubble` / `clearSessionRefreshState` / `renderSessionRefreshFailure` / `returnToSessionListFromRefreshError` / `dismissSessionRefreshError` / `selectedWorkerTranscriptRefreshRetryable` / `scheduleSessionRefreshRetry` / `returnToParentSession` / `renderTaskBoardProjection` / `renderAgentBoardProjection` / `agentNavigationUrl` / `phase2AgentLabel` / `sessionAgentGroupLabel` / `workerOrdinalFromAgentId` / `openWorkerTaskSession` / `renderEventInboxProjection` / `renderTaskHistoryProjection` / `renderWorkerControlProjection` / `sendWorkerControl` | `apps/freehand-server/assets/webui/legacy-monolith.js` | query TaskBoard, AgentBoard, EventInbox, TaskHistory, WorkerControl, ConfigStatus, SessionList, and selected transcript truth through the existing ADP/运行时 owner path; render compact read-only 生命周期观察 cards; expose live 等待中/retry identity as owning session plus turn in the Header/session/Agent surfaces, including the parent Master observation while an inactive 工作器子项 is selected, while a later same-session terminal transcript turn or closed owner projection invalidates stale `active_turn_id`/`ToolPending` observation and selected sessions never fall back to unrelated global active sessions; derive mobile Header lifecycle buckets from selected-session 任务面板/智能体面板 truth plus config-owned Worker limit; render the Header Worker rail as one compact row per current child Worker task with status plus owner-timestamp duration and inline click-to-expand details; label Worker resources and each independent host's direct-session group from stable owner-backed Agent identity so a Worker namespace can never be mislabeled as Master; when no session is selected, render every configured Agent as one mobile sheet row and navigate through loopback-only `local_web_url` or remote-only `relay_web_url`; when a session is selected, replace that directory with the current-session complete 工作器子项-task list with no fixed cap or global-history fallback; expose each Worker task card's `data-task-id` and 权威投影 `data-worker-session-id` for one-by-one inspection; click a child task through 权威投影 `worker_session_id`, clear the prior transcript, render an explicit 加载中 state while the pinned `QuerySessionTurns` request is in flight, treat owner-confirmed active 工作器记录-not-persisted misses as retryable 等待中 with 任务面板 evidence instead of connection failure, refresh large transcript payloads without browser-side semantic trimming, and open the 工作器会话 without deriving a session id in the browser; render non-retryable selected-session refresh failures as session-local cards with 新建会话/返回会话列表/忽略错误 exits instead of global ADP connection failure; expose an explicit return to the exact parent 主控会话 and direct sibling-Worker switching; render `1..=5` Worker-limit mutation only in system Config and route it through the config-owner protocol command | `UiQueryResult` Phase 2 projections + SessionList active-turn projection + selected-session turn/model-request projection + config status projection + Header Worker rail expand intent + worker-control/resource button intent + Agent-directory/Worker-task/parent-return click intent + mobile sheet open/close/back intent | 生命周期观察 cards + live session/turn identity + mutually exclusive mobile Agent directory or selected-session Worker task sheet + Header Worker status/duration rail + explicit Agent/Worker/Master navigation + selected Agent transcript, selected-session 加载中 state, retryable 工作器记录 等待中 state, or session-local refresh-failure card with explicit exits + system Config resource control + user-safe worker-control/resource receipt/status text | WebUI detail drawer, system Config, mobile Agent/Worker sheet, and session relationship Header | 运行时 owner projections through ADP | bound |
| 12 | `handle_query_checkpoints` / `refreshCheckpoints` | `apps/freehand-server/src/lib.rs` / `apps/freehand-server/assets/webui/legacy-monolith.js` | serve compatibility checkpoint query and render read-only checkpoint summaries from ADP protocol state by default | protocol checkpoint snapshot | checkpoint snapshot + secondary inspector cards | WebUI shell | ui.protocol state | bound |
| 13 | `cancelActiveTurn` | `apps/freehand-server/assets/webui/legacy-monolith.js` | send `CancelTurn` or `CancelLatestActiveTurn` over ADP for the nonterminal active protocol turn from button or Escape key; terminal/no-active turns clear only local pending input | latest nonterminal protocol turn id | ADP command receipt + refreshed projection or local clear status | WebUI shell | daemon `/adp` | bound |
| 14 | `handle_command_ingress` | `apps/freehand-server/src/lib.rs` | keep dispatch-port and spawn-blocking join failures explicit at the HTTP compatibility transport boundary | dispatch port error or join error | explicit HTTP 500 failure payload | command ingress | protocol failure mapper | bound |
| 16 | `initial_adp_subscription_projection` | `apps/freehand-server/src/lib.rs` | serve initial ADP subscription snapshot including 运行时-backed task list and error-center projections | subscription command plus protocol state plus 运行时 query port | optional UI projection or explicit failure | `handle_adp_subscribe` | `UiRuntimeQueryPort::query_runtime` / `UiProtocolState::query` | bound |

## Sync Status Against Code

- app boundary now renders a usable WebUI shell instead of a minimal text-only smoke
- theme code is split into `assets/theme.css` and `assets/theme.js`
- WebUI layout/protocol-consumer code is split into `assets/webui.css` and `assets/webui.js`
- WebUI Android ready-host composer entry now opens an existing valid persisted session or protocol-owned new conversation after startup refresh and focuses the visible composer; no Home composer or native Android input fallback is used
- WebUI shell now advertises `data-adp-endpoint="/adp"`; the front-end opens a WebSocket, sends protocol_version=4 handshake first, waits for `handshake_accepted`, and uses user-facing 连接/服务 wording for failures
- WebUI command/query/subscribe payload construction now imports `adpQueryOf`, `adpCommandOf`, and `adpSubscribeOf` from the Rust-generated `webui/generated/adp-protocol.js` module; app code must not handwrite ADP frame-class constructors
- WebUI shell now renders a compact session rail whose New controls open one dialog for either protocol-persisted global conversation creation or cwd-bound task creation; `/new` opens the same dialog instead of directly creating a local-only draft
- app boundary now serves protocol-only HTTP query and SSE subscribe smoke routes from a reusable protocol-only library surface
- app boundary now serves protocol-only POST command ingress dispatch-receipt/failure smoke route from that shared transport surface
- HTTP/POST routes remain compatibility transport surfaces; WebUI JS uses versioned ADP handshake + command/query/subscription truth and EventSource for latest-turn SSE display refresh
- app boundary now surfaces explicit command-ingress dispatch-port failures and dispatch-task join failures instead of collapsing them into success
- app boundary now serves static embedded CSS/JS/logo assets through an explicit 404ing route
- app boundary now serves Android update manifest/APK routes for the Android client; manifest version fields come from 运行时 env overrides when present, otherwise from compiled `dist/android/update.json`, and missing manifest/APK artifacts fail explicitly instead of reporting a false current version
- 运行时 host reuse now happens through injected state and dispatch port, not by duplicating transport behavior
- protocol-owned client-specific projection helper exists and is now a shared owner boundary for the app smoke
- subscribe routes now keep one SSE connection open and stream later matching updates after the initial snapshot
- debug subscribe route now also keeps one SSE connection open when the first debug snapshot is not available yet
- WebUI submit path still explicitly refreshes latest turn truth over ADP after a successful command receipt
- WebUI submit path forwards selected session id and optional selected cwd through `SubmitUserInput`; global conversations are first persisted through `CreateSession` without cwd and may submit using 运行时 default cwd, while task sessions are persisted through `CreateSession` with cwd
- WebUI checkpoint panel now refreshes protocol checkpoint summaries and sends explicit rewind commands without parsing 运行时 files
- WebUI 取消 button and Escape key now send `CancelTurn` through protocol command ingress instead of only clearing local input
- WebUI cancel path now covers the submit-in-flight window with `CancelLatestActiveTurn`
- WebUI stop/continue online proof is covered by `scripts/verify-webui-stop-continue-online.mjs`: fixed-session provider activity must render before 取消, cancellation must project `cancelled` and clear live state before the next retry backoff completes, and the same persisted session must accept a later successful turn without creating a random session
- WebUI tool cards now render protocol-projected 等待中/completed/failed lifecycle states from ADP turn projection truth
- WebUI success/failure diagnostic prompts remain available through slash commands and shortcuts, while persistent Success/Failure composer buttons are intentionally absent
- WebUI composer control strip now exposes multi-image attachment selection, preview/remove pool, selected-session refresh, cwd input, and read-only model selector controls through protocol-owned submit 元数据
- WebUI settings shell is landed with coarse top-level entries only on first open: 模型, 智能体运行时, 连接, 可观测性, 外观, and 关于. 模型服务配置, provider switching/strategy, and 模型组 stay behind 模型 drilldown; 诊断日志 sit under 可观测性; Worker-limit edit form sits under 智能体运行时; 安卓 APK 升级 bridge card sits under 连接; unimplemented rows stay marked partial or placeholder until 权威真源 contracts exist
- WebUI settings shell includes an Android-only APK update trigger/status card; it is 不可用 in desktop browsers and delegates all check/download/install semantics to the Android bridge owner.
- WebUI settings shell now queries `QueryConfigStatus` and renders 权威真源 provider registry, 模型组 registry, current primary/fallback values, and active 模型组 id; provider/model-group selection draft state is created only by selector edits, provider definition saves route through `UpsertProviderConfig`, 模型组 definition saves route through `UpsertModelGroupConfig`, active provider selection changes route through `UpdateAgentProviderSelection`, active 模型组 selection changes route through `UpdateAgentModelGroupSelection`, and credential values are not rendered
- WebUI settings provider save/switch now submits ADP owner commands, shows validation errors or restart-required success, re-queries 权威真源 config projection, and still avoids raw API-key values/direct config writes
- WebUI layout shape classifier and CSS shape rules are landed for phone portrait, tall phone, phone landscape, tablet portrait, tablet landscape, foldable unfolded, and desktop large; the classifier only writes DOM data attributes and does not mutate protocol/session state
- WebUI root route now pins `?client=android-webview` first paint to `tablet_portrait` on the server-rendered body and shell while leaving normal Web roots unpinned for browser classification
- WebUI image drafts are session-scoped, render as thumbnails with preview/remove controls, send base64 only in current-submit 元数据, clear after successful command receipt, and remain for retry after dispatch failure; persisted transcript cards retain 元数据 only
- WebUI mobile session drawer now supports content-area right-swipe opening and renders persisted sessions under expandable agent groups; task/global labels are display-only and derive from protocol cwd
- WebUI no longer normalizes same-tool lifecycle rows by `tool_call_id`; every protocol-projected `ToolSummary` item renders as its own visible section while 等待中 cards animate with local elapsed timers and submit clears the input field immediately while retaining pending state in the conversation stream
- WebUI submit timeout/transport failure first re-queries 权威真源; materialized transcript or same-parent 任务面板 task truth clears pending state and continues from the refreshed conversation, while unverified receipts preserve the pending conversation state and selected/draft session instead of clearing to an empty session; the browser shows explicit service-truth verification status and keeps the input recoverable through history
- WebUI chat bubbles render `.chat-message-time` from protocol/receipt
  timestamp truth through `appendChatMessageTime`,
  `timestampToMilliseconds`, and `localChatTimeLabel`
- WebUI new/empty selected sessions now render a clean empty conversation prompt and do not project previous latest-active turns or generic 等待中-data system feedback into the selected session
- WebUI session-list truth now gates all turn/transcript render inputs after the list has loaded, including latest-active query results, latest-turn ADP/SSE updates, and `SessionTurns`; old latest-active turn truth from a non-destructively removed session cannot reappear in a new clean session
- WebUI release online verification removes existing sessions through ADP before creating a new conversation, then proves the new draft has no selected turn and no leaked prior transcript
- WebUI submit/dispatch and tool-wait lifecycle states now both refresh once per second so users can see where the turn is blocked and how long it has waited
- WebUI foreground recovery now re-queries sessions, the selected transcript, config, and lifecycle owner projections on `pageshow`, window `focus`, `online`, and visible `visibilitychange` events; this refresh is throttled and preserves selected/pending session state instead of clearing to a clean empty conversation.
- WebUI render updates preserve the operator's manual scroll position once the transcript is not near bottom; bottom-following resumes only after the operator returns to the bottom or sends a new prompt
- WebUI transcript bottom clearance is driven by the measured composer height instead of fixed padding, preventing the latest row from being hidden by fixed mobile composer chrome
- WebUI online verifier captures `scrollProof` snapshots proving manual upward scroll is preserved across refresh, bottom-pinned refresh keeps the latest message visible above the composer, and user-facing session list text hides internal `master-lifecycle-*` sessions
- WebUI model-request 等待中 state now comes from typed `UiTurnProjection.model_request.kind` and refreshes once per second with elapsed wait time; phase changes reset local timing
- WebUI selected-session transcript refresh now renders nonterminal protocol `model_request` rows even when the row is not classified as current live; only the current live turn receives elapsed animation
- WebUI completed/failed tool cards now show protocol-projected semantic target/body while status/outcome stays in the status line, and tool-complete-to-next-model 等待中 renders as its own timed lifecycle state
- WebUI 等待中/model continuation cards are now gated to the current live turn so restored history cannot show fake animation after execution has stopped
- WebUI inactive text-only or restored turns now render as neutral 等待中/active state instead of fake streaming; animation requires submit-in-flight, 等待中 tool activity, or protocol-projected model request truth
  - WebUI tool terminal state now uses green/red compact state dots but does not compress repeated title/summary/body text or filter generic success-result items
- WebUI tool cards now render `display.action`, `display.summary`, `display.parameter_summary`, `display.result_summary`, all `display.fields`, `display.diff`, and raw detail lines; category parsing stays in `tool.display`, not in JavaScript, and success/failure result text remains visible when projected by the owner
- WebUI selected-session transcript display now preserves chronological round bubbles for ids such as `runtime-turn-47` and `runtime-turn-47-r2`; the old all-rounds-in-one visible summary card is intentionally removed
- WebUI selected-session transcript rendering now uses `conversationTurnsForRender` plus `buildConversationRenderModel` to fold the latest same-session turn into the selected transcript while keeping lifecycle animation scoped to the current live turn
- WebUI conversation rendering is cycle-timeline ordered: persisted turns keep protocol transcript order, every real turn uses `turn:<session_id>:<turn_id>` even when multiple rounds share one submit id, pending/accepted receipt updates use `submit:<session_id>:<submit_id>`, and local pending/accepted submit receipts are inserted before the matching submit/live/recent same-session turn instead of being appended after all protocol turns. The DOM verification unit is the parent `.turn-cycle-card`: it carries `data-cycle-key`, `data-cycle-kind`, `data-turn-id`, `data-session-id`, `data-submit-id`, `data-live`, `data-terminal`, `data-frozen`, and durable timing attributes when 权威真源 provides them, while child `.chat-message` rows remain the visible phase details. Terminal cycles are frozen for semantic content, with a narrow same-key refresh if the first frozen render missed `created_at`/timing or if a `ToolPending` card narrows from owner-unknown `waiting_lifecycle` to owner-proven `waiting_user`; nonterminal non-live cycles stay replaceable so late 权威真源 can update them. This keeps the user request before its model-等待中/tool/result rows and prevents later rounds from overwriting an earlier card through a shared submit id.
- WebUI startup and manual session clicks now keep the selected session pinned; 最新活跃轮次 query/subscribe/SSE updates do not replace the transcript when they belong to another session
- WebUI inactive tool precursor cards now project completed tools as success cards and failed tools as failed cards instead of falling back to neutral 等待中 cards
- WebUI execution card CSS now uses explicit blue/green/red status borders for running/success/failed states
- WebUI main transcript now renders chat bubbles: user bubbles are right-aligned, assistant bubbles are left-aligned, tool activity is embedded in the assistant bubble as a semantic block, and model-wait/retry continuation rows use italic reasoning typography
- WebUI mobile v3 CSS intentionally overrides desktop card weight for phone/tablet-portrait shapes: flat app bar, continuous conversation stream, lightweight assistant/user bubbles without left border strips, compact tool semantics with color-block backgrounds, fixed collapsed composer, and focused composer space reserved only for the compact input height
- WebUI session rail now exposes Rename, Remove via `DeleteSession`, and double-Esc rollback through ADP commands; archive/restore controls are intentionally absent from the WebUI surface
- WebUI `/new` opens the New dialog instead of directly creating a local-only draft; 新会话 persists session 元数据 through `CreateSession`, and the old selected-session/no-turns system card stays absent from the chat stream
- WebUI assistant cards stay within their own round and strip raw `<freehand_completion>` blocks; final user-facing completion content remains in the terminal Final row at the bottom of the round sequence
- WebUI Final rows now extract the complete terminal `Summary` block and use a dedicated source-format-preserving summary renderer, while phone focused composer keeps attachment/CWD/model/status detail collapsed out of the main input surface
- WebUI missing-debug race is locked by pending-state rendering plus late-debug ADP subscription coverage; ADP failure frames render as visible failure cards/status instead of stale pending
- WebUI user-facing labels, status rows, failure cards, and diagnostic prompts must not expose `ADP`; ADP remains an internal protocol/debug/automation term only
- WebUI hidden diagnostic failure prompts must follow the active runtime Master tool boundary: current-cwd workspace tools such as `read_file` should succeed, so failure samples must use cross-cwd workspace-boundary or 不可用-tool calls
- app dependency boundary is intended to remain protocol-only and must not import reason/provider/node/config semantics
- app query transport now requires accepted ADP handshake before query frames, accepts an injected `UiRuntimeQueryPort`, and still does not import 运行时/task/error-center semantics
- app query transport derives a typed `UiQueryAccessScope` from loopback Host versus remote/Relay access before calling the scoped runtime query port; it does not rewrite ConfigStatus payloads after projection
- app subscription transport now uses the injected `UiRuntimeQueryPort` for task list and error-center initial snapshots while keeping later updates on protocol subscription events
- WebUI Phase 2D status drawer now renders TaskBoard, AgentBoard, EventInbox, TaskHistory, and WorkerControl from 运行时 owner query results; it does not write browser-persisted task/control truth
- WebUI WorkerControl buttons now send `WorkerControl` commands through ADP and re-query the owner projection; the visible receipt hides internal feature ids
- WebUI Worker historical-failure presentation now uses the SessionDetail recovery module to reconcile ordered `SessionTurns` with TaskBoard closed truth; it refreshes a frozen same-key cycle when recovery first becomes owner-proven or debug mode changes, without deleting or rewriting the failed turn
- WebUI 定时任务面板 now queries `QueryTimerList`, schedules timers through `ScheduleTimer`, cancels through `CancelTimer`, renders only through the top-right Timer entry, and keeps schedule/history truth in the 运行时 timer owner instead of browser or home dashboard state
- WebUI 工具面板 now queries `QueryToolRegistry`, renders owner
  projection rows and guidance, proves no local `web_search` function row, and
  keeps session-specific tool turns in conversation rendering instead of this
  registry/设置 surface
- WebUI 搜索面板 now queries `QuerySessionSearch`, renders owner
  persisted session rows plus nested 工作器子项 matches, and keeps global
  session history free of worker/subagent temporary sessions
- generated wiki must be regenerated from `docs/mainline-calls/app.webui-smoke.json` when this function-map truth changes

## 2026-08-07 Local Multi-Agent Identity Closeout

- `applyAdpQueryResult` accepts owner-backed ConfigStatus and calls the existing `renderSessions` projector after state.configStatus is written, closing the SessionList-before-ConfigStatus render edge. Positive/negative labels and stock Chrome four-origin isolation are recorded in `artifacts/webui-online/mobile-ui-tree-phase1-20260807T143011-8206`.

## 2026-07-26 Mobile Modular Surface Split Update

- owner: `app.webui-smoke` / `apps/freehand-server`
- route/surface shell:
  - `apps/freehand-server/assets/webui.js` remains a thin module bootstrap.
  - `apps/freehand-server/assets/webui/bootstrap.js` installs theme/layout/edge/surface contracts and loads `legacy-monolith.js` only as compatibility shell.
  - `apps/freehand-server/assets/webui/app-shell/edge-registry.js` is the explicit route/control edge registry.
  - `home.open_agent_directory` opens the configured-Agent directory only from Home with no selected session; `session.open_agent_sheet` remains the selected-session Worker-task edge.
  - `apps/freehand-server/assets/webui/app-shell/route-controller.js` is the only route mutation helper.
  - `apps/freehand-server/assets/webui/app-shell/adp-client.js` owns ADP socket/request plumbing for the compatibility shell.
- split surfaces:
  - Home: `surfaces/home-dashboard/{index,model,view,controls}.js`
  - SessionDetail route/session switch and recovered-history presentation: `surfaces/session-detail/{index,controls,recovery}.js`
  - Tools registry: `surfaces/tools-registry/{index,view,controls}.js`
  - Timer dashboard: `surfaces/timer-dashboard/{index,view,controls}.js`
  - Settings shell/diagnostics/navigation: `surfaces/settings/{index,view,diagnostics}.js`
  - SessionSearch: `surfaces/session-search/{index,view}.js`
  - Memory: `surfaces/memory/{index,view}.js`
  - NewSession: `surfaces/new-session/{index,controls}.js`
- compatibility boundary:
  - `legacy-monolith.js` may still host transcript rendering and older owner-projection wiring during strangler migration, but the above surface entry/render/control paths are now delegated to modules and served as independent assets.
  - Controls dispatch `dispatchWebUiEdge` / route controller; modules receive owner projection/query/command callbacks from the compatibility shell and must not create browser-local session/task/tool/timer/config truth.
