# Wiki: `app.webui-smoke`

Generated from `docs/mainline-calls/app.webui-smoke.json`. Do not edit by hand.

- owner crate: `apps/freehand-server`
- owner module: `apps/freehand-server/src/lib.rs`
- function map: `docs/function-maps/app.webui-smoke.md`
- generated wiki: `docs/wiki/app.webui-smoke.md`
- test design: `docs/testing/app.webui-smoke.md`

## Resource Operation Backlinks

- ui_projection.post_android_turn_finished_notification

## Request Mainline

- app boundary receives a minimal WebUI smoke invocation
- app boundary consumes `freehand-ui-protocol` projection truth only
- app boundary stays decoupled from reason, provider, node, and config semantics
- app boundary may render query snapshot, debug snapshot, and separate slave-card projection without owning protocol semantics
- app boundary serves a real WebUI shell that loads protocol-consumer JS and split CSS assets
- app boundary keeps theme assets separate from WebUI layout assets
- front-end default control/status path is ADP WebSocket `/adp` for query, subscribe, and command frames
- front-end session rail exposes protocol-owned rename, remove via DeleteSession, and double-Esc rollback controls without storing session truth locally; archive/restore are intentionally absent from WebUI
- front-end selected empty-session rendering binds empty `SessionTurns` to the selected session, clears previous active turn and debug state, and suppresses generic waiting-data system cards so new sessions do not leak prior session content
- front-end keeps success and failure scenario prompts as hidden slash/keyboard diagnostic affordances without rendering persistent sample buttons in the composer
- front-end composer control strip exposes attachment buttons, preview, selected-session refresh, cwd input, model selector, slash commands, and keyboard shortcuts as UI-layer affordances over ADP
- front-end provider definition form submits UpsertProviderConfig through ADP without switching the selected provider; the primary/fallback selector submits UpdateAgentProviderSelection; provider test buttons submit TestProviderWebSearch; config mutations re-query QueryConfigStatus for restart-required saved projection, and WebUI never reads or writes ~/.freehand/config.toml directly or accepts raw API-key values
- front-end Settings includes an Android-only APK update card that calls only window.FreehandAndroidApkUpdate.check and renders status returned through window.__freehandAndroidApkUpdateStatus
- front-end layout classifier maps viewport width plus aspect ratio into presentation-only shape attributes without changing protocol/session truth
- server-rendered root shell pins `client=android-webview` first paint to `tablet_portrait` on body and shell layout attributes so Android WebView does not flash the default desktop grid before JS refinement
- front-end mobile portrait layout keeps the conversation workspace as the primary surface; session CRUD/list and debug/config detail surfaces are hidden in explicit overlay drawers opened from mobile controls and must not occupy the normal document flow
- front-end conversation scrolling keeps a user-scroll lock so live render updates follow the bottom only when the operator is already near bottom or a local submit explicitly forces the current turn into view
- front-end composer clearance is measured from the actual composer card height and exported as `--composer-clearance` so the latest transcript row stays visible above fixed or sticky composer chrome
- front-end mobile session drawer can be opened by a right-swipe gesture from the main interface content area without mutating ADP/session truth, selected session, transcript state, composer draft, pending submit, scroll anchor, or lifecycle timers
- front-end session drawer renders persisted sessions as agent -> session hierarchy, with task/global labels derived from protocol cwd and CRUD still routed by protocol session id
- front-end attachment drafts are scoped by selected session, persist metadata only in browser storage, submit image payloads through SubmitUserInput.metadata.attachments, clear on command receipt, and remain available for retry after dispatch failure
- Android WebView turn-finished notifications are triggered only from observed live protocol turn projection transitions from nonterminal to terminal, not from restored historical terminal turns
- front-end submit timeout or transport failure first refreshes owner truth for sessions, selected transcript, latest turn, and lifecycle projections before deciding whether the submit receipt is still unverified
- transport-facing app routes expose HTTP query for latest active turn and per-turn debug snapshot
- transport-facing app routes expose HTTP query for runtime-owned checkpoint summary projection
- transport-facing app routes expose daemon-hosted Android update distribution: /android/update.json consumes explicit runtime env override or compiled dist/android/update.json sidecar truth, and /android/freehand-android.apk serves the staged APK artifact
- transport-facing ADP query route can call an injected protocol-owned runtime query port for read-only owner projections such as task list/history and error-center metadata before using protocol-state snapshots
- front-end Phase 2D status drawer queries TaskBoard, AgentBoard, EventInbox, TaskHistory, and WorkerControl through the existing ADP/runtime query path; the browser only caches transient render data and must not persist task/control truth
- front-end Worker labels derive from Worker-only resource ordinals; the Master AgentBoard row must not offset configured Worker labels
- transport-facing app routes expose SSE subscribe for latest turn and per-turn debug snapshot
- HTTP query and POST command ingress remain compatibility routes; SSE latest-turn subscribe is consumed by WebUI as a display-refresh mirror without owning command dispatch
- per-turn debug SSE is a live subscription and waits for late debug snapshots when turn projection arrives before debug projection
- transport-facing app routes expose POST command ingress for protocol-owned validation and dispatch-port-backed owner routing
- the protocol-only transport implementation may be reused by a separate runtime host app, but it must remain protocol-only
- front-end cancel button and Escape key send protocol-owned CancelTurn commands through command ingress only for nonterminal active turns
- front-end Escape sends CancelLatestActiveTurn when submit is in flight but no concrete turn_id has reached the browser yet
- command-ingress transport failures must stay explicit at the app boundary and may not collapse into success projection
- transport-facing ADP subscribe route can accept protocol-owned task list and error-center subscriptions and obtain initial snapshots from the injected runtime query port

## Response Mainline

- app boundary renders a protocol-driven WebUI page shell; live content is populated from ADP query/subscribe/command frames plus latest-turn SSE display refresh events
- app boundary serves protocol-owned query and subscription payloads without becoming a reason or debug truth writer
- app boundary serves runtime-query-port payloads without importing runtime, task, metadata, or error-center owner crates
- app boundary serves protocol-owned command dispatch receipts without claiming truth mutation success
- app boundary serves protocol-owned command dispatch failures and dispatch-task join failures explicitly when the injected dispatch port fails
- ADP subscribe returns an explicit accepted/waiting state before later turn/debug events, so the WebUI can render waiting instead of appearing frozen
- SSE subscribe routes emit one initial snapshot followed by continuous incremental projection updates over the same connection, latest-turn subscribe keeps waiting when no turn exists yet, and debug subscribe keeps waiting when no debug snapshot exists yet
- WebUI submit success path actively re-queries latest turn truth over ADP after command receipt to cover command-complete-before-browser-subscriber timing
- WebUI foreground recovery re-queries sessions, the selected transcript, config, and lifecycle owner projections on pageshow, focus, online, and visible visibilitychange events while preserving selected and pending session state
- WebUI success/failure diagnostic prompts populate the composer through slash commands or keyboard shortcuts, then the operator can send them through the normal ADP submit path; these prompts must not appear as persistent composer buttons
- WebUI composer control strip renders low-noise controls below the composer without creating a second protocol or truth source
- WebUI provider registry renders configured providers, provider web_search configured/effective route diagnostics, provider web_search live-test trigger/status, provider definition save and provider selection switch render field-level validation errors, disable duplicate actions while in flight, re-query owner-backed config status, preserve owner fallback selection until an explicit selector edit, and show restart-required after a successful owner-backed save
- WebUI Android APK update card shows daemon update source, disables duplicate clicks while native update phases are in flight, and displays current, available, downloading, downloaded, installer_started, already_checking, or failed status returned by the Android bridge
- Android update manifest route returns env override or compiled sidecar JSON, while missing or invalid manifest truth is explicit and never reports a hardcoded old current version
- WebUI Phase 2D status drawer renders compact TaskBoard, AgentBoard, EventInbox, task history, and WorkerControl cards from owner query results; worker-control buttons submit WorkerControl commands and then re-query owner truth before updating the drawer
- WebUI forwards selected cwd through SubmitUserInput.cwd and keeps draft-session empty state out of the chat stream
- WebUI selected empty sessions render a clean empty prompt and ignore latest-active turns from other sessions until the selected session has its own transcript or live turn
- WebUI Escape behavior is stateful: active/submit-in-flight cancels, non-empty composer clears, empty composer first Esc arms rollback, and second Esc calls `RollbackLatestSessionTurn` before refilling composer from the rolled-back user text
- WebUI attachment tray renders session-scoped draft metadata, image thumbnails, preview, remove controls, and file-handle availability; restored metadata stays visible but is not treated as rehydrated binary payload
- WebUI sends image payloads only in SubmitUserInput.metadata.attachments, clears the session draft after successful command receipt, and restores the composer text while retaining draft attachments after dispatch failure
- WebUI renders submitted image attachments from UiTurnProjection.attachments as metadata-only lines after refresh without raw/base64 payloads
- front-end script projects protocol-owned ADP `UiQueryResult`, `UiSubscriptionEvent`, and `DebugStateSnapshot` frames into semantic message cards and detail panes, including the user prompt
- front-end script projects latest-turn SSE events through the same turn projection path as ADP subscription updates so SSE refreshes the visible chat bubbles
- root route renders Android WebView requests with server-side initial `tablet_portrait` body and shell layout attributes, while normal browser roots remain unpinned for JS/browser classification
- front-end script applies layout shape to body and shell data attributes on load, resize, orientationchange, and visualViewport resize without mutating selected session, transcript, composer draft, pending submit, ADP state, or lifecycle timers
- front-end script updates `--composer-clearance` from the real composer card height on load, resize, visual viewport movement, and composer size changes
- front-end script applies UI-only mobile drawer state through body and shell data attributes; drawer open/close never mutates ADP, session truth, transcript truth, composer draft, pending submit, or lifecycle timers
- front-end debug state distinguishes missing snapshot (debug pending) from debug SSE transport errors (debug stream reconnecting)
- front-end script renders protocol-projected tool lifecycle status from ADP turn projections so every tool call/result activity shows waiting, completed, and failed states over the same WebSocket without browser-side same-tool coalescing
- front-end script renders every ToolSummary item as a separate visible tool section, including repeated tool_call_id updates, renders waiting cards with animation and local elapsed timers, and clears the composer input immediately after submit while keeping the pending user card visible
- front-end script clears pending submit state after an ambiguous submit failure when refreshed owner truth materializes the submitted user text or a same-parent TaskBoard task created after the submit start; otherwise it preserves pending input, selected/draft session, and attachment drafts with explicit service-truth verification status
- front-end script renders submit and dispatch waiting as an animated pending card with elapsed time, then switches to tool executing with elapsed time when a waiting tool activity is visible
- front-end script renders protocol-projected model request waiting as a visible reasoning card that clearly shows the request has been sent and the UI is waiting for model response; only the current live turn animates elapsed time, while selected transcript refresh rows render static waiting/retrying/switching status
- front-end script renders completed or failed tool result detail in the same tool card and renders tool-complete-to-next-model waiting as an animated elapsed-time card
- front-end script preserves the operator's manual scroll position when the transcript is not near bottom and does not call scrollIntoView from ordinary render updates
- front-end script renders a neutral non-animated waiting state when a turn has no terminal status, no waiting tool activity, and no protocol-projected model request; it must not invent streaming from text-only or restored inactive state
- front-end script renders runtime-turn-N plus later runtime-turn-N-rM round projections as chronological per-round lifecycle cards instead of merging the whole execution into one summary card; each request/model round is wrapped by one .turn-cycle-card parent whose child chat messages show user, model waiting, tool calls, tool results, and final rows; pending/accepted receipts reconcile by submit id, every real protocol turn reconciles by session id plus turn id even when several rounds share one submit id, and terminal cards freeze by reusing the existing DOM node
- front-end Header, session list, and Agent observation surfaces derive active session/turn identity from persisted SessionList active-turn status plus selected transcript model_request truth, so waiting/retry state names its owning session and turn; a later same-session terminal transcript turn invalidates an older active_turn_id or ToolPending observation; when a session is selected, observation may use only that selected session or its parent Master and must not fall back to an unrelated globally active session; selecting an inactive Worker child may still show its parent Master's live observation without relabeling that retry as Worker-owned
- front-end script projects checkpoint summaries into a secondary inspector card and sends explicit rewind commands through command ingress
- main conversation cards render only `public_conversation`; internal reasoning, usage, raw completion schema, provider payload, and debug lines stay outside the public stream while the user prompt remains visible
- theme module owns white and black theme switching and is separated from WebUI layout/runtime scripts
- CLI and WebUI divergence remains a rendering decision only, not a protocol decision
- the app is a render-only transport boundary, not a reasoning or provider boundary
- front-end cancel handling clears pending local input only after sending CancelTurn for the current nonterminal active turn and refreshing protocol truth
- terminal cards use protocol-projected status strings so cancelled and failed terminal states do not render as success
- front-end cancel handling uses CancelTurn when turn_id is known and CancelLatestActiveTurn during submit-in-flight pre-SSE window
- ADP task list subscription returns accepted plus initial runtime task list projection and later runtime-published task list events

## Error Mainline

- invalid smoke input or missing projection returns explicit app error
- transport or render wiring failures are surfaced explicitly
- ADP transport failures, decode failures, and protocol failure frames are rendered as visible failure cards and status text
- ADP command/query/subscribe request timeouts are explicit UI failures so the composer cannot stay in a silent dispatching state
- unknown static assets return explicit 404
- checkpoint query uses protocol state only and must not parse runtime checkpoint files in the app boundary
- blank latest-turn subscribe does not fail early; it keeps waiting for the first matching turn
- dispatch port failures and spawn-blocking join failures both surface explicit HTTP 500 failure payloads
- runtime query port failures surface explicit ADP failure frames and do not become app-owned fallback state
- direct reason, provider, node, or config coupling is a policy violation, not a fallback path
- cancel without an active turn clears only local input and does not invent a runtime mutation
- attachment dispatch failure preserves draft metadata and page-held file handles for retry and must not silently drop the selected files
- restored attachment metadata without a current page file handle is visible as metadata-only and must not pretend the binary payload was rehydrated
- transient missing debug snapshots are rendered as pending debug state, not command failure
- debug SSE transport errors are rendered as reconnecting state and must not be hidden behind stale pending state
- task/error-center subscription initial query failures surface explicit ADP failure frames instead of app-owned fallback state
- Phase 2D query and WorkerControl command failures render explicit user-visible status errors and must not backfill task/control state from local browser guesses
- settings provider definition upsert and provider selection failures render explicit user-visible config errors and must not update local Settings state as if save succeeded
- Android APK update bridge absence, desktop/browser use, malformed payloads, JavaScript bridge call failures, missing update sidecar truth, or missing staged APK artifacts render explicit failures and must not pretend an update check started or that the APK is current

## Shared Multi-Reference Functions

- `turn_projection_for_client`
  - owner: `crates/freehand-ui-protocol/src/lib.rs`
  - purpose: keep client-specific slave-card visibility inside the protocol owner
  - allowed callers: CLI or WebUI adapters, tests
  - related tests: slave turn subscription smoke
  - why shared: app boundary must not duplicate client-specific projection logic
- `dispatch_port_failure`
  - owner: `crates/freehand-ui-protocol/src/lib.rs`
  - purpose: keep injected dispatch-port failures on one protocol-owned error projection contract
  - allowed callers: protocol-only app adapters, runtime-backed HTTP hosts, tests
  - related tests: command ingress dispatch failure smoke
  - why shared: app boundary must not invent a second HTTP failure vocabulary

## Function Call Table

| step | symbol path | file path | responsibility | input semantic | output semantic | caller | callee | source resource | target resource | resource operation | binding status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 01 | `render_webui_smoke / render_webui_smoke_for_client / handle_root` | `apps/freehand-server/src/page.rs / apps/freehand-server/src/lib.rs` | render protocol-driven WebUI shell and endpoint bindings, including Android WebView initial layout attributes from the root query client | static page request plus optional `client` query | HTML shell with default or Android-pinned first-paint layout | app entrypoint or root route | page module |  |  |  | bound |
| 02 | `assets::asset_response` | `apps/freehand-server/src/assets.rs` | serve version-addressed split CSS, JS, and shared logo assets with explicit content type and no-store cache policy so Android WebView cannot retain stale dashboard code or stale branding | asset path | CSS, JS, or logo response or 404 | app asset route | embedded assets |  |  |  | bound |
| 03 | `build_webui_router` | `apps/freehand-server/src/lib.rs` | define shared protocol-only HTTP, SSE, ADP, and static asset surface | protocol state plus dispatch port plus runtime query port | router with root, assets, query, subscribe, command, and ADP routes | app entrypoint, tests, or runtime host | app router |  |  |  | bound |
| 03a | `handle_android_update_manifest / android_update_manifest_body / read_android_update_manifest_file / handle_android_update_apk / android_update_apk_path` | `apps/freehand-server/src/lib.rs` | serve Android APK update distribution from explicit runtime env override or compiled sidecar truth plus staged APK artifact truth | GET /android/update.json or GET /android/freehand-android.apk plus FREEHAND_ANDROID_* env and dist/android/update.json or staged APK path | validated update manifest JSON, APK bytes, or explicit 404/500 for missing or invalid distribution truth | Android updater or operator | process env and filesystem |  |  |  | bound |
| 04 | `handle_command_ingress` | `apps/freehand-server/src/lib.rs` | expose protocol-owned command-ingress transport endpoint backed by an injected dispatch port | HTTP JSON command | HTTP dispatch receipt or failure payload | WebUI transport | protocol owner |  |  |  | bound |
| 05 | `serve_webui_listener` | `apps/freehand-server/src/lib.rs` | serve shared protocol-only router on a listener | TCP listener plus protocol state plus dispatch port plus runtime query port plus shutdown future | live HTTP, SSE, and ADP transport boundary | app entrypoint, tests, or runtime host | app server |  |  |  | bound |
| 06 | `turn_projection_for_client` | `crates/freehand-ui-protocol/src/lib.rs` | gate slave-card visibility by client kind | turn projection plus client kind | client-specific projection | app boundary | protocol owner |  |  |  | bound |
| 07 | `initializeThemeToggle` | `apps/freehand-server/assets/theme.js` | switch white and black visual theme only | UI theme choice | body theme class plus persisted localStorage setting | WebUI shell | theme module |  |  |  | bound |
| 08 | `subscription_event_stream / projection_to_sse_event` | `apps/freehand-server/src/lib.rs` | convert protocol-owned subscription updates into continuous HTTP SSE delivery, including waiting subscriptions for late debug snapshots | `UiSubscriptionEvent` receiver plus selector | streamed SSE events | subscribe routes | protocol state |  |  |  | bound |
| 09 | `handle_adp_socket / handle_adp_connection` | `apps/freehand-server/src/lib.rs` | expose protocol-owned ADP WebSocket frames for WebUI default query, subscribe, and command control | ADP WebSocket frames plus protocol state plus dispatch port plus runtime query port | ADP response frames and subscription events | WebUI shell | shared protocol transport |  |  |  | bound |
| 09a | `handle_adp_query` | `apps/freehand-server/src/lib.rs` | route ADP query frames to the injected runtime query port first, then protocol-state query when no runtime owner handles the query | ADP query frame plus protocol state plus runtime query port | ADP query result or failure frame | ADP socket route | runtime query port or protocol state |  |  |  | bound |
| 10 | `ensureAdpSocket / requestAdp / handleAdpFrame` | `apps/freehand-server/assets/webui.js` | maintain the default WebUI ADP connection and route query_result, subscription_accepted, subscription_event, command_receipt, and failure frames | `UiAdpResponse` JSON frames | visible WebUI state updates or failure cards | WebUI shell | daemon `/adp` |  |  |  | bound |
| 11 | `refreshTurn / refreshSessions / refreshConfigStatus / syncSettingsProviderForm / syncProviderSelectionControls / updateProviderSelectionDraftFromControls / renderSettingsProviderRegistry / submitProviderConfigUpdate / submitProviderSelectionUpdate / renderMessages / appendTurnActionBar / editAndRerunFromTurn / rollbackEffectiveTranscriptThroughTurn / startNewConversationFromText / renderToolSection / toolSummaryBodyLines / toolSemanticLines / pushToolLine / splitToolDetailLines / logicalSessionTurns / stripFreehandCompletionBlock / normalizePublicConversation / classifyLayoutShape / applyLayoutShape / viewportDimensionsForLayout / setMobileDrawer / closeMobileDrawer / syncMobileDrawerForLayout / handleBackNavigationIntent / installMobileSessionSwipeGesture / setComposerFocused / openNewSessionDialog / submitNewSessionDialog / renderCommandStatus / refreshDebug / refreshAfterAmbiguousSubmitFailure / pendingSubmitAcceptedByTaskTruth / acceptedSubmitReceiptChatCards / acceptedSubmitReceiptForRender / startAmbiguousSubmitRecoveryPolling / stopAmbiguousSubmitRecoveryPolling / submitUserInput / setSessionList / workerChildSessionsForParent / workerChildSessionForSessionId / selectedParentSessionSummary / renderSessionRelationHeader / renderSessionTree / renderWorkerSessionNavigation / renderSessionAgentGroup / renderSessionWithWorkerChildren / renderSessionItem / renameSelectedSession / deleteSelectedSessions / rollbackLatestSessionTurn` | `apps/freehand-server/assets/webui.js` | consume ADP query and subscription turn payloads, render semantic/tool/debug cards, submit provider definition upserts and active provider selection switches through protocol commands, keep Settings provider selection draft inactive until an operator selector edit and reset it after owner-backed save so initial refresh/upsert cannot clear fallback truth, expose protocol-owned session rename/remove/rollback controls plus per-turn edit-from-here rollback and new-session prefill actions, open the New dialog for CreateSession-backed global conversation or cwd-bound task creation, clear ambiguous submit state only from materialized transcript truth or same-parent TaskBoard task created_at truth, render an accepted service receipt card instead of a clean empty session while transcript truth is still absent, render every protocol-projected request, response, tool activity, tool result line, and terminal row without browser-side text/tool-id coalescing or generic-result filtering, keep result/failure tool detail lines visible on mobile instead of hiding owner-projected execution errors with secondary-row CSS, apply aspect-ratio layout shape attributes, keep mobile portrait session/detail/settings surfaces in UI-only overlay drawers outside normal conversation flow, keep mobile drawer headers sticky so close remains visible while long drawer content scrolls, expose an Android/WebUI back hook that blurs focused form controls and closes dialogs, Header tree dropdowns, Agent sheets, or mobile drawers before app-level exit/navigation, open the session drawer with a presentation-only content-area right-swipe gesture, render persisted sessions inside one expandable Master agent group and TaskBoard-parented worker sessions as indented temporary child rows without owning session truth, render the Header dashbar/dropdown session tree from persisted session metadata plus UiTaskSnapshotProjection parent_session_id, attached_session_ids, worker_session_id, and task_id schema truth, expose DOM data-session-id and data-task-id only as verification projections of those protocol fields, project live waiting/retry identity as owning session id plus turn id in the Header/session/Agent surfaces, keep mobile focused composer state presentation-only, and render settings provider registry plus primary/fallback selectors from protocol/runtime config projection plus owner-backed save results without config file writes or credential values, and keep missing debug snapshots pending instead of failed | `UiAdpResponse` frames plus debug snapshot/pending state plus config status/provider-registry/upsert/selection frames plus command dispatch receipt plus selected session UI intent plus viewport size plus mobile drawer button and gesture events plus Header tree toggles plus Android/browser back intent and per-turn action button clicks | DOM message blocks, settings cards/forms/status, sticky mobile drawer header and close state, persisted session lists with indented worker child rows, Header session dashbar/dropdown tree keyed by schema-projected session and task ids, live session/turn observation data attributes, New dialog state, composer refill after rollback receipt, command status, layout shape attributes, composer focus attributes, and mobile drawer state attributes | WebUI shell | ADP protocol frames |  |  |  | bound |
| 11e | `timelineItemCycleCard / cycleCardFromChatCards / cycleCardKey / reconcileCycleCardFragments / frozenCycleCardNeedsAuthoritativeMetadataRefresh / cycleCardMetaForTimelineItem / cycleCardIsTerminal` | `apps/freehand-server/assets/webui.js` | wrap every conversation timeline item in exactly one chronological .turn-cycle-card parent, key pending/accepted receipts by submit id but every real protocol turn by session plus turn id, update only the same nonterminal turn cycle in place, freeze terminal cycles by reusing their existing DOM node so later turns append as new cards instead of mutating old cycles, and allow same-key frozen replacement only to fill missing owner created_at, first-response timing, or total-elapsed timing independently | RenderConversation timeline item with protocol turn, pending submit, accepted receipt, loading, or failure state | one uniquely keyed parent request-cycle card containing existing user, assistant, model-waiting, tool-call, tool-result, and final chat-message rows plus order/freeze data attributes | WebUI shell | ui.protocol projection plus CSS layout rules |  |  |  | bound |
| 11f | `androidApkUpdateManifestUrlForDisplay / androidApkUpdateBridge / androidApkUpdateBridgeAvailable / androidApkUpdatePhaseInFlight / optionalAndroidApkUpdateNumber / normalizeAndroidApkUpdateStatus / receiveAndroidApkUpdateStatus / renderAndroidApkUpdateSettings / requestAndroidApkUpdateCheck` | `apps/freehand-server/assets/webui.js` | render the Android-only APK update Settings card, call only the native WebView bridge for manual checks, normalize native status payloads, and keep desktop or browser sessions explicitly unavailable | Settings button click plus native ApkUpdateStatus payload | visible APK update source, status, phase, and enabled or disabled check button | WebUI Settings shell | Android WebView JavaScript bridge |  |  |  | bound |
| 11b | `loadAttachmentDrafts / persistAttachmentDrafts / addAttachmentFiles / renderAttachmentTray / attachmentsForSubmit / textWithSubmittedAttachmentDisplay / clearCurrentAttachments` | `apps/freehand-server/assets/webui.js` | manage session-scoped attachment draft metadata, render selected image thumbnails and preview/remove controls, send current image payloads through SubmitUserInput.metadata.attachments, clear on successful command receipt, preserve drafts after dispatch failure, and render refreshed submitted attachment metadata without raw payload | selected session plus browser File handles plus metadata-only restored drafts | attachment tray DOM plus neutral submit metadata payload plus metadata-only submitted attachment display | WebUI control layer | ADP SubmitUserInput.metadata.attachments and UiTurnProjection.attachments |  |  |  | bound |
| 11g | `maybeNotifyAndroidTurnFinished` | `apps/freehand-server/assets/webui.js` | call the Android notification bridge exactly once after a live Android WebView turn is observed nonterminal and then terminal | UiTurnProjection session_id, turn_id, terminal status/text, and prior same-turn nonterminal projection | FreehandAndroidNotifications.turnFinished payload with session, turn, status, title, and short text | WebUI turn projection renderer | Android WebView JavaScript bridge | ui_projection | android_notification | ui_projection.post_android_turn_finished_notification | bound |
| 11d | `turnLifecycleForRender / liveTurnStatus / renderTurnMeta / hasNonTerminalProtocolActivity / turnRequiresLifecycleTruthRefresh / turnIsCurrentLiveTurn / turnIsWaitingForModelResponse / buildModelRequestRenderRow / modelRequestStaticStatus / modelRequestTransport / modelRequestTransportPhase / modelRequestTransportLabel` | `apps/freehand-server/assets/webui.js` | render every protocol-projected model/tool activity round, keep selected transcript model_request rows statically visible when they are not current live, keep the latest selected ToolPending lifecycle turn under owner-truth refresh, render provider retry/failover only as model-request transport substate, and make terminal or ToolPending truth override outstanding submit, tool, or model activity | UiTurnProjection lifecycle, model_request, model_request.transport, tool activity, and render state | visible per-round tool/model rows without historical animation, terminal responses mislabeled as dispatching, or provider retry displayed as its own reasoning-flow row | WebUI shell | ui.protocol projection |  |  |  | bound |
| 11c | `applyPhase2QueryResult / refreshPhase2Status / renderPhase2Dashboard / sessionSummaryById / compareTurnIdOrder / compareSessionTurnPosition / sessionTurnCandidates / latestSessionTurnMatching / latestClosedTurnForSession / closedTurnObsoletesObservableTurn / closedTurnObsoletesSessionSummary / activeTurnForSession / sessionLiveObservation / globalLiveSessionObservation / liveObservationLine / buildMobileAgentDashboardModel / renderMobileAgentSummaryStrip / renderMobileAgentSheet / renderMobileAgentTaskList / renderSystemAgentResourceConfig / submitAgentResourceConfigUpdate / setMobileAgentSheetOpen / renderWorkerSessionNavigation / switchConversationSession / selectedSessionIsLoading / loadingConversationBubble / workerTranscriptWaitingBubble / clearSessionRefreshState / renderSessionRefreshFailure / selectedWorkerTranscriptRefreshRetryable / scheduleSessionRefreshRetry / returnToParentSession / renderTaskBoardProjection / renderAgentBoardProjection / phase2AgentLabel / workerOrdinalFromAgentId / openWorkerTaskSession / renderEventInboxProjection / renderTaskHistoryProjection / renderWorkerControlProjection / sendWorkerControl` | `apps/freehand-server/assets/webui.js` | query TaskBoard, AgentBoard, EventInbox, TaskHistory, WorkerControl, ConfigStatus, SessionList, and selected transcript truth through the existing ADP/runtime owner path; render compact read-only status cards; expose live waiting/retry identity as owning session plus turn in the Header/session/Agent surfaces, including the parent Master observation while an inactive Worker child is selected, while a later same-session terminal transcript turn invalidates stale active_turn_id or ToolPending observation and selected sessions never fall back to unrelated global active sessions; derive mobile Header lifecycle buckets from selected-session TaskBoard/AgentBoard truth plus config-owned resource capacity; label Worker resources using Worker-only ordinals so the Master AgentBoard row cannot shift Worker labels; keep the mobile Agents sheet read-only as the current-session complete Worker child-task list with no fixed cap or global-history fallback; expose each Worker task card's data-task-id and owner-projected data-worker-session-id for one-by-one inspection; click a child task through its owner-projected worker_session_id, clear the previous Agent transcript, render an explicit loading state while the pinned QuerySessionTurns request is in flight, treat TaskBoard-confirmed active Worker transcript-not-yet-persisted misses as retryable waiting with task id/status/assignee evidence instead of a persistent connection-failure card, consume reason-persistence logical-turn transcript projections without browser-side semantic trimming, discard late transcript responses for a previous selected session, render non-retryable refresh failures explicitly, and open the Worker conversation without deriving a session id in the browser; expose explicit return to the exact parent Master plus sibling Worker switching; require Header tree Worker rows to be keyed by UiTaskSnapshotProjection.worker_session_id and task_id rather than UI copy, id prefix, or DOM order; render Worker-capacity mutation only in system Config and route it through the config-owner protocol command | UiQueryResult Phase 2 projections plus SessionList active-turn projection plus selected-session turn/model-request projection plus config status projection plus worker-control/resource button intent plus Worker-task/parent-return click intent plus mobile sheet open/close intent | detail-drawer status cards, live session/turn identity, mobile Agent header/task sheet, explicit Worker/Master navigation, selected Agent transcript, selected-session loading state, retryable Worker transcript waiting state, or explicit refresh-failure card, system Config resource control, and user-safe worker-control/resource receipt/status text | WebUI detail drawer, system Config, mobile Agent task sheet, and session relationship Header | runtime owner projections through ADP |  |  |  | bound |
| 12 | `handle_query_checkpoints / refreshCheckpoints` | `apps/freehand-server/src/lib.rs / apps/freehand-server/assets/webui.js` | serve compatibility checkpoint query and render read-only checkpoint summaries from ADP protocol state by default | protocol checkpoint snapshot | checkpoint snapshot plus secondary inspector cards | WebUI shell | ui.protocol state |  |  |  | bound |
| 13 | `cancelActiveTurn` | `apps/freehand-server/assets/webui.js` | send CancelTurn or CancelLatestActiveTurn over ADP for the nonterminal active protocol turn from button or Escape key; terminal or no-active turns clear only local pending input | latest nonterminal protocol turn id | ADP command receipt plus refreshed projection or local clear status | WebUI shell | daemon `/adp` |  |  |  | bound |
| 14 | `handle_command_ingress` | `apps/freehand-server/src/lib.rs` | keep dispatch-port and spawn-blocking join failures explicit at the HTTP compatibility transport boundary | dispatch port error or join error | explicit HTTP 500 failure payload | command ingress | protocol failure mapper |  |  |  | bound |
| 15 | `loadSamplePrompt` | `apps/freehand-server/assets/webui.js` | load hidden success/failure diagnostic prompts into the composer without inventing a second command path or rendering persistent sample buttons | sample kind | composer text plus visible command status | WebUI shell | normal ADP submit path |  |  |  | bound |
| 16 | `initial_adp_subscription_projection` | `apps/freehand-server/src/lib.rs` | serve initial ADP subscription snapshot including runtime-backed task list and error-center projections | subscription command plus protocol state plus runtime query port | optional UI projection or failure | handle_adp_subscribe | UiRuntimeQueryPort::query_runtime / UiProtocolState::query |  |  |  | bound |

## Sync Status Against Mainline Call

- app boundary now renders a usable WebUI shell instead of a minimal text-only smoke
- theme code is split into `assets/theme.css` and `assets/theme.js`
- WebUI layout and protocol-consumer code is split into `assets/webui.css` and `assets/webui.js`
- WebUI shell now advertises `data-adp-endpoint="/adp"` and the front-end opens a WebSocket to that endpoint by default
- WebUI success/failure diagnostic prompts remain available through slash commands and shortcuts, while persistent Success/Failure composer buttons are intentionally absent
- WebUI shell now exposes composer controls for file, image, video, preview, selected-session refresh, and read-only model selection
- WebUI layout shape classifier and CSS shape rules are landed for phone portrait, tall phone, phone landscape, tablet portrait, tablet landscape, foldable unfolded, and desktop large
- WebUI root route now pins `?client=android-webview` first paint to `tablet_portrait` on the server-rendered body and shell while leaving normal Web roots unpinned for browser classification
- WebUI new/empty selected sessions now render a clean empty conversation prompt and do not project previous latest-active turns or generic waiting-data system feedback into the selected session
- WebUI attachment drafts are session-scoped in local UI metadata, render as selectable image chips with preview/remove controls, submit image payloads through neutral metadata attachments, clear after successful command receipt, and remain for retry after dispatch failure
- WebUI Android notification bridge fires only for observed live nonterminal-to-terminal turn transitions and is covered by fixed-session browser proof
- app boundary now serves protocol-only HTTP query and SSE subscribe smoke routes from a reusable protocol-only library surface
- app boundary now serves protocol-only POST command ingress dispatch-receipt or failure smoke route from that shared transport surface
- HTTP/POST routes remain compatibility transport surfaces; WebUI JS uses ADP for command/query/subscription truth and EventSource for latest-turn SSE display refresh
- app boundary now surfaces explicit command-ingress dispatch-port failures and dispatch-task join failures instead of collapsing them into success
- app boundary now serves static embedded assets through an explicit 404ing route
- runtime host reuse now happens through injected state and dispatch port, not by duplicating transport behavior
- protocol-owned client-specific projection helper exists and is now a shared owner boundary for the app smoke
- subscribe routes now keep one SSE connection open and stream later matching updates after the initial snapshot
- debug subscribe route now also keeps one SSE connection open when the first debug snapshot is not available yet
- WebUI submit path still explicitly refreshes latest turn truth over ADP after a successful command receipt
- WebUI checkpoint panel now refreshes protocol checkpoint summaries and sends explicit rewind commands without parsing runtime files
- app dependency boundary is intended to remain protocol-only and must not import reason, provider, node, or config semantics
- app query transport now accepts an injected UiRuntimeQueryPort while remaining free of runtime/task/error-center imports
- app subscription transport now uses the injected UiRuntimeQueryPort for task list and error-center initial snapshots while keeping app dependency boundary protocol-only
- WebUI session rail now exposes rename, remove via DeleteSession, and double-Esc rollback as protocol commands over ADP instead of local session truth; archive/restore controls are intentionally absent
- WebUI Phase 2D status drawer now renders TaskBoard, AgentBoard, EventInbox, task history, and WorkerControl from runtime owner query results; it does not write browser-persisted task/control truth
- WebUI mobile Agent Dashboard Worker labels now use Worker-only ordinals so configured worker/worker-2/worker-3 render as Worker 1/2/3 even when AgentBoard includes Master
- WebUI WorkerControl buttons now send WorkerControl commands through ADP and re-query the owner projection; the visible receipt hides internal feature ids
- generated wiki must be regenerated from `docs/mainline-calls/app.webui-smoke.json` when this function-map truth changes
- WebUI Cancel button and Escape key now send CancelTurn through protocol command ingress instead of only clearing local input
- WebUI cancel path now covers the submit-in-flight window with CancelLatestActiveTurn
- scripts/verify-webui-stop-continue-online.mjs proves fixed-session provider retry renders before Cancel, cancellation materializes ADP Cancelled truth and clears browser live state before retry backoff completes, the cancelled card remains visible, a later prompt appends a distinct Success turn in the same persisted session, and no random persisted session id is created
- WebUI tool cards now render protocol-projected waiting/completed/failed lifecycle states from ADP turn projection truth without same-tool coalescing
- WebUI tool cards now render protocol-structured display fields including action, kind, target, parameter_summary, result_summary, summary, every field, command, diff before/after, and raw detail lines, and do not read obsolete display.result
- WebUI Header now owns Master/Worker relationship navigation through a compact dashbar plus half-screen dropdown session tree, with Header Back to Master when a Worker transcript is selected
- WebUI chat messages now render per-turn Copy, Edit from here, and New session actions; edit-from-here routes through persistence-owned RollbackLatestSessionTurn instead of deleting DOM history
- WebUI same-tool lifecycle updates no longer normalize by tool_call_id; waiting cards animate, elapsed timers render locally, and submit clears the composer immediately while retaining pending state in the conversation stream
- WebUI selected-session transcript refresh renders nonterminal protocol model_request rows with static waiting/retrying/switching status when they are not current live, terminal truth overrides outstanding submit activity, Header/session/Agent surfaces expose the owning live session and turn, and scripts/verify-webui-live-tool-render-online.mjs scopes fixed-session evidence to the current run marker and newly added base/continuation turn ids while proving unique real-turn cycle keys, live tool cards plus continuation waiting before final summary, and zero live rows after current-run terminal success
- WebUI request-cycle cards now key real protocol turns by `turn:<session_id>:<turn_id>` instead of submit id, so multiple runtime rounds from one user submit append as separate chronological cards while pending/accepted receipt cards still update by submit id
- WebUI inactive text-only or restored turns render as neutral waiting or active state instead of fake streaming; animation requires submit-in-flight, waiting tool activity, or protocol-projected model request truth
- WebUI missing-debug race is locked by pending-state rendering plus late-debug ADP subscription coverage; ADP failure frames render as visible failure cards/status instead of stale pending
- WebUI settings provider definition save submits UpsertProviderConfig over ADP, active provider switch submits UpdateAgentProviderSelection, both show validation errors or restart-required success, re-query owner-backed config projection, and avoid raw API-key values/direct config writes
- WebUI settings includes an Android-only APK update trigger/status card that delegates all check/download/install semantics to the Android bridge owner and renders desktop/browser use as unavailable
