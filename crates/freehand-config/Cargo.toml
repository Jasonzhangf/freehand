[package]
name = "freehand-config"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
freehand-contracts = { path = "../freehand-contracts" }
serde = { version = "1", features = ["derive"] }
thiserror = "2"
toml = "0.9"
