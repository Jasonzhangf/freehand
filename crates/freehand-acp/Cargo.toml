[package]
name = "freehand-acp"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
freehand-contracts = { path = "../freehand-contracts" }
freehand-runtime = { path = "../freehand-runtime" }
agent-client-protocol = "2.0"
tokio = { version = "1", features = ["io-util", "sync", "rt", "rt-multi-thread", "macros"] }
